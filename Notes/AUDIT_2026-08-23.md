# Crooked Galaxy — living audit (refreshed 2026-08-29)

## Reference boundaries

The local Shakes & Fidget material remains study-only under `References/` and is excluded from Godot imports. Crooked Galaxy does not reuse its proprietary code, formulas, names, text, protocols, interface, or assets.

The useful general observations remain limited to product principles: repeatable loot needs legible comparison, long progression benefits from stable collections and feature hubs, and large catalogs benefit from reusable visual/data boundaries. Crooked Galaxy implements these ideas independently through original rules, content, procedural presentation, stable IDs, and deterministic validation.

## Current product state

- The complete bounty loop is playable: board, approach, timed hunt, incident, automatic combat, reward, loot decision, workshop, and stronger contracts.
- Thirty-five four-target worlds through level 320 provide the complete launch axis of 140 original wanted targets, 70 planet-specific incidents, bosses, equipment families, level-banded progression, and persistent travel.
- Repeat motivation includes capture streaks, three mastery tiers per target, improved loot quality, one-time mastery scrap funding, career milestones, AFK patrol rewards, and a persistent wanted archive that recommends the closest unfinished mastery tier.
- Loot supports rarity, four combat-relevant modifications, planet of origin, and a two-piece planetary kit bonus. Item comparison evaluates the complete build rather than raw power alone.
- The arsenal provides filters, sorting, explicit tradeoffs, item protection, two loadouts, safe bulk recycling, power calibration, and capped integrity reinforcement with partial investment recovery. Both workshop paths require Scrap plus a level-scaled Credit service fee.
- The arsenal's field test translates both workshop paths into projected odds against the next warrant before the player spends scrap, holding the briefing's currently recommended approach fixed for a fair comparison.
- Mobile presentation uses safe areas, large touch targets, vertical inventory scrolling, procedural portraits/backdrops, and runtime-synthesized audio.
- The keyed Fenda now spans three independent twelve-enemy realities, one shared UTC entry, gameplay-only keys, sealed first-clear rewards and class-neutral encounter envelopes through level 215.
- The nine-slot workshop services and projects every equipped item through one mobile dossier; advanced Rift artifacts preserve their authored economy level.

## Engineering state

- `game_state.gd` owns the phase machine and persisted transactions; deterministic rules are isolated in focused modules.
- `ui_factory.gd` owns shared presentation primitives; `arsenal_view.gd`, `reward_view.gd`, and `career_view.gd` own the complete inventory/workshop, loot-decision, and career/archive renderers.
- Saves use explicit incremental migrations and preserve interrupted briefing, hunt, combat, reward, chapter, loadout, and workshop state.
- `tools/check_project.ps1` runs 62 deterministic suites, repository/document/reference gates, and a clean boot. Each run uses isolated log files, while persistence tests use unique workspace-local saves so open Godot instances cannot corrupt test results. Dedicated balance guards protect onboarding, mission approaches, tactical equipment, long-horizon progression, mobile interaction and the planetary network.
- Balance simulation executes the same damage helpers as runtime combat and verifies displayed odds against 5,000-fight samples.
- A separate 100-career simulation follows the first chapter's mastery path through real loot generation, automatic upgrade decisions, XP, and recommended approaches, exposing the loot-only upper bound without conflating it with individual fight calibration.
- Long-horizon simulations carry real loot, XP, mastery scrap, workshop spending, fuel profiles, recommended approaches and sampled defeats through the implemented network and annual production ceiling.
- Automated UI guards cover the main loop, reward decisions, career/archive, arsenal, galaxy, incidents, Fenda, onboarding, translations, expanded text and Android-safe navigation.

## Findings addressed during this audit

1. Equipment modifications existed, but the two slots still behaved mostly as isolated stat containers. New opening-shot and incoming-damage traits now create distinct tactical identities.
2. Loot origin was flavor-only. Every newly generated item now records its planet, and matching weapon plus armor activates an explicit `+1 power / +6 health` planetary kit.
3. Raw item comparison could discard a weaker-looking piece that improved the complete build. Reward recycling, bulk recycling, upgrade labels, combat odds, and simulator decisions now evaluate the equipped build and its kit transition.
4. The 450×800 arsenal capture exposed horizontal clipping and a collapsed inventory area. Workshop and loadout cards were compacted, sorting received its own row, item comparisons moved into the flexible details column, and mobile tests now enforce horizontal bounds plus useful inventory height.
5. Concurrent Godot instances shared default logs and fixed test saves. The checker and persistence suite now isolate both resources per process.
6. Early planet progression was visible only beneath the contract list, and repeating a threshold capture could bypass the newly unlocked warrant. The next-target objective now sits above the board, reward screens preview exact capture progress, and threshold rewards route the first visit back to the expanded board with the new target named explicitly.
7. Automatic combat exposed only the latest exchange, so players could not tell what their build contributed. Combat now accumulates exact turns, damage dealt/received, criticals, mitigation, opening bonus, and active kit; victory shows a compact report, while defeat keeps a red diagnostic on the board with the target's remaining health. Round state is saved as it advances so this evidence also survives interruption.
8. Reward rendering had become a dense branch inside the main phase controller. It now lives in `reward_view.gd` with explicit state/host dependencies and isolated coverage for normal repetition, threshold unlock routing, board return, and safe instant recycling; `main.gd` is back near one thousand lines.
9. Workshop buttons exposed prices and raw stat gains but not their practical value. The arsenal now compares current, `+1 power`, and `+8 health` builds against the next warrant. Same-target projections share a deterministic combat roll stream, so genuine upgrades cannot appear worse because of Monte Carlo noise; the 5,000-fight simulator remains aligned with displayed odds.
10. The wanted archive documented mastery but did not convert it into a next action. Career rules now select the closest unfinished mastery tier, the screen explains the next rare/epic loot bonus, and a direct action opens that target's contract briefing. The same route remains highlighted on the bounty card after returning to the board. Reward screens count the pending capture, preview the next mastery threshold, and announce a level before it is claimed. The touched renderer moved from `main.gd` into `career_view.gd` with isolated coverage.
11. A scripted repeat path across 100 deterministic careers found a 99% median arrival chance after the first mastery tier when every real loot upgrade is equipped. A failure-aware pass initially measured defeats in 8% of Gloop careers, 7% at Barão, 4% at Madame, and 3% at the boss, while the workshop median remained at zero upgrades until Madame. Raising warrant stats collapsed separately calibrated build scenarios, so that trial was rejected. Instead, each newly earned mastery tier now grants a one-time 6/10/14 scrap reward: the simulator shows one workshop action before Barão, two before Madame, and three before the boss, while all combat guard rails remain unchanged. The reward is previewed in a mobile-readable two-line block, exact in board feedback, and can route directly into the funded workshop—even when the same capture reveals a new warrant—without removing the direct board route.
12. Entering the workshop from a combined mastery/warrant unlock exposed a target-selection error: the field test skipped the newly opened uncaptured warrant and projected the following locked tier. It now keeps the current highest available target until its first capture, then advances to the next warrant; the card labels the distinction as `MANDADO ATUAL` versus `PRÓXIMO MANDADO`.
13. Arsenal transactions relied on changing totals and button states for feedback, so the mastery funding message disappeared on arrival. A compact workshop record now carries the latest exact state notice through mastery routing, equipment changes, recycling, loadouts, and upgrades.
14. Field-test odds still required the player to compare four different workshop costs mentally. Every affordable power or integrity action is now simulated against the focused warrant; exactly one button receives a star for the best odds gain per scrap, with complete-build gain as a deterministic tie-breaker when displayed odds are saturated.
15. After preparing a newly opened warrant, the player still had to return to the board and find it again. When the field-test target is available and uncaptured, the panel now links directly to its briefing; future locked targets remain projections without an invalid action.
16. Alternate-strategy simulation exposed that nine Gloop captures could unlock the boss with a 95% median theoretical win chance, bypassing both intermediate warrants. Planet tiers now advance sequentially: three captures of the current prerequisite target unlock the next warrant. A repeated 30-career comparison unlocks the boss in 100% of mastery-route runs and 0% of sample-then-farm or Gloop-only runs. Total planet captures remain career statistics, while board and reward progress name the exact target required. The obsolete total-capture tier helpers were removed to prevent two contradictory progression models, and completed planets remain pinned to tier 3 for legacy-save compatibility.
17. The first continuous four-planet sample found no progression wall, but did expose long-campaign power inflation: median arrival power rises from 12 to 46 across Dustball, then reaches 77, 110, and 155 at successive chapter bosses, with nearly every recommended contract at 99%. This is now the blocking product evidence against adding a fifth chapter—not a technical blocker—and the campaign simulator records odds, low-odds incidence, power, and health at every target for controlled curve work.
18. The long-campaign saturation came from a hidden feedback loop: approach-modified enemy power also inflated dropped equipment, so the routinely recommended fast route financed its own future power advantage. Loot tier now uses the canonical target power; approaches alter the current encounter and advertised credits/XP only. A trial that additionally escalated risky multipliers by planet was rejected after telemetry drove both late alternatives as low as 1%. With canonical loot and stable multipliers, the 20-career sample reaches the final boss with `REDE SILENCIOSA` at 99%, `ESCOTILHA` around 60%, and `MANDADO CORPORATIVO` around 16%; the recommender genuinely alternates between safe and fast instead of seeing three saturated options. The campaign report now records the selected approach and all three arrival odds at every warrant so this distinction remains visible.
19. Once loot inflation was removed, the corporate warrant had no workshop identity despite carrying the highest combat risk. A successful corporate capture now pays two explicit scrap: three such wins equal the first +6 mastery grant, so the repeatable option complements rather than eclipses the milestone. Briefing, recommendation value, reward preview, state transaction, toast, and board record all account for it. The campaign simulator now compares the normal recommendation against an opportunistic corporate strategy and reports both corporate scrap and resulting workshop actions. In the deliberately success-assuming upper-bound cohort, the strategy earns 58 corporate scrap and completes 39 workshop actions versus 32 on the recommended route, yet arrives at the final boss with slightly lower median power (146 versus 148) because its XP tradeoff remains active; it improves build agency without dominating progression.
20. Workshop field tests still measured the canonical target while the actual briefing asked the player to choose a modified contract, making late-boss projections saturate and disagree with the next screen. Approach evaluation is now centralized in `contract_rules.gd` and shared by briefing, simulator, and arsenal. The workshop fixes the currently recommended approach while comparing all upgrade outcomes, names it explicitly, and still opens the canonical target briefing. A boss-ready Omega profile now projects `ENTRADA PELA ESCOTILHA` at 74%, +1 power at 75%, and +8 integrity at 81%; the dedicated 450×800 capture keeps the complete card and actions legible.
21. The continuous campaign previously assumed every displayed attempt won. It now samples defeat from the same deterministic combat odds, resets capture streaks, reevaluates approaches when that reset can change expected return, retries each capture up to a hard safety limit, and grants loot/workshop progress only after success. Across 20 careers per strategy, neither the recommended route nor the opportunistic corporate route stalls. The recommended cohort reaches Omega at 95% median arrival, then actually fights at 89% median (57% minimum) with 15% of careers seeing a boss defeat before recovery. The corporate cohort earns 62 median scrap and 39 workshop actions, still has 0% stalls, and reaches the boss at 93% arrival / 81% attempted median. Per-attempt approach and odds telemetry also exposes rare transient states below the 55% recommendation floor when every available route is weak, rather than hiding them behind arrival medians.
22. The failure-aware endpoint supported a fifth chapter without changing the validated progression machinery. Cassino Quasar adds four original warrants, two probability-themed incidents, a neon backdrop, a complete equipment family, four procedural portraits, a chapter milestone, and persistent travel through the existing catalogs. Initial boss health produced occasional sub-55% attempts; tuning reduced only fifth-chapter health. The full 20-career sample then completed both strategies with 0% stalls. Recommended play made no attempt below 55%, alternated quiet/fast routes throughout the chapter, and saw 30% of careers lose at least once to A Casa Eterna before recovery. Opportunistic corporate play earned 68 median scrap and 44 workshop actions without dominating final power, made no fifth-chapter attempt below 55%, and also remained stall-free. Dedicated 450×800 captures verify the board, incident, finale, and full twenty-character portrait sheet without clipping; static boss-ready guards now protect the intended safe/fast/corporate spread.
23. Growing to twenty targets made the career ledger a thirty-plus-card scroll with no direct archive navigation, while galaxy cards described every unlocked destination merely as an open route. The career header now provides 48-unit progress/archive shortcuts and keeps the mastery action visible above either destination. Galaxy cards distinguish completed chapters from open routes and name the current warrant plus its exact capture requirement. Updated captures use coherent progression state and verify the first archive entry appears immediately after the shortcut; UI, isolated-renderer, and mobile guards cover the new controls.
24. The threshold reward announced a newly revealed warrant but forced the player to choose between going straight there and visiting the workshop without any risk context. It now evaluates the post-loot build with the same centralized approach rules as the briefing and displays the recommended route plus projected win chance before the loot decision. The label explicitly distinguishes an equipped upgrade from the unchanged build, preserving truthful guidance when the item is not actually better.
25. Defeat diagnosis explained the remaining enemy health and suggested build changes, but recovery still required finding the global arsenal action. The diagnostic card now links directly to the field-test workshop, where the same active warrant and recommended approach drive upgrade projections. The 44-unit action remains inside mobile bounds and preserves the visible rematch card below it.
26. The workshop normally projects the following warrant after a target's first capture. During defeat recovery this could replace the failed contract with a still-locked future tier, breaking the promised diagnosis→upgrade→revenge loop. An active defeat now takes precedence over ordinary forecasting: the field test restores the canonical failed target from the persisted combat report, labels it as `REVANCHE`, simulates upgrades against its recommended applied contract, and exposes the existing one-tap briefing action. Coverage reproduces the real post-defeat state with an empty transient bounty, plus the ambiguous one-capture tier, and verifies that the defeated target wins focus even after state restoration.
27. A real calibration transaction during defeat recovery now has dedicated regression and visual coverage. Spending scrap updates equipment and projected odds without clearing the persisted combat report; the exact calibration record remains visible beside the `REVANCHE` field test, and the one-tap contract action remains available. This closes the diagnosis→upgrade→evidence→retry chain rather than validating only its entry point.
28. Defeat reset the capture streak correctly but communicated it only by removing the board banner, obscuring the pacing cost and the next reward baseline. The combat report now persists the exact lost streak, includes it in the board notice, and explains that the next successful capture restarts at ×1. Flow and UI coverage use a real ×4 loss, while the defeat capture verifies the message remains readable beside diagnosis and workshop recovery.
29. A deterministic recovered-third-capture scenario now proves the systems compose correctly: losing at ×4 persists the loss and resets to zero; the revenge victory restarts at ×1 with no phantom credit bonus; the third target capture still grants mastery 1/3, exactly six workshop scrap, and the next sequential warrant. A dedicated reward capture exercises the same combined threshold after the visible workshop upgrade.
30. The recovered reward had no phantom streak bonus, but silence at ×1 made it unclear whether embalo had restarted. Every sequence-opening reward now states `EMBALO REINICIADO ×1` and explains that the credit bonus begins on the next consecutive capture; later rewards retain the exact paid bonus. This also teaches the system on the first-ever reward without another tutorial surface.
31. The ordinary reward offered a repeat action without showing why repetition was valuable. It now previews the next capture's exact streak multiplier, percentage, and added credits immediately above the repeat choices. Rewards that cross a warrant threshold omit this prompt, preventing the old contract's streak value from competing visually with the newly opened progression target.
32. Auditing repeat-to-briefing exposed that the first preview overpromised an exact added-credit value based on the completed contract, even though the player can select a differently priced approach and incidents can modify payment again. The preview now guarantees only the invariant multiplier and percentage `sobre o pagamento`; the briefing remains authoritative for each route's total. Flow coverage proves quiet and premium routes share the advertised streak percentage while producing different absolute bonuses.
33. Repeated-contract briefings displayed streak-adjusted payment totals without saying the bonus was already included, inviting players to add the advertised percentage twice. Centralized approach evaluations now expose streak, percentage, and absolute included bonus alongside total credits. Every affected approach card labels that inclusion explicitly; no label appears at ×1, and recommendation scoring continues using the same total payout.
34. Incident choices described relative credit changes but left the player to combine them with the selected approach and active streak. Every choice card now applies its consequence to the live contract, runs the canonical streak transaction, and shows the resulting victory payment plus the exact embedded streak amount. Cost remains separate in the choice title, so projected gross reward is not confused with credits spent to take the option.
35. Once combat began, the selected incident and its economic consequence disappeared behind the automatic encounter. Combat now carries a compact incident summary above the arena with the applied result, final projected payment, and any embedded streak amount. The value uses the already-mutated live contract, so it remains identical to the later reward transaction unless the player abandons before combat.
36. Paid incident choices showed their immediate cost in the title and gross victory payment below, still requiring subtraction during a timed decision. Only paid options now append the net contract gain after cost; free choices remain compact. Flow coverage checks the preview against the same charged-cost and canonical payout baseline.
37. Paid incident cost disappeared from the contract after it was charged, so the eventual reward returned to a gross-only receipt even though the earlier choice promised a net value. The mutated contract now retains the already-paid cost. Reward UI and board notice reconcile gross credits, cost, and net contract gain; a complete briefing→paid incident→combat→claim test proves the final wallet delta equals the earlier projection exactly.
38. Adding the paid-incident receipt pushed the densest ordinary reward beyond the 450×800 fixed layout and visually displaced its persistent header. The loot card now uses compact mobile padding, seven-unit row spacing, and a smaller emblem while retaining all decision evidence and full-width actions. This recovers more height than the receipt consumes instead of hiding information or shrinking touch targets.
39. The automatic encounter retained incident economics, but the victory beat reverted to an indefinite “authenticating” message before the same numbers returned on reward. Victory now approves the canonical gross payment, embedded streak amount, and net balance after paid cost above the loot transition. The UI smoke path uses a real paid choice and verifies the receipt survives incident, combat, victory, and reward as one continuous transaction.
40. Claiming reward applied equip/store/recycle correctly, but the final contract notice overwrote the temporary equipment message and preserved only currencies. Claim summaries now carry the exact item name and action, and the persistent board/workshop record names equipped, stored, or recycled loot. Recycling also names the destroyed item alongside recovered scrap, closing the reward→inventory handoff with auditable evidence.
41. Ordinary rewards expressed upgrades as build deltas but did not translate them into the next bounty, while threshold rewards showed only post-equip odds. Every reward with a progression target now fixes the approach recommended for the projected build and compares that same route before and after equipping. This makes the loot→stronger bounty relationship explicit without allowing recommendation changes to masquerade as item gains.
42. The first next-hunt comparison simulated the pending item but omitted XP applied by the same claim. A reward that crossed a level threshold could therefore understate the workshop's immediate field-test odds, while weak loot could receive credit for a level gain. The projection now applies pending XP and equipment to a duplicate player, labels unequipped gains separately, and shares a pure result that is verified exactly against the post-claim field test.
43. Ordinary equipped rewards returned to a board that retained the receipt but exposed only a generic arsenal label, making the advertised next-hunt projection harder to verify than the mastery route. While the canonical equipped receipt remains active, that existing shortcut now becomes `TESTAR BUILD` and opens the same workshop field-test card. Stored and recycled receipts retain the ordinary arsenal action, so the context does not claim an equipment change that did not happen.
44. The field-test action said `CAÇAR AGORA` but intentionally called `select_bounty`, which opens the three-route briefing rather than starting a hunt. The CTA now says `ESCOLHER ROTA`, and the isolated flow proves that the briefing recomputes the same recommended route shown by the workshop after the upgrade. This preserves player agency without making the handoff look like a broken instant-start action.
45. Even after the CTA correction, the workshop recommendation disappeared as soon as the briefing opened, forcing the player to rediscover which route had produced the tested odds. The field-test handoff now carries transient target, approach, and odds context; the matching briefing displays a `BUILD TESTADA` confirmation only when its live recommendation still agrees. Choosing a route or leaving clears the context, and ordinary board/career entries clear or never create it.
46. Clearing briefing context on approach selection also erased evidence when the player deliberately rejected the tested route. Approach selection now converts transient UI context into a persistent contract record naming tested route, tested odds, chosen route, and whether it was overridden. The hunt distinguishes `TESTE DE CAMPO CONFIRMADO` from `ROTA TESTADA SUBSTITUÍDA`, and save/load coverage keeps that evidence through an interrupted hunt event.
47. The persisted tested-route record remained in the contract during incidents and combat but was rendered only by the timed hunt screen. A shared compact renderer now carries the identical confirmation/override string through the paused incident and automatic encounter. Combat still names the active approach in its header and the applied incident/payment separately, so the new line explains decision provenance rather than duplicating those outcomes.
48. Victory previously dropped the tested-route provenance one beat before presenting its combat and payout evidence. The capture stamp now closes that trail with the same confirmed/overridden record. Reward intentionally omits it: UI coverage proves that the next phase replaces old-route provenance with pending loot, transaction, progression, and next-hunt odds instead of accumulating another receipt line.
49. Defeat clears `current_bounty`, so tested-route provenance was lost exactly where it could improve recovery advice. Combat initialization now snapshots that context into the persistent combat summary. An overridden tested route produces `OVERRIDE DERROTADO` and recommends reevaluating route choice; a failed confirmed route points instead to build or incident. The board diagnosis and revenge field test reuse the exact same text, while ordinary defeats remain unchanged.
50. The twenty-target career archive had section-level navigation but every target record was read-only, so revisiting a specific warrant still required leaving career, opening the galaxy map, traveling, and finding the board card. Revealed targets that are currently available now expose a 48-unit `ABRIR` action that performs validated planet travel and opens the selected briefing. Revealed records whose sequential tier is not yet available remain actionless, and isolated coverage proves both lock protection and cross-planet routing.
51. Direct warrant actions exposed another late-career navigation cost: the fixed chronological archive still buried the active planet beneath as many as sixteen unrelated records. The archive now orders the current planet's four warrants first and retains canonical order for every other chapter. Integrity coverage proves that all twenty unique records remain present, while a Cassino Quasar capture verifies the useful actions fit the mobile viewport immediately.
52. Claiming a career milestone emitted a full state rerender and silently reset the long ledger to its first card. The UI factory now retains the career scroll offset as transient session state, the scroll bar keeps it current, and rebuilt ledgers restore it after layout. Integration coverage claims a real milestone from the archive position and also leaves/reenters the career hub, proving both paths return to the exact same section without expanding the persisted save schema.
53. Career claims updated totals and cards but left their exact transaction text visible only after returning to the bounty board. Individual and bulk claims now render a compact in-career receipt with exact credits and scrap. Capturing that new post-transaction state exposed a shared renderer defect: queued old children remained attached during the synchronous rebuild, causing duplicate node names and enough temporary layout pressure to clip the top of the 450×800 screen. Rendering now detaches the old tree immediately and frees it safely at frame end; integration coverage verifies stable named structure after a real claim, and the dedicated receipt capture confirms the full header remains in bounds.
54. `last_notice` was shared across hubs without provenance. The board parsed Portuguese text to decide whether `TESTAR BUILD` was valid, and the arsenal mislabeled travel, career, or system messages as workshop records. Notices now carry a transient explicit context alongside their display text: equipped/stored/recycled reward receipts are distinct, career claims own their receipt, workshop operations own theirs, and unrelated contexts are filtered at each specialized view. The save schema remains unchanged because both fields are session feedback. Flow coverage verifies every loot action's context, UI coverage proves equipped-looking stale text cannot forge an action, and isolated arsenal coverage rejects a travel receipt.
55. The remaining hub shortcuts no longer infer behavior from presentation text, but the mastery directive still promised `CAÇAR AGORA` while correctly opening the three-route briefing. Its CTA now says `ESCOLHER ROTA`, matching the field-test handoff and preserving player agency. The same navigation audit found that a full development reset rebuilt gameplay while retaining transient inventory filters, tested-briefing context, and deep career scroll; a centralized UI reset now restores all hub defaults before the new progress state renders. Isolated career and factory coverage protect both semantics, and the mobile capture confirms the replacement CTA fits its existing action slot.
56. Save restoration correctly persisted confirmed contract decisions while leaving pre-choice UI hints transient, but reusing a state instance could carry an old receipt into the loaded session. More seriously, syntactically valid damaged JSON assigned unverified values directly to typed phase dictionaries and could fail before rendering. Loading now clears runtime-only feedback first, type-checks bounty, loot, incident, and chapter objects, reconstructs the three approaches for an otherwise valid interrupted briefing, and reconciles incoherent phases back to a clean board while preserving valid player progress. Repairs are saved immediately. Persistence coverage proves stale receipts cannot survive, malformed reward state preserves credits and wins, and a recoverable briefing is repaired rather than discarded.
57. Repairing only the phase still allowed malformed compound player fields to reach equipment, inventory, and career renderers. The load boundary now compares player fields with the canonical default shape, keeps compatible scalar progress, merges safe equipped-item extensions over starter requirements, drops structurally invalid inventory entries, and normalizes equipment loadouts to exactly two dictionary slots. This remains runtime validation rather than a schema migration: migrations still own version-to-version meaning, while sanitization prevents damaged current-version data from violating renderer contracts. The damaged-save fixture now combines invalid equipment, inventory, loadouts, and capture maps with valid credits/wins and proves both safety and progress preservation.
58. Successful save repair was silent, so a player could notice fallback equipment or a closed interrupted contract without knowing that valid progress had survived. The load transaction now emits one session-only, system-owned notice: `SAVE ATUALIZADO` for a version migration and `SAVE RECUPERADO` for malformed current data. Both emphasize preserved progress and reconstructed/isolated records without exposing JSON, types, or file paths; neither is persisted, so the message cannot recur after a clean reload. Persistence coverage verifies the distinct wording and provenance for legacy and damaged fixtures.
59. Structurally valid numeric values could still violate progression invariants: negative scrap, level zero, negative equipment power, or a best streak below the active streak all passed shape validation. Loaded numerics are now normalized to their canonical integer/float types, currencies and counters use nonnegative lower bounds, level and base power use a minimum of one, capture maps discard nonnumeric entries and clamp negative counts, and integrity upgrades respect the existing rules cap. No arbitrary upper bound was added to level, power, currency, or lifetime progress, preserving legitimate long careers. The damaged fixture proves exact lower-bound and streak-coherence repairs.
60. Unknown identifiers were structurally valid but semantically active: ninety-nine captures of a nonexistent target could satisfy a repeat-career milestone, duplicate completion IDs inflated archive summaries, and an invalid current planet relied on visual content fallback while remaining corrupt in state. Loaded capture maps now accept only the twenty canonical targets and five planets; completion and claimed-milestone arrays are canonicalized and deduplicated; an invalid or locked current planet resolves to the furthest legitimately unlocked route; and protection/loadout references survive only while their item is owned. The fixture proves unknown records cannot influence progression and that a completed Dustball save repairs to Congelária rather than silently jumping further.
61. A nested equipment dictionary still trusted its contents: a known trait ID could claim forged `+999` modifiers, paired unknown origins could activate a phantom kit, and arbitrary rarity/color strings reached the arsenal. Loaded items now restore known traits from the slot-specific canonical catalog, remove unknown traits and origins, align rarity colors to Comum/Raro/Épico, and retain valid workshop investment within existing bounds. The same canonicalizer runs on pending loot before phase reconciliation, so a usable interrupted reward remains in `REWARD` while its forged nested payload is repaired. Persistence coverage exercises both owned inventory and pending reward cases.
62. Valid bounty and incident dictionaries could still forge encounter economics by retaining a known ID beside altered power, credits, approach multipliers, or event choices. Hunt choices now stamp a stable choice ID onto the live contract. Loading reconstructs the target from the twenty-entry catalog, reapplies only a canonical approach, reapplies the canonical incident choice by ID (or legacy result text), normalizes tested-route names/override state, replaces offered approaches and hunt events from their catalogs, and compares nested payloads with numeric-value equivalence so JSON `2.0` does not trigger false recovery against canonical `2`. The forged fixture remains in `REWARD` with its real selected route/outcome but loses `99×` multipliers and fake payouts.
63. Restored evidence remained more trusted than restored decisions. An unknown `combat_summary.target_id` could route defeat recovery to a fallback target, forged target/planet names could enter reports, impossible remaining HP could distort diagnosis, and chapter completion accepted any planet/target dictionaries. Combat summaries now require a canonical target, restore its name, accept only known kit origins and route contexts, clamp metrics and remaining health, and disappear if identity is invalid. Chapter summaries require a canonical boss belonging to the canonical planet, restore both catalog records, and clamp displayed totals. Persistence fixtures prove forged names, phantom kits, excessive HP, and negative finale totals cannot survive while valid evidence does.
64. Interrupted logs and clocks were the final unsanitized phase state. Combat rows now require a catalog player/enemy attack, normalize damage and quality, and discard invented actions; starting any new briefing or direct bounty also clears the previous fight's rows alongside its summary. Invalid hunt timestamps restart the same applied contract with canonical duration, future starts are shifted without changing saved duration, paused-event time is nonnegative, and legitimately expired hunts still advance through the existing AFK path. Resumed combat clamps player/enemy HP to build/contract maxima and rounds to zero or greater. Persistence coverage exercises every repair without discarding the active phase.
65. Post-hardening gates confirm the repair boundary did not alter progression or presentation. The 100-career first chapter still unlocks the boss in 100% of sequential mastery routes and 0% of both farm bypasses, with no recommended attempt below 55%. The 5,000-fight profiles remain aligned with displayed estimates. Across twenty full careers per strategy, recommended and opportunistic-corporate play both report 0% stalls; recommended has no sub-55% attempt, corporate still earns 68 median scrap and 44 workshop actions without exceeding the same 204 final power, and neither strategy has a sub-55% Cassino attempt. All primary captures regenerate successfully; sampled career receipt, paid-reward workshop, and Quasar incident screens remain within 450×800 bounds.
66. A repaired save could also resolve elapsed patrol time, causing separate recovery and AFK banners to compete above the first actionable contract. The board now folds the system-owned recovery explanation into the AFK return card, and its single acknowledgement clears both session messages together. Other notice contexts remain independent. UI coverage reproduces the combined state and verifies the exact dismissal semantics; the 450×800 capture keeps payout, recovery evidence, action, and first warrant visible without clipping.
67. Defeat produced both a short escape receipt and a complete combat diagnosis, repeating the target and streak loss before the player reached the recovery action. The board now suppresses only that redundant defeat-context receipt while the diagnostic heading retains the canonical escaped target. Combat metrics, tested-route evidence, lost streak, and the workshop CTA remain together; any claimable career rewards stay counted as a secondary header action. A dense late-chapter capture with three ready milestones keeps the diagnosis, progression bar, and full first warrant inside 450×800.
68. Receipt lifetime was already mostly action-driven: choosing a target clears reward/workshop feedback, and travel replaces it with the new route result. Standalone save recovery was the exception, because it had neither an acknowledgement nor a gameplay transaction guaranteed to replace it. System recovery now exposes a guarded `OK` action that clears only the matching context; combined AFK recovery retains its shared acknowledgement. Flow coverage explicitly protects natural gameplay-receipt expiry, while UI coverage and a dedicated 450×800 capture verify standalone dismissal without reducing the useful bounty area.
69. First-session captures showed that the reward screen taught the initial `×1` streak, then the board immediately repeated it in a separate forward-looking banner beneath the contract receipt. Sequence-opening receipts now include the exact `×1` start/restart and next `+5%` outcome themselves, suppressing only the duplicate board streak card while that reward context is active. Established `×2+` streaks retain the reminder. Dedicated first-reward and first-claim captures keep the primary repeat choice, equipped upgrade, next-warrant progress, career reward count, and full first contract visible within 450×800.
70. The initial reward also presented both warrant progression and target mastery as unrelated `1/3` counters, before either term had produced an outcome. Mastery progress now stays hidden on capture one, appears as an imminent `2/3` objective on capture two, and retains the complete level/loot/workshop announcement on capture three. The first screen therefore prioritizes loot, embalo, and the next warrant without removing any system or delaying mastery beyond the action that makes it meaningful. Isolated renderer coverage protects both staged states.
71. The first equipped item promoted the board shortcut to `TESTAR BUILD` even with zero scrap and a still-locked next warrant. Following it replaced the obvious second Gloop capture with an unactionable workshop and a premature low-odds Barão projection. The contextual field-test shortcut now requires enough scrap for at least the cheaper equipped-item calibration. First-session equipped loot retains the ordinary arsenal link; the third-capture mastery grant still funds and activates the field-test handoff beside the newly available warrant. UI coverage protects both unfunded and funded states, and the first-claim capture confirms contract repetition remains the dominant action.
72. The second-capture reward introduced mastery at `2/3` but still described the same required third repeat again in a separate next-warrant `2/3` line and route projection. Because that next capture simultaneously grants mastery 1/3 and unlocks the planet's second warrant, this state now renders one combined promise naming both outcomes and the target. The duplicate warrant line is suppressed only for that convergence; other mastery and warrant thresholds retain their independent evidence. A dedicated second-reward/board pair verifies the repeat action remains dominant and the resulting `2/3` board progress stays visible.
73. The third-capture combined reward was audited for action overload after the first-session changes. Its routes are not duplicates: the workshop path spends the newly granted mastery scrap before hunting, the warrant path preserves immediate briefing access, and instant recycling appears only for a safe disposable item. The composition therefore remains unchanged. Mobile coverage now constructs both the initial reward and the densest mastery/warrant threshold and asserts every primary destination retains a 40-unit touch target fully inside the vertical viewport.
74. The first clean briefing correctly marked the fast route as recommended, but all three high-odds actions said `ESCOLHER · SEGURO`. That risk tier was true yet failed to name the economically different contract being confirmed, and the screen did not explain why 98% could be recommended over 99%. The briefing now states that recommendation balances chance, payment, and experience; every CTA names its exact route while risk tags and chance metrics remain unchanged. UI and mobile coverage verify three distinct actions, their recommendation basis, and horizontal fit; a dedicated first-briefing capture confirms all routes and the cancel action remain usable within 450×800.
75. The recommended opening hunt lasts roughly three seconds and reaches its incident near the midpoint, so it demonstrates idle progress without imposing a long first wait. The incident previously said only that the hunt was paused, which could look like a stalled timer immediately after the player's first contract choice. It now reports the exact paused percentage and remaining whole seconds; choice-specific added time stays in each consequence. UI coverage verifies the timing explanation, while a dedicated first-incident capture preserves all three payment previews and 48-unit choices inside 450×800.
76. Victory already retained the decisive attack, aggregate damage, active build effects, and approved payment, but automatically replaced that evidence after 1.35 seconds. The readable pause is now 2.8 seconds, while a new 48-unit `ABRIR RECOMPENSA` action preserves immediate pacing for players who do not need the receipt. Phase changes still stop the one-shot timer, preventing double transitions. UI coverage protects both the explicit action and minimum pause; the refreshed 450×800 victory capture keeps every evidence line and the skip action visible.
77. Combat-speed behavior was already coherent: 2× persists for the session, changing the interval without restarting the active timer, and leaving combat stops turns without resetting the preference. The control now has a stable semantic node name and UI regression explicitly toggles 1×→2×, verifies the 0.34-second interval, completes the fight, and proves the combat timer is stopped while 2× remains selected. No pacing formula changed.
78. Abandonment correctly reset embalo from both `HUNT` and `HUNT_EVENT`, but the pre-action button always said only `ABANDONAR CONTRATO`; the exact loss appeared after returning to the board. Both phases now share a state-derived label that names `PERDER EMBALO ×N` when a sequence is active and stays compact when none exists. UI coverage verifies identical consequences before and during an incident, while flow coverage checks the resulting contract-context receipt names the exact three-capture loss.
79. Paid incident choices correctly rejected insufficient funds but labeled every disabled state only `SEM CRÉDITOS`, leaving the player to recover the cost from the choice title and compare it with the header wallet. Choice actions now have stable IDs and report the exact shortfall (`FALTAM N CR`); free alternatives remain enabled and unchanged. UI coverage reproduces a zero-credit D-7 incident, verifies the paid eight-credit choice is disabled, and proves the free detour stays actionable. Costs and outcome formulas are unchanged.
80. Incident resolution already mutated the contract, charged credits, changed phase, and saved before emitting UI state, but persistence coverage stopped at restoring the unchosen paused event. A complete forced-close regression now restores an incident, applies its six-credit choice, reloads the resumed hunt with the canonical choice ID and exact wallet, rejects a second resolution/charge, abandons, and reloads again. The final state remains a clean board with the charged balance and no resurrected contract. This proves the transaction boundary without changing schema or runtime logic.
81. Reward claims likewise saved only after applying their complete transaction and clearing pending loot, but the persistence suite previously proved interruption before claiming rather than immediately after it. A new third-capture corporate fixture now applies credits, capture progression, equipped loot plus replaced gear, two contract scrap, and six mastery scrap; reload preserves every total, stays on the board, and cannot resurrect the pending item. A second `claim_reward` call is rejected outside `REWARD` and leaves wallet and inventory byte-for-byte unchanged. Runtime behavior and schema remain unchanged.
82. Immediate repeat combines that claim transaction with a transition directly into the next briefing. A new forced-close fixture claims a corporate reward into repeat, verifies the modified completed approach and pending loot are removed before save, reloads into `BRIEFING` with the canonical Gloop target and all three approaches, and successfully selects a different quiet route. The previous contract cannot leak its approach or skip player choice. Runtime behavior and schema remain unchanged.
83. Extending the workshop-route reload assertion exposed a false recovery on every clean post-claim save whose completed approach modified target health. Claiming cleared `current_bounty` but retained the victory summary's applied `target_max_health`; without that contract on reload, evidence canonicalization compared it against base target health and emitted `SAVE RECUPERADO`. Victory evidence is fully consumed once the reward is claimed and is not rendered on the board, so the claim transaction now clears combat rows and summary beside pending loot. Combat, victory, and reward still retain the full report beforehand. Persistence, flow, and UI suites confirm clean reload, applied funding/equipment/progression, session-only receipts, and no stale combat evidence.
84. Chapter completion has the complementary evidence lifetime: its canonical finale survives reload until `CONTINUAR CAÇANDO`, then the continuation transaction clears only `chapter_completion` and saves the board phase. Persistence coverage now restores a repaired finale, consumes it, reloads again, and proves the completion screen cannot return while the completed planet and active Congelária route remain. The clean post-finale load emits neither a stale chapter receipt nor a recovery notice. Runtime behavior and schema remain unchanged.
85. AFK application already saved immediately after updating wallet and `last_seen`, specifically to prevent duplicate patrol claims, but that guarantee was not exercised after chapter continuation changed the completed-planet multiplier. A coherent post-finale fixture now simulates a 3,700-second absence with one completed planet and ten wins, verifies the first load creates a positive patrol report, then loads again immediately. Credits and scrap remain exact and the second report is empty. Runtime formula and schema remain unchanged.
86. A new clean-save matrix now generates and reloads all eight persisted phases: board, briefing, hunt, incident, combat, victory, reward, and chapter completion. It exposed three writer/canonicalizer disagreements beyond the consumed post-claim summary: empty `field_test_context`, empty `kit_origin`, and an approach-modified boss stored in finale evidence. Combat now writes optional evidence only when present, while chapter completion stores the canonical boss because applied payment/XP already live in separate totals. The matrix is part of the primary checker and every current-schema phase now round-trips without a false `SAVE RECUPERADO` notice.
87. The clean matrix now also covers the positive optional-evidence path rather than only omission: a combat with a complete Dustball kit and a confirmed quiet-route field test reloads without recovery, retains canonical `kit_origin`, and preserves the tested approach/odds decision context. This protects the distinction between absent optional fields and meaningful evidence that must survive.
88. The matrix now iterates all thirty choices from the ten hunt incidents across five canonically unlocked planet chains. Every paid/free cost, duration addition, power/defense/health/economic multiplier, applied choice ID, resumed-hunt phase, and resulting wallet survives reload without self-repair. The first run also proved route sanitization correctly rejects a fixture placed on a locked later planet; the final fixture now completes each prerequisite chain rather than weakening that guard.
89. Contract round-trips now cross all three approaches with Gloop, a mid-campaign Micélia target, and the final Cassino boss under coherent planet/tier progression. All nine saves restore the exact approach without recovery; corporate contracts retain their two-scrap metadata while every route keeps `loot_power` equal to canonical target power. This guards the previously fixed separation between encounter risk/economics and equipment tier across early, middle, and endpoint content.
90. Equipment persistence now crosses all six canonical traits, three rarities, five planetary origins, and every legal reinforcement level (0–5) with calibration histories of 0, 1, and 5. Calibration has no rules-level ceiling, so those histories deliberately cover none, first investment, and a mature item without fabricating a cap. Across 1,620 combinations, both equipped and reserve payloads retain canonical trait/color/origin/workshop metadata, while protection flags and both loadout references survive without false recovery.
91. Career rewards now have a dedicated transactional persistence suite. Each of the eight milestone IDs is claimed and reloaded independently, proving exact wallet/lifetime totals and duplicate rejection; bulk claims then repeat the audit with coherent early, mid-campaign, and fully completed profiles. Eligible IDs, summed credits, and scrap persist together, all ready rewards are consumed, and a second individual or bulk attempt is side-effect free.
92. AFK settlement clamped negative elapsed time but previously replaced `last_seen_unix` with a lower device time. Rolling the clock backwards and then restoring it could therefore reframe the same interval as a fresh patrol. Both settlement and ordinary saves now keep a monotonic watermark. A dedicated suite covers zero, 299 seconds, the exact five-minute minimum, exact/elevated eight-hour cap, the pre-capture lock, immediate duplicate settlement, backward time, and a persisted AFK return coinciding with save recovery; wallet/lifetime totals apply once and the shared acknowledgement remains session-only.
93. Buttons relied on Godot's implicit focus behavior and had no project-specific focus visual; because the UI rebuilds on every phase transition and combat turn, controller focus could also disappear with the freed node. The shared action factory now guarantees focus participation and a high-contrast three-pixel ring. Rendering records the focused action name and restores its replacement after layout, falling back to the first enabled visible action with a generation guard against stale deferred callbacks. A headless UI audit traverses board, galaxy, career, arsenal, briefing, hunt, incident, combat redraw, victory, reward, and chapter completion.
94. Motion inspection against the product pillars found only one decorative transition: the 0.32-second loot-panel fade. Automatic combat is core behavior rather than decoration, and the 2.8-second victory interval exists for evidence readability with an explicit skip. Save schema v5 now adds a persistent `reduced_motion` preference in the arsenal; it removes the loot fade without speeding combat or shortening the victory receipt. Migration defaults existing players to full motion, while dedicated UI/persistence coverage verifies both render paths, toggle feedback, clean reload, and preserved pause controls.
95. A 125% typography expansion audit at the mobile viewport exercises the final-boss briefing, densest Cassino incident, threshold reward with deliberately verbose loot, completed career, and verbose arsenal. It exposed a reward-panel minimum width of 820 units: the long unwrapped loot name widened the entire content tree and pushed both fixed decisions outside the 720-unit logical viewport. Item names and shared actions now wrap by words. All five expanded layouts retain horizontally visible controls, fixed actions remain vertically reachable, and every enabled target stays at least 40 units high.
96. Desktop focus loss and application suspension previously relied on platform timer behavior, so automatic turns or the victory receipt could advance unseen. Lifecycle suspension now tracks focus and application reasons independently: the first reason freezes combat and captures victory time remaining; timers resume only after the final reason clears. Mobile suspension saves immediately. On resume, automatic combat continues from the same round, victory completes its unread interval, and idle hunts intentionally reconcile wall-clock expiry before starting combat. A timed headless regression covers unseen-turn prevention, repeated focus-out, overlapping pause/focus notifications, preserved victory evidence, and expired-hunt reconciliation.
97. `save_game()` previously returned nothing and silently ignored an unavailable local path, leaving mutated progress only in memory with no warning. It now returns confirmed success/failure, flushes before success, and maintains a dedicated phase-independent storage warning that cannot be displaced by gameplay receipts. `TENTAR NOVAMENTE` writes the complete current payload and rerenders; only a successful flush clears the warning. The regression starts with a nonexistent parent, verifies no phantom file, renders the warning and retry, keeps the victory advance action inside the mobile viewport, creates storage, retries, and reads back the exact 777-credit in-memory state.
98. Direct overwrite could still leave a truncated primary after process interruption. Saves now flush to `.tmp`, rotate/promote it, and refresh `.bak` from the newly committed primary. Recovery precedence is staging (a complete newer write whose promotion was interrupted), primary, then backup. Crucially, backup mirrors the latest transaction rather than the prior save: a regression claims a reward, verifies backup already contains the board phase, consumed loot, wallet, inventory, and capture, corrupts primary, restores visibly, rejects another claim, and reloads cleanly. This avoids turning crash resilience into a reward-duplication rollback.
99. A corrupt primary with neither valid staging nor backup still fell through to a default in-memory player; the next ordinary save could then overwrite the only forensic artifact. Startup now resets transient runtime state, detects an existing but wholly unreadable save family, blocks gameplay and every ordinary write, and presents only `INICIAR NOVO SAVE`. That explicit action copies each artifact to a collision-safe `.corrupt` suffix before removing originals and creating a canonical primary/latest backup pair. The regression verifies byte-for-byte preservation, blocked bounty access, valid v5 replacement, and board access only after confirmed persistence.
100. Timestamped `.corrupt` families preserved evidence safely but could grow without bound after repeated storage faults. Successful fresh-start recovery now retains the two newest generations independently for primary, staging, and backup, bounding diagnostic storage to six artifacts. Pruning runs only after the canonical replacement save reports success. The corruption regression performs three complete failure/recovery cycles and proves the oldest primary artifact is removed while the exact two newest contents remain.
101. The primary checker had grown past fifty seconds, with the 30 incident choices, nine representative contracts, and 1,620 equipment combinations sharing one file with the eight essential phase round-trips. Those exhaustive matrices are now isolated in `test_persistence_matrix.gd`; the default checker remains full, while `check_fast.ps1` omits only that explicitly named suite. Measured locally, the matrix takes about 19 seconds and the fast profile runs 27 suites plus boot in 33.16 seconds. Per-test stopwatches now make future latency decisions visible, and the fast path still executes clean board/briefing/hunt/incident/combat/victory/reward/finale persistence.
102. The checker created a unique `.godot/test-logs` directory on every run and had accumulated 178 directories during autonomous validation. Successful runs now retain the three newest directories by default; failures throw before pruning, ensuring the failed run remains among the newest evidence. Cleanup resolves the project/log roots and revalidates every candidate as a descendant before recursive removal. Retention is bounded but configurable from 1–20 with `-RetainedLogRuns`.
103. Windows packaging had installed 4.7.1 templates but no project preset, version metadata, icon, or executable smoke test. A tracked `Windows Desktop` release preset, original SVG icon, version/description metadata, and `check_windows_export.ps1` now produce and headless-boot `CrookedGalaxy.exe` with persistence disabled via an internal `--smoke-test` argument, so validation cannot touch the player's real save. The first export exposed that `all_resources` packed review screenshots, tests, tools, notes, and references (2.75 MB PCK). Explicit exclusions plus `builds/.gdignore` reduced it to 0.21 MB containing only runtime scene/scripts/icon; the 104.06 MB release executable and separate PCK boot successfully.
104. Android-first interaction checks existed without a reproducible Android artifact. The tracked ARM64 preset now imports ETC2/ASTC textures and produces a 27.37 MB debug-signed APK with package ID `com.crookedgalaxy.game`. The local checker verifies export success, while `publish_android_latest.ps1` rebuilds and atomically replaces `CrookedGalaxy.apk` on the public download-only repository's `latest` release so testers can reuse one URL without exposing the source repository. A dedicated ignored local test key makes successive direct-download builds update-compatible; store distribution remains gated on a separate private release key.
105. The mechanical reference remained visible in the loop but the card-only presentation lacked the illustrated sense of place that makes systems emotionally legible. Four proprietary study images now serve as explicitly watermarked, local interactive-editor composition placeholders for contract, world/career, workshop, and combat contexts. They are raw-loaded only by editor-feature binaries with a non-headless display; headless tests and exported templates keep the original procedural backgrounds. A tracked register names every source and original replacement target. The default checker rejects tracked reference payloads, missing `.gdignore`, incomplete preset exclusions, or direct runtime paths outside the approved loader; both platform export checks mount their produced content packs in an isolated inspector and fail if a `References` directory or known placeholder survives packaging.
106. The unlocked bounty board already preserved earlier targets, but presenting all four cards with identical hierarchy made the campaign decision hard to scan and pushed the actual choice toward a long list. A deterministic board projection now leads with the highest revealed campaign target and adds at most two prior contracts ranked by distance to their next mastery threshold. Labels and actions distinguish `MANDADO PRINCIPAL`/`MANDADO FINAL` from `ROTA DE PERÍCIA`/`CONTRATO RECORRENTE`; completed chapters become a repeat-only rotation. The underlying availability API, sequential three-capture gates, archive, target data, and combat economics remain unchanged, and tests prove the first session still has one target, future tiers never appear, the boss stays first, and the mobile list remains scroll-safe.
107. The four composition studies have now completed their purpose. Independently generated portrait paintings replace every active reference-backed context with an original bounty office, frontier spaceport, arsenal workshop, and landing-pad arena. No reference image was supplied during generation, and the production prompts excluded text, logos, recognizable characters, and direct imitation. `EnvironmentBackdrop` uses imported resources in every build and tunes a per-context scrim for the existing UI; the proprietary adapter remains empty and editor-only. Visual captures confirm readable mobile hierarchy in board, galaxy, arsenal, and combat, while a new runtime test proves all four textures resolve and unknown contexts fail closed.
108. The hunter portrait remained visually identical while equipment power, rarity, origin, workshop investment, and planetary kits changed around it. The procedural portrait now accepts a defensive copy of the player loadout: weapon rarity colors the visor, armor rarity colors the shoulders, planetary origin changes the rim, a matching kit adds a gold lock line, and up to five workshop pips expose investment. Combat also names exact weapon/armor power beneath the portrait. This adds build identity without new bitmap variants, save fields, or player decisions; isolated palette and mobile/UI tests protect fallback origins, rarity colors, and the compact combat hierarchy.
109. The generated source paintings were 944×1680 despite the game's 720×1280 logical portrait viewport. Every environment import now has a 1280-pixel longest-edge limit, and the runtime test rejects an oversized imported texture. This preserves the full display resolution while reducing decoded mobile residency from roughly 6.0 MiB to 3.5 MiB per RGB/RGBA context before platform compression. The original-art milestone also advances application/Desktop/Android version metadata to 0.2.0 and Android version code 2, allowing the shared-signature `latest` APK to install as a real update over version code 1; release notes now include the resolved project version.
110. The Android checker previously trusted preset metadata after export and reported only file size. It now asks the installed SDK's newest `aapt2` to read the produced manifest, compares package ID, version name, and version code against the preset, and rejects divergence. A 40 MB direct-test APK ceiling also protects the lightweight distribution path as original art grows. Version 0.2.0 passes as `com.crookedgalaxy.game`, code 2, at 31.24 MB, while mounted-pack inspection still independently rejects proprietary reference content.
111. Loot presentation still used the same gear glyph for every weapon and armor despite carrying rarity, planetary family, and workshop history. A code-drawn `EquipmentIcon` now gives weapons and armor different silhouettes, frames them by rarity, fills them with their origin-planet color, and adds up to five investment pips. The reward reveal uses a larger icon and inventory cards use a compact one; no new bitmap, save field, or input is required. Isolated icon, primary UI, and mobile tests cover both slots, palette fallbacks, reward presence, and retained touch layout.
112. Because the equipment-icon batch followed the already public 0.2.0/code-2 environment release, final metadata advances monotonically again to 0.2.1/code 3. This ensures even a tester who installed the intermediate build can apply the final artifact from the unchanged `latest` URL as an update rather than a same-version reinstall.
113. `EnvironmentBackdrop` initially preloaded all four 1280-pixel paintings, paying their decoded-memory cost at startup even though the shell renders one context at a time. It now stores export-visible resource paths and loads only when the context changes with the global resource cache bypassed; replacing or leaving a known context drops the previous texture reference, while repeat renders reuse the current one. The old `reference_placeholder_context` name is now the production-neutral `environment_context`. Runtime coverage verifies board/workshop/world/combat routing, requested-context identity, and that unknown contexts clear both texture and state. The all-resources preset plus mounted-pack inspection still proves every production painting ships and no proprietary study file does.
114. The lazy-environment memory batch advances the final public artifact to 0.2.2/code 4. As with the preceding visual releases, the package ID and local test signature remain stable, so the fixed `latest` URL continues to update every intermediate installation monotonically.
115. Every odds request ran the same 1,024 deterministic combat trials again, including repeated board renders and multiple workshop comparisons of identical builds. `CoreRules` now caches up to 512 results using only the eight combat inputs that affect the simulation: hunter health/power/armor/opening damage/reduction and target power/defense/health. Wallet, inventory order, notices, and other irrelevant state reuse the same value; any actual build or target change creates a new key, so invalidation is structural rather than event-dependent. Core tests prove cache reuse without changing calibrated percentages, and the hard cap prevents unbounded long-session growth. The primary UI suite fell from roughly 6.5 to 3.3 seconds, arsenal from 3.7 to 1.0, and the complete fast profile from 36.75 to 27.41 seconds on the same machine.
116. Four shared environment paintings risked making five planets feel like one reskinned menu. The active planet now applies a restrained 14% tint to the current production painting—amber Dustball, cyan Congelária, lime Micélia, orange Ômega, magenta Quasar—without reloading the texture or changing UI colors. Unknown/legacy planets stay neutral. This keeps the one-context memory model and mobile contrast while making travel visually present across board, workshop, map/career, and combat; runtime tests protect same-texture tint changes and neutral fallback.
117. The Android profile previously lived only as mutable editor/export settings. The mobile runtime audit now locks the 720×1280 portrait canvas, portrait orientation, canvas-item stretching, GL Compatibility mobile renderer, and ETC2/ASTC imports. The APK checker independently rejects architecture drift away from ARM64-only, loss of immersive mode, and version disagreement between the project, Android preset, and Windows metadata before publishing. This turns the Android-first intent into an executable release constraint instead of a convention.
118. Android system Back had no explicit route, despite every screen already having deliberate safe actions. It now returns arsenal/map/career hubs to the bounty board, cancels only an uncommitted briefing, and saves/exits from the root board. Once a route is committed, Back is consumed during hunt, incident, combat, victory, reward, and finale so a hardware/gesture slip cannot abandon a streak or make an irreversible reward decision. Mobile integration coverage protects the root, hub, briefing, and active-contract cases. The README also now reflects the completed transition from local composition studies to original runtime environments.
119. The fixed download URL was convenient but gave testers no independent artifact identity, and a dirty worktree could make the release note's commit ambiguous. Publishing now refuses tracked uncommitted changes, emits a stable `CrookedGalaxy.apk.sha256` companion asset, records the lowercase SHA-256 in release notes, and reads the release back through GitHub after upload. It rejects a missing asset, remote size mismatch, or digest mismatch when the host supplies a digest. The permanent APK URL remains unchanged.
120. Save-write failure handling correctly told the player to keep the game open, but desktop close and the newly explicit Android root Back ignored the boolean failure and quit anyway. Both exit paths now share a save gate: success exits, failure rerenders the persistent warning and retry action while retaining the live session. The failure regression exercises the denied exit against unavailable storage and proves it becomes allowable only after the same exact state is successfully persisted.
121. A tester downloading repeatedly from the intentionally stable `latest` URL could not tell which APK was actually running without Android system settings. The synchronized project version now appears as a restrained `v…` marker beside the active planet in the persistent header. It costs no additional vertical space, remains visible in every phase and hub, and mobile coverage binds the rendered value directly to `application/config/version` so the UI cannot drift from release metadata.
122. The 100 ms hunt refresh timer was autostarted for the entire application lifetime, waking ten times per second on boards, menus, incidents, combat, rewards, and finales only to return immediately. It is now owned explicitly by the shell and runs solely during a focused `HUNT` phase; every other phase and either lifecycle suspension stops it. Resume first reconciles elapsed wall-clock time, then restarts the timer only if the hunt still exists. Mobile and lifecycle tests protect board/incident/combat sleep, hunt wake, and background suppression, reducing idle CPU and battery churn without changing countdown cadence.
123. The consolidated post-change validation runs 30 fast suites plus boot in 27.51 seconds, then passes the exhaustive incident/contract/equipment persistence matrix separately. The 5,000-fight balance sample remains aligned with displayed estimates across all twenty targets, approaches, traits, kits, and representative incidents. Twenty full careers per strategy still report 0% campaign stalls: recommended play makes no sub-55% attempt, while the deliberately opportunistic corporate strategy earns 68 median scrap and performs 44 workshop actions; its isolated risky Ômega attempt does not propagate into a sub-55% Cassino attempt. The art, timer, Back, save-exit, and release changes therefore leave the established campaign economy intact.
124. Package ID and increasing version code are insufficient for in-place APK updates if the ignored local keystore is accidentally replaced. The public SHA-256 fingerprint of the established direct-test certificate is now tracked separately from the secret key, and every APK is verified with the installed SDK signer before release. The same manifest audit locks API 24 minimum compatibility, a non-regressed target SDK, and the actual packaged native ABI to ARM64-only. A regenerated or wrong key therefore fails before it can break the permanent update channel for existing testers.
125. Android pause correctly attempted an immediate save, but a failed background write only changed state; returning to an idle board did not necessarily rebuild the UI, so its persistent warning could remain invisible. Resume now schedules a render whenever a save warning exists, after lifecycle timers reconcile. The storage-failure regression clears the prior banner, forces the pause write to fail, resumes, and verifies the retry action is immediately restored before any gameplay input.
126. An ARM64 APK installation and cold launch succeeded on the local Android 15 Pixel 9 emulator through its native-bridge path, with the expected package/version/API metadata and no fatal, ANR, script, or GLES link errors under host graphics. The first ready-state capture exposed large top/bottom bars on its 1080×2424 display: the 720×1280 canvas was being preserved rather than treated as a minimum. Stretch aspect is now explicitly `expand`, allowing backgrounds and safe-area layout to consume modern tall portrait displays while leaving existing 9:16 captures unchanged. Both runtime and export-profile checks reject a return to letterboxing.
127. Device input then exposed a distinction hidden by direct notification tests: Godot's default Android `Quit On Go Back` behavior terminates the activity after sending the back request, overriding the shell's hub route. The project now explicitly disables that engine default and delegates every Back press to the already tested safe router. Runtime and export-profile checks lock the setting; the implementation still saves/exits deliberately from the root board while secondary hubs remain in-process.
128. The consolidated Android-first batch advances synchronized project/Desktop/Android metadata to 0.2.3 and version code 5. This is strictly newer than the public 0.2.2/code-4 artifact and retains the verified certificate, so the final emulator and permanent-link publication exercise the same in-place update path testers will use.
129. The Android cold-start capture still showed Godot's default splash long enough to read under emulated ARM translation, breaking product identity before the original interface appeared. A tracked 720×1280 SVG source and reproducible Godot rasterizer now produce an original Crooked Galaxy PNG using the established orbit/planet mark, cyan/gold/navy palette, and Portuguese bounty-hunter tone. Project and Android profile checks bind the PNG path and portrait dimensions; the background color matches the artwork so taller devices extend cleanly rather than flashing or letterboxing.
130. Export inspection previously proved only the absence of proprietary study material. Both platform checkers now also mount their produced content pack and require the branded splash, original icon, and all four production environment paintings. This makes the positive art boundary executable: a future exclusion or broken resource dependency fails packaging instead of shipping a mechanically valid but visually regressed build.
131. The branded-startup batch advances synchronized metadata to 0.2.4 and Android version code 6. It retains the same verified direct-test certificate and package ID, allowing the permanent link to update both the public 0.2.3 build and earlier installs in place.
132. Revalidating the startup batch exposed a stale mobile assertion that demanded 120 units for the arsenal's deliberately compact nested inventory while its fixed accessibility footer remained visible. The test now protects one complete 48-unit touch row and waits for both layout passes. More importantly, the 125% typography audit caught the debug reset action extending 12 units below the viewport; the arsenal alone now uses 14-unit section spacing, restored to the 16-unit default on every render, keeping all fixed actions visible and expanding the inventory without removing information or controls.
133. The final 0.2.4/code-6 APK was installed cleanly on the Android 15 Pixel 9 emulator at 1080×2424. Timed cold-start captures distinguish the native Android icon window, the original Crooked Galaxy boot artwork, and the ready board; the artwork's top edge now returns to the same dark extension color as its bottom edge so the 9:16 source blends into taller displays. The ready build exposes `v0.2.4`, uses the full 20:9 viewport, opens the compact arsenal with every fixed footer action visible, routes hardware Back to the board, and emits no fatal, ANR, activity, or script errors.
134. Level had only an automatic `+2` base-power step, so the agreed Strength, Vitality, Dexterity, Intelligence, and Cunning vocabulary now forms save schema v6. Every attribute starts at a neutral 10 and each level grants two explicit points; migrating established hunters awards the exact unspent allowance for completed levels without rewriting their existing base power. Universal pre-class effects are deliberately restrained: two Strength add one power, each Vitality adds four health, three Dexterity prevent one incoming damage, two Intelligence add one opening damage, and each Cunning shifts the attack roll by 0.5 percentage points up to 15%. A portrait hub advertises pending points on the board, scrolls all five cards, drafts taps without mutating the save, and commits only after confirmation. Invalid IDs, fractional spends, and overspending fail atomically. Dedicated rules/UI coverage plus mobile, focus, 125% text, migration, phase round-trip, and balance guards keep this class-ready foundation compatible with the current game.
135. The class-ready progression batch advances synchronized project/Desktop/Android metadata to 0.3.0 and Android version code 7. The package ID and established direct-test certificate remain unchanged, so the permanent APK channel can migrate existing 0.2.x saves through schema v6 and install as an in-place update.
136. The signed 0.3.0/code-7 APK installed with `-r` over the emulator's 0.2.4 package and opened its existing level-one save as schema v6 with all five attributes at 10, no unearned points, and the visible one-time preservation notice. A controlled two-point fixture then opened the real 1080×2424 attribute hub, drafted one Strength point without touching disk, confirmed it, and read back schema 6 with Strength 11 and exactly one point remaining. The build marker, full-height layout, disabled/active action states, and original world backdrop all render correctly; Android logs contain no fatal exception, ANR, activity startup, or script error.
137. Re-auditing the class direction against the vision found no reference-defined roster worth copying. The product pillars instead require bounty-hunter identity, humorous sci-fi, build decisions, automatic combat, and mobile readability. The first independent roster therefore uses three profession-flavored archetypes rather than generic fantasy names: Quebra-Mandados (Strength), Pistoleiro Orbital (Dexterity), and Hacker de Contratos (Intelligence). Vitality and Cunning stay universal so every class shares survivability and improvisation choices instead of dividing the existing equipment economy into isolated games.
138. Class rules are centralized outside presentation and add exactly one Power for every two invested points in the selected primary attribute. That small common formula makes the three specializations directly comparable while preserving Strength's universal power, Dexterity's mitigation, Intelligence's opening damage, every other attribute effect, equipment, kits, and simulation-backed odds. New and migrated hunters remain explicitly unassigned; the board advertises the choice without silently choosing a build. Early-access reclassification is free, explicit, board-only, and confirmed from a portrait scroller so later balancing cannot trap a save.
139. Save schema v7 adds only the validated class ID. Unknown or damaged IDs repair to the safe unassigned state; migrations never infer a class from previously spent attributes. Dedicated rules/state/UI tests cover the complete roster, equal specialization scaling, invalid IDs, contract-phase protection, reversible draft and confirmation. Mobile, focus, 125% typography, migration, and visual-capture audits now include the class selector.
140. The initial-class batch advances synchronized project/Desktop/Android metadata to 0.3.1 and Android version code 8. The complete 32-suite profile including exhaustive persistence matrices passes in 51.61 seconds. The signed 31.67 MB APK remains API 24+, ARM64-only, uses the established direct-test certificate, contains all required original runtime art, and contains no proprietary study references.
141. Android validation installed 0.3.1/code-8 in place over the emulator's 0.3.0/code-7 package. Its existing schema-v6 profile retained Strength 11 and one unspent point, advertised the unassigned class on the board, and rendered both profile and three-class selector across the full 1080×2424 viewport. Confirming Pistoleiro Orbital rewrote the save as schema v7 with `class_id: orbit_gunslinger` without changing any existing attribute or progression field. Runtime logs contain no script error, fatal exception, or ANR.
142. A read-only optimization audit measured inventory as the first confirmed scalability limit: rendering 20, 50, 100, and 200 items built roughly 321, 651, 1,201, and 2,301 nodes and took 33, 70, 154, and 357 milliseconds respectively, while the 200-item player JSON remained only 23 KB. Persistence was therefore not the bottleneck. The workshop now sorts the complete filtered view but instantiates only a twelve-item page, clamps stale pages after filtering or recycling, resets paging when filter/order changes, and exposes 48-unit previous/next actions inside the existing scroller. At 200 retained items this reduces the active tree to 237 nodes and the same-machine warm render to about 61 milliseconds, an 83% latency reduction without schema or economy changes.
143. The same audit found class specialization was not combat-equivalent despite its common power formula. With two invested primary points against Barão Boom, Quebra-Mandados reached 75% while Pistoleiro and Hacker reached 42% and 45%; at six points Pistoleiro's mitigation caught or exceeded Strength while Hacker remained behind. Candidate simulations showed two extra opening damage per invested Intelligence moves Hacker to 68% in the early case and 78% in the representative midgame case, remaining below or equal to the corresponding 75–84% peers. Hacker now owns that thematic first-turn specialization in addition to common class power; deterministic regression protects the intended early ordering.
144. The optimization batch advances synchronized project/Desktop/Android metadata to 0.3.2 and Android version code 9. Save schema remains v7 because inventory pagination is transient and the Hacker effect derives entirely from already persisted class and attributes. The complete 32-suite profile including exhaustive persistence matrices passes in 51.34 seconds; the 31.67 MB signed export remains API 24+, ARM64-only, includes required original art, and excludes proprietary study references.
145. Android validation installed 0.3.2/code-9 in place over 0.3.1/code-8 and cold-started in roughly 2.04 seconds. The existing schema-v7 profile retained Pistoleiro Orbital, Strength 11, and its unspent point byte-for-byte; no migration or pagination field entered the save. Runtime logs contain no script error, fatal exception, or ANR.
146. The initial Quebra-Mandados, Pistoleiro Orbital, and Hacker de Contratos roster is now explicitly registered as prototype content rather than final lore. Every definition carries replaceable-content metadata and mechanical effect data; combat resolves the declared primary-stat divisor and opening multiplier without branching on a specific class ID. Player-facing specialization copy is derived from those same values, preventing rules and descriptions from drifting when the roster changes. The mobile selector identifies the archetypes as provisional while retaining free reclassification, explicit confirmation, complete 450×800 visibility, and the existing balance envelope. Project/Desktop/Android metadata advances to 0.3.3/code-10; save schema remains v7 because persisted class IDs and player state are unchanged.
147. Prototype labels alone still asked players to translate formulas into their own build before choosing. A side-effect-free specialization preview now evaluates every definition against the hunter's committed attributes and returns the same Power and opening values consumed by combat. Each selector card renders that exact current bonus, including an honest inactive state at neutral attributes, without selecting a class or mutating the save. A representative 450×800 capture keeps all three cards, values, and confirmation reachable; dedicated UI/rules coverage protects the Hacker's combined `+1 Power · +4 opening` preview at Intelligence 12. Project/Desktop/Android metadata advances to 0.3.4/code-11; save schema remains v7.
148. The campaign balance harness had become weaker than the shipped character model: it gained levels but left every attribute point unspent and every class unassigned. A shared development-only build policy now runs all three prototype classes through comparable 50/25/25 primary-stat, Vitality, and Cunning allocations after each reward; an explicit unassigned control preserves old-baseline comparison. Campaign reports include final class attributes, level, credits, scrap, route mix, and the share of approach choices saturated at 99%. Six-seed recommended samples finish near level 29 with approximately 220–234 Power, 579 health, and 36.8–36.9k unused credits; all three builds complete without stalling, while most arrival choices saturate. Corporate-when-viable samples finish near level 27 with 43.2–44.6k credits, about 98 contract scrap, greater late-route variety, and no stalls. These results confirm that the next gameplay work should address credit utility and odds/route compression before increasing content or class power. A dedicated checker suite protects class selection, exact point conservation, the 50/25/25 policies, and the legacy control.
149. The first response to the confirmed currency surplus is the original `MERCADO TORTO`, a three-offer equipment market reached from every planet board. Its deterministic stock always contains weapon and armor choices, persists bought offer IDs and paid renewals in bounded save schema v8 fields, and equips only complete-build upgrades automatically. Stock trails the active warrant tier, so credits provide catch-up and collection utility without buying the next enemy's reward in advance. Six deterministic careers per class spent a median 16,708 credits on 13 purchases and two renewals, finishing near 20.5k rather than the prior 36.8–36.9k surplus and with no stalls. The samples also raised 99% option saturation to 89–92%, confirming the market solves part of credit utility but cannot substitute for the next planned odds/route-compression pass. Version 0.4.0/code-12 adds transaction, migration, sanitizer, focus, typography, Android touch/Back, visual-capture, and economy-simulation coverage.
150. Route compression came from real equipment growth crossing combat turn-count thresholds, not from an estimator defect. Artificial probability caps and player-relative enemy scaling were rejected because either would make displayed odds diverge from combat or make upgrades secretly self-defeating. The accepted calibration keeps the quiet route unchanged, raises fast-route payment to +45% with 1.18× attack, 1.08× defense, and 1.28× base resistance, and raises corporate payment to +85% with 1.22× attack, 1.14× defense, and 1.32× base resistance while retaining its deliberately balanced two scrap. Only resistance receives a small planet step (+0.03 fast / +0.02 corporate), producing longer aggressive fights without a late damage cliff; loot power remains canonical. Six deterministic careers for every class and both strategies reduce 99% option saturation from 89–92% to 64–66%, retain 0% stalls, and keep every recommended attempt at or above 55%. Recommended play offers at least two viable routes in 76–83% of decision snapshots and a visible 15-point risk spread in 24–30%; the remaining compression is concentrated in Congelária/Micélia and is now measured explicitly rather than masked by a single saturation count. Modern fourth/fifth-planet guard profiles require strictly ordered risk bands and visible gaps. Version 0.4.1/code-13 carries this calibration without a save migration.
151. The remaining plateau was a content-curve discontinuity: Congelária began at 28/11/165 immediately after Dustball's 28/11/160 boss, and Micélia began at 44/19/275 immediately after Congelária's 43/18/260 boss, despite the player receiving levels, market purchases, workshop upgrades, and boss loot at both boundaries. Congelária and Micélia now advance through explicit combat profiles while retaining their former values as `loot_power`, separating encounter difficulty from equipment growth and preventing stronger enemies from inflating the following reward. Per-planet simulator summaries show 99% option saturation falling from 100% to 35–39% in Congelária and from 94% to 33% in Micélia across the three classes and two route policies; recommended builds produce visible 15-point route separation in 58–83% of those chapter decisions. All six class/policy samples complete with 0% stalls and no attempted fight below 55%. Dedicated arrival and boss guards lock the safe recovery route, ordered aggressive risks, and combat/loot separation. Version 0.4.2/code-14 carries the curve without a save migration.
152. The reference transport principle fits the bounty-hunter fantasy but its specific mounts, formulas, visuals, names, and rental structure remain study-only. Crooked Galaxy's independent implementation is the permanent `HANGAR DUVIDOSO`: four original comedic spacecraft cost 500/2,200/6,500/15,000 credits, unlock after zero through three completed chapters, and remove 10/20/30/40% only from the selected approach's base hunt interval. Incident delays remain integral; combat odds, rewards, loot and AFK output do not change. Ownership and active selection are atomic schema-v9 fields, guarded against duplicates, unknown IDs, premature unlocks, double charging, contract-phase mutation, and interrupted saves. The portrait scroller and board access pass Android touch/Back tests and a real 450×800 capture. Six deterministic careers across every class and both route policies retain 0% stalls and no attempted fight below 55%; recommended builds buy three transports for 9,200 credits, retain 11.1–12.8k credits, and save 214–248 simulated hunt seconds, while the higher-income corporate sample can reach the 24,200-credit full collection and saves up to 367 seconds. The expanded 36-suite profile plus project boot passes in 55.45 seconds. Version 0.5.0/code-15 introduces the system and migrates existing hunters without granting or removing currency.
153. The functional hangar initially used abstract glyphs, which proved timing and economy but left the transport fantasy isolated inside one menu. Four independently designed `TransportIcon` silhouettes now draw through normalized Godot geometry: an uneven windowed junk shuttle, roof-lit taxi, magnetic wedge interceptor, and finned executive yacht. They share no reference art, add no bitmap residency, remain readable at the 46–58 unit mobile sizes, and follow the equipped vehicle into the map route strip, contract briefing, and live hunt. Real 450×800 hangar/map/briefing/hunt captures preserve every action and the complete first route card; mobile integration verifies all four hangar icons plus the active identity in map, briefing, and hunt. The complete 36-suite profile plus boot passes in 55.92 seconds. Version 0.5.1/code-16 carries the visual integration without changing schema, prices, timing, combat, or economy.
154. The market-versus-transport question existed only in simulation telemetry, so a player could spend credits in either shop without seeing the opportunity cost. A side-effect-free spending projection now gives both destinations a neutral comparison surface: the market names the nearest unowned transport with its permanent speed bonus, price, and unlock gate; the hangar counts only current offers that improve the complete combat build and reports their cheapest price. Forty-eight-unit cross-navigation makes the alternative one touch away without reserving currency, prescribing a purchase, or changing stock, transport, combat, or save data. Real 450×800 captures keep all summary panels and the first actionable cards visible. The complete 36-suite profile plus boot passes in 55.63 seconds. A fresh six-career, three-class active/off matrix remains stall-free in every combination: both sinks together leave 11.1–12.8k credits after 12 market buys and three transports; market-only leaves 18.2–20.9k after 13 buys; transport-only buys the full 24.2k collection and leaves 7.4–11.0k; neither leaves 31.6–35.2k. Version 0.5.2/code-17 carries the guidance without a schema migration.
155. Internal Android composition testing now permits the four already registered reference backgrounds without weakening the public boundary. `Android Internal References` copies them into a Git-ignored staging directory, embeds them as raw PNG bytes behind the `reference_placeholders` feature, and keeps the visible replacement watermark. Its pack inspector requires all four files and decodes each image before accepting the 47.12 MB APK. A real interactive capture confirms the registered contract, world, workshop, and combat images render behind the existing mobile hierarchy. Normal Windows/Android presets still exclude both raw and staged reference content, the public `latest` APK remains original-art-only at 31.70 MB, and no proprietary payload enters Git. The complete 36-suite profile plus boot passes in 55.36 seconds.
156. The first internal adapter retained every visited 2000×1400 reference texture and left the production background resident underneath. Each decoded RGBA placeholder costs approximately 10.68 MB, making a four-context session retain roughly 42.72 MB before counting production art. The adapter now reuses only an unchanged current context, drops the prior texture whenever context changes or placeholders are unavailable, and makes the shell unload its production environment while the internal layer is visible. Direct decode/release coverage guards the single-residency contract. Rebuilt internal and public packs still pass their opposite content boundaries at 47.12 MB and 31.70 MB respectively; the complete 36-suite profile plus boot passes in 54.39 seconds.
157. The board's six secondary destinations occupied a narrow full-height column beside the title, consuming roughly the same vertical space as the contract decision and forcing the actual bounty lower on a 450×800 screen. They now form an original 3×2 hunter-hub grid below a compact title/XP row. Short labels retain exact contextual counts and continuation states, unassigned class is a coral pending action, and every destination keeps its 48-unit logical touch target plus focus ring. First-session, completed-chapter, and paid-receipt captures show the full leading contract materially earlier while preserving notices, rank progress, and equipment anchors. Mobile integration binds the grid to exactly six destinations and three columns; 125% typography remains horizontally and vertically safe. The complete suite passes in 53.69 seconds; the internal Android package passes with its four documented placeholders at 47.12 MB, while the public package passes the clean-pack boundary at 31.70 MB (ARM64, API 24+, stable test signature). Version 0.5.3/code-18 carries the layout without changing state or economy.
158. Briefing data existed but required vertical comparison across tall cards; established states with kit, mastery, streak, transport, or field-test context pushed the third route action below the initial mobile fold. Each route now uses one stable decision grammar: name and dynamic recommendation, strategy plus explicit risk, concise fiction, included streak/scrap benefits, then time, current-build odds, credits, XP, and a 48-unit named action. The recommended card receives a restrained lime border and `MELHOR EQUILÍBRIO` label rather than hiding alternatives. Real first-session, established, and tested-build captures keep all three complete choices visible at 450×800 while retaining the fixed cancel action; dedicated integration asserts the final corporate action remains inside the initial scroller viewport, and 125% typography remains safe. The complete 36-suite profile plus boot passes in 53.59 seconds. Internal and public Android packs pass their opposite content boundaries at 47.12 MB and 31.70 MB respectively; the public package remains API 24+, ARM64, and signed with the stable test certificate. Version 0.5.4/code-19 changes presentation only; recommendation, combat, rewards, timing, economy, and save schema are untouched.
159. Reward screens contained complete evidence but flattened loot, equipment delta, contract receipt, streak, mastery, warrant progress, and build projection into one centered text sequence. They now expose a left-aligned loot identity header, an immediate `NOVO / EQUIPADO / RESULTADO` comparison row, a separate detailed equipment delta, and distinct receipt/progression surfaces. Ordinary wins retain repeat as the solid primary destination; threshold wins promote the newly opened warrant to the solid primary action while keeping funded workshop preparation outlined, and inferior loot retains a coral recycling alternative. First, established, mastery/unlock, corporate, and recycling captures preserve every bottom action at 450×800. Isolated coverage binds all three comparison metrics and both grouped evidence surfaces, then verifies that the unlock destination is materially solid rather than merely present. The complete 36-suite profile plus boot passes in 54.13 seconds. Internal and public packages pass their opposite content boundaries at 47.12 MB and 31.70 MB; the public APK remains API 24+, ARM64, and signed with the stable test certificate. Version 0.5.5/code-20 changes only presentation and action emphasis; transaction ordering, equipment evaluation, rewards, progression, economy, and save schema remain unchanged.
160. Live combat required players to infer advantage from two spatially separated bars, read raw HP without percentages, and translate a repeated prose log back into the event cards. The arena now derives a non-predictive relative-health strip from authoritative current/max HP, explicitly naming `PRESSÃO SUA`, `DO ALVO`, or `EQUILIBRADA`; each fighter exposes raw and percentage HP, and the hunter's active weapon/armor evidence is more legible. The latest event pair has stable player/enemy identities and larger damage/quality text, while a structured turn heading states both damage totals before retaining the narrative sentence. Initial, established, late-planet, and tested-route captures keep arena, report, and speed control visible at 450×800. Integration covers the 100/100 opening state, both percentage labels, both event identities, and the two-sided turn summary; focus, lifecycle pause/resume, 125% text, and reduced-motion profiles remain valid. The complete 36-suite profile plus boot passes in 53.76 seconds. Internal and public packages pass their opposite content boundaries at 47.12 MB and 31.70 MB; the public APK remains API 24+, ARM64, and signed with the stable test certificate. Version 0.5.6/code-21 changes presentation only; automatic timing, combat resolution, traits, rewards, persistence, and economy remain untouched.
161. The workshop's best-value simulation was previously visible only as a star on one of four upgrade buttons. A compact `MELHOR INVESTIMENTO` strip now names the affected slot and item, exact scrap cost, projected field-test odds or saturated build impact, and offers one 48-unit `APLICAR` action above the manual paths. Normal, revenge, post-upgrade, funded-receipt, filtered, and paginated captures preserve the scan order and a complete inventory touch row at 450×800; 125% text remains safe. Version 0.5.7/code-22 changes presentation only.
162. Reference-backed composition testing now covers interface identity as well as scenery. The internal class selector uses one registered ornamental portrait surface and three registered class emblems, with the selected emblem also following the hunter into the attribute profile. The loader remains the only runtime file naming raw references; editor/internal builds decode the assets on demand, headless and public builds fall back to the original UI, and the visible replacement watermark remains mandatory. The staged allowlist grows from four to exactly eight decoded PNGs while the public pack continues to reject every staged path. Version 0.5.8/code-23 introduces no class, attribute, combat, economy, or save change.
163. The second interface-reference batch separates career from the general galaxy surface with a registered long-form ledger, frames only the hunter summary and current mastery target, and adds a twelve-unit ornamental boundary between the board's secondary hub and active contract evidence. The treatment deliberately rejects character-heavy achievement art that conflicts with the space-bounty theme; existing original portraits, milestone symbols, and contract cards remain authoritative. All three additions disappear cleanly when references are unavailable, so the public layout and touch geometry are unchanged. The staged allowlist grows to exactly eleven decoded PNGs. Version 0.5.9/code-24 changes internal composition only.
164. Distribution policy is consolidated around the actual use case: one private-team Android test APK at the permanent `latest` URL. The obsolete second Android preset and checker are removed. `Android APK` now stages, requires, and decodes the exact eleven documented placeholders while continuing to exclude the raw `References/` tree; Windows remains reference-free. Version 0.6.0/code-25 makes this packaging change without altering gameplay or save data.
165. Market, hangar, and arsenal still read as one location because all three inherited the same workshop composition. The market now receives a registered dealer silhouette and the hangar a registered transport-stable environment, while the arsenal keeps the fort/workshop study. An initial dealer-forest background read as exploration rather than commerce in a real capture and was rejected before release; the clearer merchant layer replaces it. A stronger scrim preserves the existing mobile cards, actions, and contrast over both character-rich sources. Headless and Windows builds continue to fall back to the original workshop background; the Android allowlist grows to exactly thirteen decoded PNGs. Version 0.6.1/code-26 changes presentation only.
166. The galaxy map communicated five routes as text-heavy cards, so destinations remained visually interchangeable until their names were read. Each chapter now has an original vector emblem derived from its established theme: cratered desert, fractured ice, fungal spores, scrapyard machinery, and a tilted quasar ring. Current orbit adds a cyan satellite marker, while locked routes retain their silhouette under a compact padlock. The 58-unit badges reuse the existing heading height, preserve travel actions in the initial mobile viewport, require no bitmap residency, and are asserted for all five canonical planets. Version 0.6.2/code-27 changes presentation only.
167. The most-used bounty screen showed level and power but omitted the hunter whose build produced them. The same 72-unit header footprint now combines an equipment-aware procedural hunter portrait with level and power, reuses the already registered temporary portrait frame when available, and acts as a keyboard/controller-accessible 48-unit shortcut to class and attributes. Contract position, resource chips, hub grid, progression, and gameplay remain unchanged. Version 0.6.3/code-28 changes navigation and presentation only.
168. The character shortcut correctly led to attributes, but all five rows began with an interchangeable number. Original vector glyphs now identify Strength, Vitality, Dexterity, Intelligence, and Cunning through weight, heart, sight, circuit, and eye silhouettes. Each glyph shares the existing value column, so card height, scrolling, draft actions, formulas, and confirmation geometry remain unchanged. Version 0.6.4/code-29 changes presentation only.
169. The character destination still behaved as a points menu rather than the build sheet established by the reference. The studied character-slot silhouettes confirm a portrait-centered equipment hierarchy, while the implementation deliberately renders the hunter and the player's actual weapon and armor through existing original vector components instead of shipping decorative reference gear. Class, level, power, health, opening damage, reduction, and all five draftable attributes now share one bounded mobile scroller; the full workshop remains one explicit action away. Android/editor builds reuse the already registered ornamental class surface and portrait frame, so the placeholder allowlist remains exactly thirteen images. Version 0.7.0/code-30 changes navigation and presentation only.
170. Physical Android review found that the first unified sheet stacked the global dashboard, title, class explanation, equipment cards, metrics, workshop action, points, and verbose attribute rows into competing visual layers. The reference's character-slot container, class emblem, inventory shortcut, and status icon were re-audited for hierarchy. The sheet now replaces the global dashboard with a compact location header, enlarges the central hunter, reduces each equipment frame to slot/icon/power, delegates full item names and details to the directly linked arsenal, and removes redundant attribute descriptions while retaining exact effects. Version 0.7.1/code-31 changes presentation only.
171. The physical arsenal still combined field testing, recommendation, equipped upgrades, loadouts, filters, bulk recycling, inventory cards, preferences, and destructive debug controls in one portrait stack; the actual inventory viewport could collapse to little more than its scrollbar. Reference inventory and character sub-navigation support a deliberate two-surface hierarchy. `EQUIPADO` now contains only build evaluation and investment, while `MOCHILA` owns item management and preferences. The transient section survives ordinary board navigation, but character, reward, funded-build, and defeat-recovery routes force the relevant equipped view. Version 0.8.0/code-32 changes navigation and presentation only.
172. Physical-board review exposed the same hierarchy problem one level earlier: six destinations appeared before the contract, making the root screen read like a settings dashboard instead of a bounty location. The reference's location-first navigation principle supports separating the active activity from secondary feature access without copying its iconography or layout. `MANDADOS` now opens by default with onboarding/receipt, exact warrant progress, and the active contract; `DESTINOS` owns a readable 2×3 grid for arsenal, market, hangar, map, career, and hunter management plus current location/transport context. Equipment chips leave the board because the unified hunter sheet and arsenal already own that evidence. The selected tab is session-only, Android Back returns destinations to contracts before exiting, and real 450×800 captures plus mobile/UI integration protect both surfaces. The complete suite plus boot passes in 56.49 seconds; the internal Android package passes at 51.42 MB with all thirteen documented placeholders, API 24+, ARM64, and the stable test signature. Version 0.8.1/code-33 changes navigation and presentation only.
173. The galaxy map still rendered every location as a paragraph card, obscuring the world overview behind five similar text blocks. Visual review also exposed that `PlanetIcon` matched obsolete abbreviated IDs, so all canonical planets lost their intended interior motif and appeared mostly as generic colored circles. The renderer now resolves the exact stable IDs into five tested motifs—crater, fractured ice, spores, machinery, and quasar ring—and uses compact location rows: current orbit receives an accent frame and exact warrant progress, completed chapters retain capture totals, the available route exposes the sole `VIAJAR` action, and locked routes name one prerequisite. All five destinations fit together at 450×800 over the illustrated world surface. The complete suite plus boot passes in 55.89 seconds; the 51.43 MB Android package retains the thirteen documented placeholders, API 24+, ARM64, and the stable signature. Version 0.8.2/code-34 changes presentation only.
174. Hunt incidents still presented three mechanically distinct choices as nearly identical form rows, forcing the player to read every small sentence before recognizing the repeated decision pattern. All ten incidents already share three semantic families, so an original vector grammar now makes them scannable: a shield marks paid tactical advantage, a branching route with clock marks time exchanged for XP, and a lightning bolt marks greater combat risk for credits. Gold, cyan, and coral card treatments reinforce those roles without replacing exact effects. Financial copy contracts from a legalistic sentence into `VITÓRIA` and, when paid, `LÍQUIDO`; affordability and exact missing-credit actions remain unchanged. The complete first incident fits at 450×800 with the illustrated location visible and all three 48-unit actions intact. The complete suite plus boot passes in 55.72 seconds; the 51.43 MB Android package retains the thirteen documented placeholders, API 24+, ARM64, and the stable signature. Version 0.8.3/code-35 changes presentation only.
175. Class selection repeated name, attribute, two fiction lines, specialization, and build impact inside every archetype card, while the career ledger distinguished its five planets primarily through small text. The class screen now keeps all three choices directly visible as compact emblem selectors and expands exactly one focused sheet with the complete formula and live build effect; an unassigned hunter must still draft even the default preview before confirmation. Career adds a truthful next-level XP bar and reuses the tested original planet motif system for five colored capture rows with explicit current/completed/locked states, keeping all sectors and the first milestone visible at 450×800. The twenty-target archive, mastery shortcut, exact rewards, state transitions, and economy remain unchanged. The complete suite plus boot passes in 54.53 seconds; the 51.43 MB Android package contains all thirteen documented placeholders and passes API 24+, ARM64, version, pack, and stable-signature checks. Version 0.9.0/code-36 changes presentation only.
176. The arsenal still mixed two narrow upgrade columns with inventory filters, item actions, accessibility preferences, and a destructive debug reset, while its generic fort placeholder did not communicate equipment management. The documented workshop placeholder now uses the reference weapon-store scene, which reduces the staged test APK despite providing a clearer bench context. `EQUIPADO` renders full-width weapon and armor rows with the existing original equipment icons and separate calibration/reinforcement actions; `MOCHILA` owns only item filtering, sorting, protection, equipping, and recycling; `AJUSTES` owns sound, reduced motion, and a clearly described local-test reset inside its own danger surface. No equipment, economy, persistence, or preference semantics change. Real 450×800 captures cover all three surfaces, including the new settings state. The complete suite plus boot passes in 54.31 seconds; the 45.96 MB APK retains exactly thirteen documented placeholders and passes API 24+, ARM64, version, pack, and stable-signature checks. Version 0.9.1/code-37 changes navigation and presentation only.
177. Reward screens separated loot and equipment comparison but still flattened target mastery, capture streak, warrant progress, unlocks, and route projection into centered lines with weak semantic distinction. An original vector receipt grammar now assigns a crosshair to mastery, forward chevrons to streak, and a warrant document to contract-chain progress. Each category owns a compact left-aligned row, and related consequences such as mastery bonus or post-equip route odds stay paired beneath the same marker. Exact values, node identities, transaction choices, priority actions, and reward state remain unchanged. Real 450×800 captures cover ordinary repeat, simultaneous mastery/warrant unlock, next-warrant promotion, and immediate recycling with all actions visible. The complete suite plus boot passes in 52.42 seconds; the 45.96 MB APK retains exactly thirteen documented placeholders and passes API 24+, ARM64, version, pack, and stable-signature checks. Version 0.9.2/code-38 changes presentation only.
178. Combat's correct relative-health text occupied a narrow arena with small portraits and no corresponding visual measure, while victory scattered target portrait, verdict, aggregate report, payment, and payment status across separate vertical elements. The arena now gives both fighters larger 152-unit portraits and a full-width cyan/coral pressure strip whose proportions derive only from current remaining-health ratios; exact HP bars, turn events, narrative, speed, and automatic timing remain unchanged. Victory groups target, final blow, summary, build evidence, and payment as one capture dossier, while victory and defeat reports share an immediate check/cross verdict header. Early and late-planet 450×800 captures keep the full arena, turn report, speed action, and victory reward action visible. The complete suite plus boot passes in 53.65 seconds; the 45.97 MB APK retains exactly thirteen documented placeholders and passes API 24+, ARM64, version, pack, and stable-signature checks. Version 0.9.3/code-39 changes presentation only.
179. The persistent header spent four tall cards on credits, scrap, rank, and victories before every active screen, while the paused incident left a large empty stage above a centered text block. The global header now keeps the hunter shortcut, level, power, location, build version, and all four resources in a single compact ledger, returning vertical space to contracts, briefing, commerce, travel, combat, and reports. Incidents lead with a horizontal field dossier that binds signal code, title, fiction, paused position, and remaining time before the existing shield/route/lightning decisions. Real 450×800 captures keep all three briefing routes, incident choices, and commerce offers clear while showing more illustrated location. The complete suite plus boot passes in 52.06 seconds; the 45.97 MB APK retains exactly thirteen documented placeholders and passes API 24+, ARM64, version, pack, and stable-signature checks. Version 0.10.0/code-40 changes presentation only.
180. The board's separated `DESTINOS` surface fixed activity hierarchy but its six outlined buttons still relied on small labels over illustrated scenery, making navigation slower than the reference's icon-led location grammar. Six original vector emblems now identify arsenal, market, hangar, galaxy, career, and hunter before reading; their color, label, exact dynamic count/state, focus ring, callback, 78-unit card, and 2×3 geometry remain intact. The implementation ships no bitmap and has bounded draw cost. A real 450×800 capture keeps the full grid and current-location card together, while mobile and expanded-text integration remain valid. The complete suite plus boot passes in 51.86 seconds. Version 0.10.1/code-41 changes presentation only.
181. The live hunt centered the target, chosen approach, and transport effectively but left its progress bar semantically anonymous; only the changing countdown explained movement. A compact pursuit track now names `PARTIDA`, one of three truthful progress-derived stages, the exact percentage, and `ALVO` immediately above the existing authoritative bar. No timer, incident trigger, transport reduction, AFK reconciliation, or abandonment consequence changes. The 450×800 capture preserves the illustrated waiting composition, target identity, route, transport, countdown, and bottom action. UI and 125% typography checks remain valid; the complete suite plus boot passes in 54.41 seconds. Version 0.10.2/code-42 changes presentation only.
182. The project had polished individual screens but no stable game-level navigation: primary destinations were hidden behind a `DESTINOS` tab, the same six-card grid mixed core identity with shops/settings, and every screen relied on bespoke return buttons. The reference's stable icon-led location hierarchy was re-audited. A persistent five-slot mobile dock now owns Mandados, Arsenal, Caçador, Galáxia, and Menu with original vector icons, strong selected states, exact 64-unit touch targets, and contextual badges for class, status points, career claims, and funded field tests. `MENU DA FRONTEIRA` becomes a genuine secondary 2×2 service surface for Mercado, Hangar, Carreira, and Ajustes plus illustrated planet/transport context. Services return to Menu; primary hubs return to Mandados; contract-owned phases clear and hide the dock. Real 450×800 captures keep complete board, arsenal, character, class, galaxy, market, hangar, career, and settings actions visible above the dock, while mobile, focus, and 125% typography integration remains valid. The complete suite plus boot passes in 53.74 seconds. The 45.98 MB APK retains all thirteen documented placeholders and passes API 24+, ARM64, version, pack, and stable-signature checks. Version 0.11.0/code-43 changes navigation and presentation only.
183. A remaining prototype seam placed device preferences and the destructive local-test reset inside the Arsenal, despite the new secondary menu already presenting Ajustes as a service. Ajustes is now an independent game surface with its own title, return hierarchy, focusable controls, and Menu-selected dock state; Arsenal is reduced to the two player-facing equipment tasks, Equipado and Mochila. While exercising this separation, the career audit exposed a teardown race that could clamp a remembered archive position when a claimed milestone rerendered the page. Career scroll restoration now blocks transient layout signals and reapplies the exact position after responsive layout settles. The dedicated 450×800 settings capture fits all controls above the persistent dock, and the complete suite plus boot passes in 52.99 seconds. The 45.98 MB APK retains all thirteen documented placeholders and passes API 24+, ARM64, version, pack, and stable-signature checks. Version 0.11.1/code-44 changes navigation and presentation only.
184. GPU captures of rapid Board-to-Menu navigation exposed an intermittent dock-frame defect invisible to semantic tests: the old dock row was detached but remained queued for end-of-frame destruction, allowing stale CanvasItems to mask the three newly drawn central icons while their buttons and accessibility tree remained valid. Dock replacement now frees the detached row synchronously before constructing its successor. Mobile integration asserts that the route transition leaves exactly five buttons and five icons, and the new 450×800 capture visually confirms Mandados, Arsenal, Classe/Caçador, Galáxia, and Menu together. The complete suite plus boot passes in 52.47 seconds. The 45.98 MB APK retains all thirteen documented placeholders and passes API 24+, ARM64, version, pack, and stable-signature checks. Version 0.11.2/code-45 is a visual render-lifecycle hotfix only.
185. The post-interface audit found that the game-level shell was coherent but contract decisions still flattened class identity: fast play dominated recommendations, the corporate route rarely paid enough to justify its pressure, most established Dustball options saturated at 99%, and a new hunter could enter the bounty loop without seeing class choice. Route economics now trade a slower 35% fast premium against a doubled-credit, three-scrap corporate payout; only Dustball's aggressive routes receive extra introductory resistance, while the deliberately safe Gloop contract remains the tutorial gate. Every prototype class declares a preferred contract style, and the shared recommendation engine applies that profile only after the 55% viability gate, exposing exact class synergy in briefing, arsenal, and reward projections. Deterministic complete-career samples report 0% stalls and no recommended sub-55% attempts: Quebra-Mandados chooses quiet/fast/corporate at 21/45/35%, Pistoleiro at 21/73/6%, and Hacker at 35/50/15%; Dustball saturated options fall from the earlier roughly 73–83% range to 45–49%. The first board now names the pending identity decision and links directly to the class selector, whose focused sheet names its contract style. Real 450×800 captures keep onboarding, all three class selectors, and all three briefing actions visible. The complete 36-suite profile, exhaustive persistence matrix, and boot pass in 53.31 seconds. The 45.98 MB Android artifact contains all thirteen documented placeholders and passes API 24+, ARM64, version, pack, and stable-signature checks. Version 0.12.0/code-46 is the first class-aware contract-balance release.
186. The next interface audit found two remaining layers that read as prototype instrumentation rather than game locations: the Frontier Menu used tiny centered multiline button copy, and combat separated its route, incident consequence, and payment into loose text above an otherwise strong arena. Each service card now pairs a larger left-aligned destination name with a concise function or live state beside its emblem; combat opens with one compact contract dossier containing turn, route, incident, exact payment, and streak bonus. Repeated GPU captures also reproduced the supposedly fixed disappearing-dock defect: semantic controls survived, but rebuilding CanvasItems during rapid Board-to-Menu transitions could still lose central icon/caption draws. The dock tree now persists and updates selection, captions, icon colors, and badges in place; mobile integration asserts object identity across navigation, and contract-owned phases hide rather than destroy it. The capture harness clears stale AFK receipts before screenshots. Real repeated 450×800 captures confirm all five dock destinations, the four service cards, complete combat arena, report, and speed action. The complete 36-suite profile, exhaustive persistence matrix, 125% typography coverage, and boot pass in 56.14 seconds. The 45.98 MB Android artifact includes all thirteen documented placeholders and passes API 24+, ARM64, version, pack, and stable-signature checks. Version 0.12.1/code-47 changes presentation and render lifecycle only.
187. The first Builds 1.0 batch found that all three prototype classes shared primary-stat Power scaling, but only Hacker changed an actual combat turn. The declarative effect dictionaries now give every class an immediate, scaling signature without ID-specific branches: Quebra-Mandados' Casco Duro absorbs one base damage per enemy hit plus one per two invested Strength; Pistoleiro Orbital's Mira Orbital adds 0.5% base attack-roll precision plus 0.25% per invested Dexterity, capped at 3%; Hacker's Invasão adds two base opening damage plus one per invested Intelligence. Each exact live value appears in selection, the hunter sheet, affected turn event, and final build evidence; generic opening and class invasion are separated so the report never double-counts them. Recognized composite class events survive an interrupted combat, while sanitization still rejects invented evidence. The initial 12% Gunslinger precision cap saturated mature route choices, so deterministic guard rails reduced it to 3% before release. Sixty recommended careers (twenty per class) and fifteen corporate-when-viable stress careers (five per class) all finish with 0% stalls and no chosen fight below the 55% recommendation gate. Repeated 450×800 captures keep all three selector rows, focused mechanics, hunter sheet, combat arena, and victory action readable; 125% typography also passes. The complete 36-suite profile, exhaustive persistence matrix, and boot pass in 59.42 seconds. The 45.99 MB Android artifact includes all thirteen documented placeholders and passes API 24+, ARM64, version, pack, and stable-signature checks. Save schema stays v9 because every new value derives from the existing class and attributes. Version 0.13.0/code-48 begins mechanical class identity.

188. The inventory contract is now independent of the three placeholder classes and any future roster. One centralized nine-slot catalog defines weapon, helmet, suit, gloves, boots, technical rig, implant, gadget, and alien relic across player state, build aggregation, item validation, equipment transactions, protection, both loadouts, comparison, filters, hunter sheet, arsenal overview, and code-drawn slot silhouettes. Schema v10 preserves the equipped weapon and suit of every established save, adds seven empty positions, and expands both loadout records without selecting or inventing loot. The current reward and market catalogs deliberately remain weapon/suit-only until each added family has an explicit stat role and campaign simulation. The 37-suite fast profile plus boot passes in 51.17 seconds and the exhaustive profile passes in 68.15 seconds, including a dedicated universal-equipment integration test and the existing mobile and 125% text guards. The 45.99 MB Android artifact passes content, API 24+, ARM64, version, and stable-signature checks with all thirteen documented placeholders. Version 0.14.0/code-49 establishes the class-neutral paper doll without changing current campaign balance.

189. The first three empty positions now become real campaign loot without reopening the original vertical-power problem. Congelária introduces helmets, Micélia adds gloves, and Ferro-Velho adds boots; Dustball remains a two-slot tutorial, later planets inherit all earlier families, and the arsenal explicitly names active or upcoming secondary drops. Thirty-six planet-specific item definitions and nine canonical modifications give the families opening, health, Power, and mitigation identities. The first proportional-power trial was rejected after raising saturated route options from the established ~40% range to 63% and making the Breaker's corporate route dominate 79% of choices. The accepted lateral curve fixes secondary base Power at one, reserves a second point for epics, and moves value into rare effects. Five-career cross-class probes then completed with 0% stalls and no recommended attempt below 55%; saturated options measured 50% for Breaker, 37% for Gunslinger, and 36% for Hacker, comparable to the preceding class-signature baseline. The complete 37-suite profile, expanded exhaustive equipment persistence matrix, mobile layout, 125% text, and boot pass complete in 96.13 seconds. The 46.00 MB Android artifact passes content, API 24+, ARM64, version, and stable-signature checks with all thirteen documented placeholders. Version 0.14.1/code-50 changes content and presentation without changing save schema.

190. The first independent PvE ladder now gives the universal inventory a second progression axis without turning planet contracts into a nine-slot drop pool. Completing Dustball reveals the six-floor Fenda Clandestina in the Frontier Menu. Every floor previews deterministic combat odds, its fixed credits/XP, and one unique lateral reward before entering the existing animated automatic combat directly: floors one through three introduce technical rigs and floors four through six introduce implants. Fenda transactions never increase warrant wins, reputation, planet captures, target mastery, or capture streak; defeat preserves both the current floor and an active warrant streak, while claim advances exactly one canonical floor and cannot be repeated or skipped. Initial balance probes rejected a curve whose first two floors saturated at 99% and whose third collapsed to 2%. The accepted curve anchors floors one, three, and four near 74% at representative Congelária, late-Micélia, and Ferro-Velho builds, while the final jailer is a 32% aspirational fight for the established Cassino boss build. Schema v11 persists bounded floor progress and canonicalizes interrupted fights and unique pending loot. The complete 38-suite profile, including the exhaustive persistence matrix, mobile layout, 125% text, and boot pass completes in 110.67 seconds. The 46.01 MB Android artifact passes content, API 24+, ARM64, version, and stable-signature checks with all thirteen documented placeholders; published SHA-256 is `f39c79fe3dae1546e49c5f8fa2b58d07d2646be507423b424375af2cb83719c0`. Version 0.15.0/code-51 adds the first dungeon-style progression branch and two real universal equipment families.

191. A cross-class audit rejected the original Gunslinger-only Fenda calibration: representative Breaker odds reached 99% where Hacker fell to 37%, producing a 62-point first-floor spread and allowing mitigation to dominate long encounters. The accepted encounter grammar is explicit and class-neutral: every Fenda enemy ignores 75% of damage mitigation and doubles opening damage, with the fifth floor using ×1.5 to avoid saturating the Hacker checkpoint. Floors two and five receive the additional enemy pressure needed to keep the cumulative reward path meaningful. Across six campaign checkpoints, all eighteen class/floor outcomes now remain between 41% and 89%, with per-floor spreads of 14–34 points. The unique rigs and implants add zero points to safe-route campaign odds in the audit and at most 22 points to any aggressive route, so they enable lateral risk-taking without becoming mandatory recovery gear. These bounds are executable in both the dedicated challenge suite and `tools/simulate_challenges.gd`. The Fenda dossier states both anomaly rules before entry, while Career now records its locked, current, or completed six-floor state and links directly to the next encounter. The complete 38-suite profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 133.99 seconds. The 46.02 MB Android package passes content, API 24+, ARM64, synchronized version, and stable-signature checks with all thirteen documented placeholders; published SHA-256 is `604dc06ca856776a762fa05e54cd1f00fdf964a19b189eb23746fb6ae08eaf04`. Version 0.15.1/code-52 changes balance and presentation without a save migration.

192. The follow-up depth audit found that one global Fenda anomaly made six enemies numerically different but mechanically interchangeable. A first full-mitigation profile was rejected after widening the representative class advantage to 57 points. The accepted ladder instead teaches three explicit profiles twice: Câmara Volátil amplifies opening damage, Falha de Contenção pierces 90% of mitigation and leaves integrity untouched, and Âncora Inercial retains moderate opening charge and half of mitigation. Their names, intended build axes, descriptions, and exact multipliers are visible before entry. With cumulative rewards equipped, floors one through five span 46–88% and the final floor 50–81%, while every per-floor class spread stays at or below 34 points. Safe campaign routes gain zero points and any audited route at most 22, preserving lateral rewards. The capture harness also stops with `ERR_UNAVAILABLE` when a renderer returns no image rather than falsely announcing empty captures, and its graphical pass now records all three dossiers. The complete 38-suite profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 120.91 seconds. The 46.02 MB Android package passes content, API 24+, ARM64, synchronized version, and stable-signature checks with all thirteen documented placeholders; published SHA-256 is `985115b4d748a76327a85dffec95da4c9971e1f1cf568d69a0e3592a22ef8358`. Version 0.15.2/code-53 changes encounter identity, balance, presentation, and review reliability without a save migration.

193. The game previously opened directly on Mandados with an empty class represented only as a dock badge. The new production gate replaces that optional identity with a four-step, Android-first creation transaction: an explicitly local login, class, species, and validated hunter name. Account/session data is stored outside the player record, while every character confirmation saves independently and interrupted creation resumes at its first incomplete field. Three clearly provisional species add narrative identity without changing combat formulas: Terrano Remendado, Sintético Descontinuado, and Nômade Nebular. The state layer rejects bounty and Fenda entry while the real application gate is incomplete; isolated deterministic fixtures can still construct partial profiles without impersonating production. Schema v12 recognizes established saves as returning local sessions, preserves their class and all campaign data, and never invents missing species or name. Fresh-start recovery now correctly returns to login, while a damaged save remains higher priority than onboarding. The hunter header and sheet carry the confirmed name and species, and the visual harness records all four creation stages. The complete 39-suite profile, exhaustive persistence matrix, mobile/touch and 125% text guards, and clean boot pass in 131.02 seconds. The 46.03 MB Android package passes content, API 24+, ARM64, synchronized version, and stable-signature checks with all thirteen documented placeholders; published SHA-256 is `35d8af4b8437b00207bfd05e694d9721cbe7e2e01e40e65a25b8e7d1dc902fd7`. Version 0.16.0/code-54 establishes mandatory player identity before gameplay.

194. Identity was mandatory but species still read as three text rows and the final name step offered no safe way to correct an earlier tap. Each provisional species now owns an original scalable emblem and a deterministic procedural-portrait treatment: Terrano Remendado keeps the warm organic face, Sintético Descontinuado adds a cool chassis palette and facial seams, and Nômade Nebular adds violet skin, a luminous third eye, and antenna marks. The same player dictionary already used by header, sheet, and combat drives the result, so the visual identity persists without a new save field, bitmap, or combat branch. The final review shows the assembled portrait and 48-unit class/species correction actions; reopening one choice retains the other, saves the correction, and returns to naming. Dedicated onboarding coverage proves all three emblems, draft/confirmation boundaries, both correction loops, touch targets, persisted identity, and unchanged portrait equipment signals. The 39-suite fast profile plus boot passes in 67.90 seconds; the complete profile with exhaustive persistence passes in 123.33 seconds. The 46.04 MB Android package passes content, API 24+, ARM64, synchronized version, and stable-signature checks with all thirteen documented placeholders; published SHA-256 is `f9caa62695ea5cf9e7c9000d30c09211cc12eda60a81ed5e38d72ede48f7b253`. Version 0.16.1/code-55 changes identity presentation and onboarding correction only.

195. The initial roster now contains eight visually distinct, explicitly cosmetic species: Terrano Remendado, Sintético Descontinuado, Nômade Nebular, Miceliano de Porão, Ferrídeo Oxidado, Abissal de Tanque, Luminar Instável, and Quimera de Catálogo. Five new code-drawn emblems, face palettes, and portrait marks preserve tiny-screen silhouette differences without bitmap residency; none enters state, combat, attribute, loot, economy, or progression formulas. The onboarding copy and product vision now make that permanent boundary explicit instead of promising future mechanics. The full 450×800 capture keeps all eight options in one readable scroll surface, with the confirmation action after the list, while mobile target and 125% text guards remain intact. Dedicated tests require exactly eight valid displayed identities, eight scalable emblems, and unique skin/accent palettes. The complete 39-suite profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 131.57 seconds. The 46.04 MB Android package passes content, API 24+, ARM64, synchronized version, and stable-signature checks with all thirteen documented placeholders; published SHA-256 is `f609b9c783bdc4bdf878a73d0ba813477909e1b8f2d6028674b6b97587b121ec`. Version 0.16.2/code-56 changes cosmetic identity only.

196. Expanding the roster exposed a creation-flow seam: species were compared primarily through 58-unit emblems, and a constrained or enlarged-text layout could rebuild from the top after a lower choice before the player reached confirmation. The species step now gives the current draft a 106-unit live hunter portrait, name, and identity preview; confirmation remains fixed below the scrolling roster. Scroll state is captured before tree replacement and restored against the current responsive tree, avoiding callbacks that retain freed controls during rapid taps. A constrained-layout regression test proves lower-roster selection, live-preview update, position restoration, and fixed 48-unit confirmation. The graphical 450×800 review shows the selected Luminar, seven complete cards, the eighth reachable under a visible scrollbar, and the confirmation footer unobscured. The complete 39-suite profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 124.44 seconds. The 46.04 MB Android package passes content, API 24+, ARM64, synchronized version, and stable-signature checks with all thirteen documented placeholders; published SHA-256 is `4d7134b1df30e48464e3c196d9991b184a3c2342c527d82d620c958f05d6fa0b`. Version 0.16.3/code-57 changes onboarding presentation only.

197. Mandatory class creation lagged behind species creation: the three mechanical archetypes were text-only cards even though their mapped reference icons were already staged and documented. Class onboarding now uses those placeholders in a 106-unit focused preview and 58-unit roster markers, names the primary attribute and preferred contract style, and exposes the exact active specialization in a dedicated provisional-archetype dossier before commitment. Confirmation is fixed outside the selector, and the shared constrained-layout restoration no longer lets automatic focus hide the preview. Restoration timers are owned by the active scene and generation-checked, so rapid stage changes and test teardown cannot resume freed UI objects. During graphical validation, the revenge reward exposed an older empty-slot defect: a first helmet, gloves, or boots drop dereferenced `power` on an empty equipped dictionary. Reward comparison now treats an empty universal slot as +0, with an isolated regression proving the first secondary drop is an upgrade; the complete graphical harness again finishes without script errors. The complete 39-suite profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 132.64 seconds. The 46.04 MB Android package passes content, API 24+, ARM64, synchronized version, and stable-signature checks with all thirteen documented placeholders; published SHA-256 is `a402b879aeef22b8ea56d0e1e6573eb8145aa32e63b47774f19c0c0dda423233`. Version 0.16.4/code-58 changes onboarding presentation and repairs empty-slot reward comparison without changing combat or saves.

198. International scope and future server authority were absent from account identity, so adding either later would have forced another ambiguous split between device state and character state. Locale and shard now have declarative registries outside gameplay data. Fresh creation explicitly selects Portuguese and `International 1`; the state boundary rejects unavailable locales and unknown worlds, applies the account locale through Godot's translation service, and persists both fields independently from class, species, and hunter name. English appears only as `EM TRADUÇÃO` and cannot create a session until the entire playable surface is localized, avoiding a misleading mixed-language build. The login states that International 1 is a global multilingual world while also stating that this APK remains device-local with no authoritative backend. The compact game header carries `INT-1` beside the exact build version. Schema v13 moves established saves to Portuguese on International 1 but preserves an empty interrupted login instead of inventing an account. Migration, invalid-selection, persistence, touch-target, 125% text, and mobile-header coverage pass; the graphical 450×800 login capture remains clear. The complete 39-suite profile, exhaustive persistence matrix, and boot pass in 127.98 seconds. The 46.05 MB Android package passes all thirteen placeholder, API 24+, ARM64, synchronized version, and stable-signature checks; published SHA-256 is `e3335d9cb53aca5112c58bbe5189f973c6749009233caf275931bc3e248c3e56`. Version 0.17.0/code-59 establishes multilingual and server-scoped account identity without pretending that a live backend already exists.

199. The language selector had honest availability rules but no translated playable surface behind its English roadmap. The complete four-stage onboarding now resolves stable semantic keys through Godot translation catalogs rather than Portuguese display literals. Portuguese and English PO resources cover login/server explanation, every class name/attribute/tagline/flavor/route, exact numeric specialization language, all eight species names/identities/taglines, final identity review, validation guidance, actions, tooltips, and state transaction feedback. Shared class and species name helpers also resolve localized identities without changing their stable IDs or mechanics. English remains unavailable to production sessions because the post-onboarding game is not yet fully extracted; tests force the locale only for QA and prove all four stages before returning to the Portuguese production path. Four real 450×800 English captures keep login and naming entirely visible, all three class comparisons readable, and the eight-species roster reachable behind its fixed confirmation. The fast 39-suite profile passes in 69.76 seconds; the complete profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 137.12 seconds. The 46.06 MB Android package contains both catalogs and all thirteen documented placeholders while passing API 24+, ARM64, synchronized version, and stable-signature checks; published SHA-256 is `de30913d5bdc19d0c4272a5c4674d1a135253bd83817ee260f2c53cf604ea9a0`. Version 0.17.1/code-60 adds the first fully bilingual product surface without changing save schema, combat, economy, or player progression.

200. After onboarding, every persistent navigation landmark still returned to Portuguese, so even an internally forced English locale could not be evaluated beyond character creation. Stable keys now cover both compact headers, International 1 build identity, hunter level/power, the four resource cells, all five primary dock destinations and their contextual `CLASS`/`STATS`/`TEST` variants, the complete Frontier Menu, dynamic service states, current planet and active transport names, and the independent Settings surface including audio, reduced motion, and explicit local reset. Five planet names and four transport names resolve through stable content IDs rather than altering saved records. Real 450×800 English captures keep the five-service grid, current-position card, full dock, every setting, and destructive-test boundary visible without overlap; the mobile integration forces English, navigates header → Menu → Settings, verifies live transport identity and values, restores Portuguese, and then executes the pre-existing full mobile audit. The fast 39-suite profile passes in 69.88 seconds; the complete profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 126.86 seconds. The 46.06 MB Android package contains both expanded catalogs and all thirteen documented placeholders while passing API 24+, ARM64, synchronized version, and stable-signature checks; published SHA-256 is `90e0cac4c3f606c94a0ed7c1926d5bb607e103caffd4b73ff3675a9408284d30`. Version 0.17.2/code-61 expands the bilingual shell without changing save schema or gameplay.

201. The first meaningful decision loop after login still became mixed-language at the wanted board. The complete board-to-hunt path now resolves stable keys for planet context, warrant rank, primary and repeat routes, mastery, streak, risk, payout, actions, briefing guidance, dynamic class recommendation, build metrics, transport timing, field-test history, hunt stages, and safe abandonment. All twenty targets contribute localized name, title, and description records; all three approaches contribute localized name, tag, and fiction. Stable target/approach IDs remain authoritative in saves and rules, including resumed hunts and overridden field tests. English QA now validates all 69 content fields, then executes board → briefing → committed hunt before restoring Portuguese for the existing Android audit. Real 450×800 captures keep the active warrant above the fold, all three briefing choices and actions visible together, and the hunt identity/countdown/abandon action unobstructed. The fast 39-suite profile passes in 70.91 seconds; the complete 40-suite profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 135.19 seconds. The 46.07 MB Android package contains both expanded catalogs and all thirteen documented placeholders while passing API 24+, ARM64, synchronized version, and stable-signature checks; published SHA-256 is `abd764e7f116dc459dc44ac4ec2bb246e3d4ae007c08dc8a1a8dc226a061df3b`. Version 0.18.0/code-62 expands the bilingual playable sequence without changing save schema, combat, economy, or progression.

202. The localized hunt ended at the first incident and then exposed Portuguese decision text, saved consequences, attack names, combat evidence, and victory handoff. All ten planet incidents now resolve stable event title/description keys and all thirty choices resolve name, mechanical effect, result, affordability, gross payment, momentum, and net payment. Chosen consequences are re-resolved from the stable choice ID on resumed hunts and in the combat dossier while the canonical Portuguese sentence remains save-compatible. Automatic warrant combat now localizes all four hunter attacks, sixty target attacks, damage qualities, tactical effects, pressure, turn evidence, route diagnosis, defeat recovery, and victory/payment handoff without changing canonical combat events or formulas. Legacy field-test contexts that predate approach IDs preserve their recorded names instead of collapsing to the base-contract fallback. Forced-English mobile QA validates every incident and attack key and executes board → briefing → hunt → incident → consequence → combat → victory before restoring Portuguese. Real 450×800 captures keep all three incident choices above the fold, the resumed consequence readable, the arena evidence intact, and `OPEN REWARD` visible on victory. The fast 39-suite profile passes in 72.30 seconds; the complete 40-suite profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 127.77 seconds. The 46.08 MB Android package contains both expanded catalogs and all thirteen documented placeholders while passing API 24+, ARM64, synchronized version, and stable-signature checks; published SHA-256 is `a35b60517189ee2aaa86a8b73e4187a580b1aa0a7949836629f3198bad37f042`. Version 0.19.0/code-63 expands the bilingual playable sequence without changing save schema, combat, economy, or progression.

203. Victory could open a localized action but the reward itself still exposed Portuguese item identity, comparison, progression, transaction choices, and the board receipt. The complete normal-contract reward now resolves rarity, all nine universal slot names, all 76 current planet-catalog item names and descriptions, all 21 item modifications, equipment deltas, salvage value, incident reconciliation, target mastery, momentum, next-warrant progress and projected odds, plus equip/store/recycle/repeat/workshop/board actions through stable Portuguese/English keys. Canonical saved item records remain unchanged; display identity is recovered from planet, slot, and catalog index, preserving established and interrupted rewards without a migration. Claim feedback is localized from the structured transaction before returning to the board, including XP, credits, scrap, equipment action, streak, level, attributes, and unlocks. Forced-English mobile QA exhaustively resolves every item and modification field, then executes victory → reward → claim → board and rejects a Portuguese receipt. Real 450×800 captures confirm one dense evidence panel above two thumb-zone decisions and one compact post-claim board banner; visual review also found and localized the internal-reference watermark. English remains unavailable to production sessions while character/equipment and the remaining game surfaces are incomplete. The fast 39-suite profile passes in 73.95 seconds; the complete 40-suite profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 139.89 seconds. The 46.10 MB Android package contains both catalogs and all thirteen documented placeholders while passing API 24+, ARM64, synchronized version, and stable-signature checks; published SHA-256 is `e9b0911fd45802440c482f23e673a5f6e15c335f2e0c09ee9416eb96c24ac300`. Version 0.20.0/code-64 closes the first bilingual board-to-reward contract loop without changing save schema, combat, economy, or progression.

204. The bilingual contract loop returned to Portuguese as soon as the player inspected the Hunter or Arsenal, making build decisions impossible to evaluate in the selected language. Stable keys now cover Hunter identity, species/level/class summary, five attributes and live effects, spendable-point drafting, three-class comparison and specialization, all nine universal slots, both starter items, planetary kits, field-test diagnosis, inventory filters/sorting/paging, rarity and origin, workshop investment, item modifications, protection, recycling, loadouts, projected upgrade value, and equip/calibrate/reinforce transaction feedback. Item display is centralized in `equipment_presentation.gd`, so reward, Hunter sheet, inventory, loadouts, and workshop resolve the same canonical planet/slot/catalog identity instead of drifting into separate translations. Legacy tested-route records without approach IDs retain their recorded names, preserving the exact diagnosis shared by defeat and recovery workshop. Forced-English mobile QA navigates Hunter → Classes → equipped Arsenal → Backpack → equip transaction → Workshop, while exhaustive item/modification coverage from the reward milestone remains active. Real 450×800 captures confirm the reference-inspired Hunter paper doll, three-class dossier, nine-slot workbench, and two complete inventory cards above the fold; the only visual inconsistency found, the Congelária drop hint, now uses the canonical English world name. English remains unavailable while galaxy, commerce, progression, Fenda, and finales are incomplete. The fast 39-suite profile passes in 74.62 seconds; the complete 40-suite profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 131.06 seconds. The 46.11 MB Android package contains both catalogs and all thirteen documented placeholders while passing API 24+, ARM64, synchronized version, and stable-signature checks; published SHA-256 is `0eaf1cde1a2ba2aac465bf64355408291d46cf8fb1b73ed02d5dc93a0cf0b4fd`. Version 0.21.0/code-65 completes the bilingual Hunter/build-management branch without changing save schema, combat, economy, or progression.

205. Galaxy travel was visually mature but still returned a forced-English session to Portuguese for its title, route states, chapter progress, transport timing, actions, and transaction receipt. The complete map now resolves all five canonical planet names/subtitles, the active transport, completed/current/locked route evidence, exact warrant progress, prerequisite planets, and travel actions through stable keys. Selecting a route also repairs an older navigation seam: the state transaction remains authoritative, but the UI now returns explicitly to the destination's warrant board so the localized receipt is visible instead of being hidden on the secondary destinations tab. Forced-English mobile QA covers active transport timing, all route states, the next unlocked action, canonical destination identity, and the post-travel receipt. The real 450×800 capture keeps all five planets, transport silhouette, timing gain, single available action, and persistent dock visible together without Portuguese leakage. English remains unavailable while commerce, progression, Fenda, and chapter finales are incomplete. The complete 40-suite profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 139.22 seconds. The 46.11 MB Android package contains both catalogs and all thirteen documented placeholders while passing API 24+, ARM64, synchronized version, and stable-signature checks; published SHA-256 is `47ce4e9af664cf91052b8deb1a8e141636b4f1cc669085aef401edbe8a058b5c`. Version 0.22.0/code-66 completes the bilingual galaxy/travel branch without changing save schema, combat, economy, or progression.

206. The two permanent-spending decisions still exposed Portuguese after the bilingual reward, inventory, and galaxy branches. Mercado and Hangar now share the same localized item, rarity, slot, modification, and transport identities already used by reward, Arsenal, Menu, and Galaxy instead of maintaining commerce-only display strings. Stable keys cover balances, refresh cost, automatic-equip policy, three rotating offers, power comparison, buy/sold/shortfall actions, permanent transport alternative, active timing, four transport taglines, chapter locks, market alternative, and all purchase, refresh, store, and equip receipts. Forced-English mobile QA executes a real item purchase and unlocked transport purchase and requires both receipts to remain visible. Real 450×800 captures keep all three market offers above the dock and all four transport cards together; visual review found no Portuguese leakage or action overlap. English remains unavailable while progression, Fenda, and chapter finales are incomplete. The fast 39-suite profile, market/transport transaction suites, mobile/text guards, and clean boot pass in 75.51 seconds. The 46.11 MB Android package contains both catalogs and all thirteen documented placeholders while passing API 24+, ARM64, synchronized version, and stable-signature checks; published SHA-256 is `6191cd22a3f36ff360d8dff1ddcbdf63a1e3748b99f10e46f90e1eff481103f2`. Version 0.23.0/code-67 completes the bilingual commerce branch without changing save schema, prices, timing, combat, loot, or progression.

207. Long-term progression still became mixed-language at the exact point where the player evaluated reasons to return. Hunter Career now resolves level/XP, lifetime AFK and milestone earnings, eight stable milestone identities and requirements, individual and bulk claims, mastery directive, five planetary progress rows, Fenda preview, twenty-record wanted archive, target/planet identity, and cross-planet actions through the same canonical IDs as contracts and galaxy travel. Offline patrol duration, reward, and eight-hour cap are localized, as are immediate XP/scrap/level/rank feedback and milestone receipts. Forced-English mobile QA exercises a real milestone claim, visible receipt, and capped offline settlement rather than checking static headings only. A real 450×800 capture keeps the level dossier, three ready milestones, next mastery route, section navigation, and all five planetary states legible before the long archive scroll; no Portuguese leakage or dock collision remained. English stays unavailable until the Fenda and five chapter finales receive equivalent coverage. The complete 40-suite profile, exhaustive persistence matrix, career transaction suites, mobile/text guards, and clean boot pass in 137.47 seconds. The 46.12 MB Android package contains both catalogs and all thirteen documented placeholders while passing API 24+, ARM64, synchronized version, and stable-signature checks; published SHA-256 is `0ac8d7332abbaaf0329e9c66f4bf8612c1e9cbe8f90783202644ce0c8d5ff500`. Version 0.24.0/code-68 completes the bilingual progression/career branch without changing save schema, reward values, XP curves, AFK limits, or campaign rules.

208. The final localization gap was concentrated in the Fenda, five chapter finales, and low-frequency failure/recovery messages. Both production catalogs now contain exactly 1,193 non-empty, unique, symmetric keys. The complete six-floor Fenda resolves every enemy name/title/description, eighteen attacks, three anomaly profiles, six unique artifacts, traits, comparison, reward handoff, defeat state, and claim receipt through stable IDs. Every chapter finale resolves its planet, boss, completion verdict, metrics, continuation, and post-finale notice. Defeat, abandonment, reset, save-write failure, corrupt-save recovery, and migration feedback no longer bypass the selected locale. English is now a real selectable production language at login and in Settings; selection updates the live tree, persists in account/session identity, and survives save/load without touching class, species, name, or progression. A permanent completeness suite parses both PO files, rejects duplicates/asymmetry/empty values, discovers all 586 literal runtime keys, exhaustively resolves dynamic Fenda/finale families, renders their English surfaces, and exercises PT→EN→PT plus persistence. Real 450×800 review confirms the English login, Settings, Fenda dossier, artifact, and Cassino finale remain readable and unmixed. The complete 41-suite profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 146.21 seconds. The 46.13 MB Android package contains both catalogs and all thirteen documented placeholders while passing API 24+, ARM64, synchronized version, and stable-signature checks; published SHA-256 is `7770dab12712d8319e81ad167c93d560347a6400f73c04370070ed9ecf7e3725`. Version 0.25.0/code-69 completes and enables the Portuguese/English playable product without changing save schema, combat, economy, loot, or progression.

209. International 1 previously existed as a label around a four-string local session, leaving account provider, character ownership, save authority, and future conflict behavior undefined. Schema 14 now separates provider, account, session state, shard, locale, active character, owned characters, authority, synchronization state, local revision, and last acknowledged server revision. Fresh local login binds one stable device-owned character; established saves migrate to the same shape without changing class, species, name, inventory, economy, or progression, while interrupted fresh login still receives neither account nor character ownership. A replaceable `AccountService` adapter reports no backend and cannot manufacture authentication, cloud synchronization, or remote conflict. Atomic saves advance `local_revision` only after a successful commit; failed writes retain the previous revision. Loaded local saves that claim foreign character ownership, server authority, or synchronized state are rejected. The future resolution matrix explicitly distinguishes unavailable backend, foreign character, divergent pending work, remote-newer, local-newer, and equal revision states, and forbids heuristic merges of currency, inventory, rewards, or phases. Settings exposes the product truth in Portuguese and English: local test profile, International 1 device scope, device authority, revision, and no remote conflicts. The contract is documented independently in `Notes/ACCOUNT_SERVER_CONTRACT.md`. Real 450×800 review keeps the added account panel, language selector, accessibility settings, reset boundary, and dock visible together. Both catalogs retain exact 1,199-key parity. The complete 42-suite profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 133.07 seconds. The 46.14 MB Android package contains the new adapter and schema plus both catalogs and all thirteen documented placeholders while passing API 24+, ARM64, synchronized version, and stable-signature checks; published SHA-256 is `4b49e73592105628c27b74eadfbd31f22933c8b746cee11a0f4636d914f45345`. Version 0.26.0/code-70 establishes the account/server boundary without pretending that online infrastructure exists.

210. The first cross-class audit exposed a stable Fenda hierarchy rather than matchup identity: Quebra-Mandados sat at 80–88% on nearly every floor, Pistoleiro Orbital at 46–54%, and Hacker de Contratos between them, with a maximum 34-point spread. The underlying cause was the Breaker's duplicate Strength-to-Power conversion plus per-hit mitigation, while the Gunslinger's campaign-safe 3% precision cap could not express itself against anomaly profiles. Strength class conversion now advances once per three invested points while its hard-shell signature remains intact. An anomaly-owned targeting multiplier amplifies only class precision, never universal Cunning or base rolls. Câmara Volátil now pierces mitigation while amplifying opening damage; Falha de Contenção dampens opening attacks and targeting bonuses; Âncora Inercial preserves mixed defense and stabilizes targeting. The resulting deterministic ladder assigns exactly two favorable floors to each prototype class—Hacker on volatile chambers, Breaker on containment failures, Gunslinger on inertial anchors—with balanced builds at 48–81% on the five mature checkpoints, 52–80% on the opening floor, and a maximum 29-point spread. Permanent guards enforce the intended 2/2/2 matrix, 40–90% viability window, bounded spread, campaign reward boundaries, and separation of Cunning from class precision. Campaign endpoint tests caught and rejected an initially overpowered global precision increase before release, so existing route risk remains intact. Portuguese and English anomaly dossiers were updated in exact 1,199-key parity. The complete 42-suite profile, exhaustive persistence matrix, mobile/text guards, and clean boot pass in 144.59 seconds. The 46.14 MB Android package contains both catalogs and all thirteen documented placeholders while passing API 24+, ARM64, synchronized version, and stable-signature checks; published SHA-256 is `ea58bb62229addeed9f7fb1b6fa980b0376f1e1c81e3a11487d6cba4d0ebf0d1`. Version 0.27.0/code-71 establishes distinct prototype-class matchup identities without changing save schema, inventory shape, economy, loot, campaign targets, or account boundaries.

211. An authorship triage separated reusable game ideas from protected expression and found no concept-level blocker, provided Crooked Galaxy continues using independently created anatomy, art, copy, and implementation. Several provisional names had obvious entertainment or game collisions, so schema 15 replaces every launch-species ID explicitly: Terran, Synthetic, Starworn, Fungoid, Abyssal, Mothari, Scraproot, and Glitchlight. Ferrite and Chimera leave the launch roster for possible later redesign. The three mechanically tested classes become the initial design baseline while their reference-backed preview images remain documented internal placeholders. Character creation gains a required cosmetic stage with palette, eyes, feature, and marking, producing 81 recipes per species without mechanical bonuses or bitmap residency. Established hunters migrate to the neutral recipe; interrupted creation resumes at customization. Portuguese and English remain symmetric, onboarding tests exercise the new five-step flow, and Android metadata advances to 0.28.0/code-72. The complete suite, including exhaustive persistence, passes in 138.42 seconds; final onboarding, translation, and mobile retests also pass. The 46.14 MB ARM64 APK passes version, API 24+, pack, and stable-signature audits with SHA-256 `2d1da87729238962fe304ca7e13976d28352a39f40769ace263923b974f8bb38`. This is a practical collision check rather than a formal worldwide trademark clearance.

212. The remaining authorship risk was not conceptual but literal: Android still staged thirteen third-party study images and the interactive editor preferred them over four already-finished Crooked Galaxy paintings. The runtime reference adapter, feature flag, staged include filter, watermark, and public-build exception are now removed. Contracts, world/career/onboarding, arsenal/commerce, and combat resolve only the four independently generated production environments. Three new scalable code-drawn emblems give Quebra-Mandados a breach hammer, Pistoleiro Orbital twin sidearms and a targeting ring, and Hacker de Contratos a broken circuit seal. A new code-drawn corner frame replaces the portrait bitmap, and an original cyan separator replaces the board ornament. The capture harness now covers the new appearance stage and finalized species IDs. Both export checkers reject raw reference paths and every former staged payload; Android no longer needs access to the local reference library during export. The reference-free APK falls from 46.14 MB to 31.91 MB while retaining ARM64, API 24+, version 0.28.0/code-72, and the stable test signature. Fast checks pass in 77.99 seconds and the complete suite, including exhaustive persistence, passes in 144.45 seconds. The inspected APK SHA-256 is `1d711dc5de135241369b30a0b4cbf6d5998ae3e3622dd2d58788a1e957fe9a87`.

213. The finalized class identities previously stopped at passive stat conversion: mitigation, precision, and opening damage. Automatic combat now adds a restrained second mechanical layer. Quebra-Mandados retaliates after every third surviving enemy exchange; Pistoleiro Orbital has a bounded evasion window and a small follow-up burst on a perfect shot; Hacker de Contratos applies sustained one-point defense overload. Runtime and deterministic bounty odds use the same triggers and arithmetic, while the odds cache includes every new class axis. Combat cards name each activation and the persistent report records counter damage, burst damage, evaded attacks, and total defense bypassed against the class selected when the encounter began. Interrupted saves accept only recognized evidence and canonical nonnegative totals. An initial generous Gunslinger tuning was rejected after saturating advanced campaign routes; scalable counter and overload trials were also rejected after breaking the Fenda's guarded 2/2/2 matchup matrix. The released signatures remain fixed tactical punctuation while primary attributes own scalable growth. Fenda anomaly data explicitly suppresses resisted mechanics and preserves Hacker/Breaker/Gunslinger ownership of volatile/containment/inertial pairs without branching on class IDs. The complete suite, including exhaustive phase persistence, passes in 165.68 seconds. The inspected reference-free ARM64 Android package is 31.92 MB, passes API 24+, synchronized version and stable-signature gates, and has SHA-256 `0aacfddbd345ad2ceebdf5bcc99a564b5af56cebb28b4b7a76a78427339392b0`. Version 0.29.0/code-73 introduces this combat-depth layer without changing save schema, species behavior, universal inventory, campaign targets, economy, or loot.

214. Campaign targets previously changed mostly through numerical pressure, so a player had little reason to read an ordinary enemy before comparing route percentages. Dustball remains a neutral tutorial; every later chapter now rotates improvised plating, reckless pressure, and an elusive signature before a mixed elite boss. These profiles visibly suppress opening damage, pierce mitigation, disrupt class aim, or amplify overload/counterplay without reading the player's class or scaling from the player. Briefing cards state the exact behavior and a class-neutral response, and combat preserves the active profile label. Four existing Fenda rewards gain universal tactical identities: counterweight servos retaliate every fourth round, the quickdraw bus adds a bounded perfect-shot burst, illegal adrenaline grants 1% evasion, and null synapse bypasses one defense. Class and equipment mechanics combine additively, participate in deterministic odds and complete-build scoring, and expose exact deltas in the arsenal. An initial floor-four result exceeded the guarded Fenda spread at 31 points; reducing only that enemy's health from 680 to 665 restored the 2/2/2 ownership matrix without changing rewards or campaign pressure. A twenty-career recommended-Breaker cohort and a reduced cross-matrix of all three classes under recommended and corporate-when-viable policies completed with 0% stalls and no sampled fight below 55%; their distinct route mixes show that profiles alter decisions rather than force one build. Both catalogs remain complete and symmetric. The complete suite, including exhaustive phase persistence, passes in 201.91 seconds. The published reference-free ARM64 Android package is 31.92 MB, passes API 24+, synchronized version and stable-signature gates, and has SHA-256 `c8f89ee949185b9abe8306d50b440ad789a363550a44d709e97132e31b5776d0`. Version 0.30.0/code-74 adds campaign build questions and universal equipment responses without changing save schema, species behavior, inventory shape, economy, loot, account boundaries, or target progression.

215. Physical Android testing exposed intermittent input latency on screens that requested uncached combat estimates. Each estimate intentionally executes 1,024 seeded combats, but the inner turn loop was also re-reading class definitions, attributes, and all nine equipment slots for every attack, defense, burst, evasion, and counter calculation. A 24-target cold benchmark measured 3,034,780 µs total, or 126,449 µs per estimate, before any interface construction; Arsenal can request several new estimates in one render. All build/target invariants and the 100-round counter cadence are now precomputed once outside the trial loop while preserving its seed, trial count, random-call order, arithmetic, and exact output. The same benchmark falls to 57,603 µs total, or 2,400 µs per estimate—about 52× faster—with identical checksum; cached calls remain under 2 ms for the full batch. Environment paintings now retain decoded textures after first use instead of forcing another load when returning to a context, and the vector space backdrop no longer redraws when the planet is unchanged. A permanent 12-target cold/cache budget test and standalone odds/UI construction benchmarks guard these paths. The complete suite, including exhaustive persistence and mobile layout, falls from 201.91 to 102.24 seconds and passes without changing combat outcomes, saves, progression, or presentation. The published 31.92 MB reference-free ARM64 APK passes API 24+, synchronized version and stable-signature gates with SHA-256 `b74ec93ce167d9554903571f41ad4c0ffb50e01b2d730078a3a0f932497f0906`. Version 0.30.1/code-75 is the Android responsiveness patch.

216. The remaining synchronous UI audit found that Career always instantiated planetary progression, eight milestones, the Fenda track, and all twenty wanted records even though a phone displays only one portion at a time. Progress and wanted records are now genuine session-local tabs that construct only their selected ledger and open with an independent top position. The default Career tree falls from 343 to 181 nodes and from 52.33 ms to 39.74 ms in the local synchronous benchmark; the isolated 225-node archive constructs in 17.57 ms. Arsenal now passes its already-computed readiness result into workshop recommendation instead of repeating the same lookup, reducing the measured warm render from roughly 38 ms to 36.38 ms. Invalid transient tab state returns safely to Progress, tab choice survives ordinary hub navigation, and account reset returns to the default. The complete 43-suite release gate, including the 1,659-case persistence matrix, translations, mobile constraints, and boot, passes in 111.45 seconds. The published 33,470,038-byte ARM64 APK passes API 24+, synchronized version and stable-signature gates with SHA-256 `5ecb74f771641a3295ece324ffb27840b22a536f340e086b1bb330999c38fc43`. Version 0.30.2/code-76 is the bounded Career construction patch.

217. The former five-chapter campaign contradicted the intended idle bounty fantasy: only one sequential target was actionable, captures unlocked the next warrant, worlds had to be completed in order, missions lasted seconds, and Hangar upgrades discounted the entire short timer. The normal board is now a deterministic three-offer interplanetary network. Safe, balanced, and dangerous roles produce immutable level-banded targets from the worlds discovered at hunter levels 1, 4, 8, 13, and 19. Equipment power is deliberately excluded from enemy generation, so upgrading after inspecting or accepting an offer remains meaningful. Every mission stores destination travel and pursuit separately; base routes grow from 30 seconds at Dustball to 8 minutes at Cassino, while transports reduce travel only by 10–40%. Accepting a warrant establishes its destination automatically. Galaxy, Hangar, Career, Arsenal readiness, market stock, Fenda unlock, offline patrol breadth, milestones, localization, save repair, and repeat actions now consume the same level-based rules. Legacy chapter state remains loadable for transaction-safe backward compatibility but the new board never creates a boss finale or sequential planet gate. A dedicated mission-rule suite guards three offers, world discovery, role pressure, deterministic rotation, immutable stats, canonical save repair, and travel-only discounts. The complete 44-suite gate, including the 1,659-case persistence matrix, translations, mobile constraints and boot, passes in 114.44 seconds. The published 31.93 MB ARM64 APK passes API 24+, synchronized version, stable-signature and reference-boundary checks with SHA-256 `015d3e6b104b8a5e3a3e9b51d1cd7647054be5182d2e6cf75382b8b335fe58ac`. Version 0.31.0/code-77 is the interplanetary bounty-network redesign; save schema is unchanged.

218. A post-redesign audit found three seams where the old chapter model still leaked into the new network. Career's mastery directive traveled manually and opened a canonical chapter target; the reward renderer still predicted sequential warrant unlocks after three captures; and the claim receipt called reputation growth a newly unlocked contract even though board availability now depends on level. Every production target handoff now creates a level-banded mission snapshot, network rewards announce the refreshed three-offer board while preserving exact-contract repetition for mastery, and rank receipts describe standing rather than fictional content unlocks. A second audit found a more serious long-horizon curve defect: quadratic mission XP would have taken a balanced hunter from level 1 to roughly level 667 in 200 wins. XP is now linear (`36 + 7 × mission level`, modified by offer value), remains below one level threshold through the guarded level-100 checkpoint, and produces approximately level 41 after 200 standard wins. Combat pressure now follows a legacy-informed linear curve while all three offer roles share the same snapshotted mission level, eliminating the opening 99%-to-1% cliff. A permanent audit tool samples the three classes, seven level checkpoints, twenty board cycles, recommended approaches, route durations and XP. Role copy now describes routine, priority and high-value warrants rather than promising a universal safety order that enemy profiles and class builds can legitimately alter. The complete 44-suite gate, including the 1,659-case persistence matrix, translations, mobile constraints and boot, passes in 106.35 seconds. The published 31.93 MB ARM64 APK passes API 24+, synchronized version, stable-signature and reference-boundary checks with SHA-256 `5bd94e051ba4f98af884541715a06081d0ebcc1b1589283ce3deaf2de311332e`. Version 0.31.1/code-78 is the network-boundary and long-horizon balance correction; save schema remains unchanged.

219. The three-offer network was mechanically sound but its first mobile presentation stacked three complete target dossiers, repeating portrait, copy, payout, odds, timing and action while forcing comparison through a long scroll. The reference server study confirms that its apparent single-quest focus is backed by three generated quest slots that refresh together after completion. Crooked Galaxy therefore preserves three real offers and one active contract while changing only information hierarchy. A hunter with no victories sees one guided first warrant and copy that introduces the network only after capture. Established hunters see all three offers simultaneously as touch-safe compact tickets containing target portrait, destination, payout and total duration; one selected ticket controls the single expanded dossier below. Switching tickets replaces rather than accumulates detail, and capture still refreshes all three deterministic snapshots. Real 450×800 capture keeps all choices, the complete selected target, its risk, timing and primary action visible together without needing three dossier-length scrolls. Portuguese and English remain symmetric. The complete 44-suite gate, including the 1,659-case persistence matrix, translations, mobile constraints and boot, passes in 102.07 seconds. The published 31.93 MB ARM64 APK passes API 24+, synchronized version, stable-signature and reference-boundary checks with SHA-256 `fbe26146a642e693381259c9f108b7c04ad28a842b7557bfc99dd8c677260117`. Version 0.31.2/code-79 is the compact mission-choice hierarchy; save schema, mission generation, balance, timing, economy and combat are unchanged.

220. A second pass treated the compact tickets as a decision surface rather than decoration. They previously omitted build odds, displayed raw credits while the expanded dossier included capture-streak payment, and used only a gold border for selection even though Dustball's destination accent is also gold. Every ticket now names the target and destination, pairs its routine/priority/high-value category with the same deterministic current-build odds as the dossier, shows the exact next streak-adjusted payment and transport-adjusted total time, and gives the selected choice a filled panel plus diamond marker. Controller selection survives the content-tree replacement and restores focus to the same ticket. Three cold estimates plus construction measure 24.84 ms locally; a normal cached ticket switch measures 8.11 ms. A new pacing gate simulates standard offers from a clean level-one hunter: Congelária appears at win 8 after 468 base wait seconds, Micélia at win 23/2,034 seconds, Ferro-Velho at win 46/5,638 seconds, and Cassino at win 77/12,810 seconds. These are guarded as broad discovery windows rather than declared final retention tuning; physical Android sessions must precede any timer or XP expansion. A separate compatibility suite proves that established all-planet progress, active destination, owned transport, immutable accepted snapshot, tamper repair, interrupted travel/pursuit/approach timing and abandonment all return safely to the three-offer board. Real 450×800 review, 125% text expansion and PT/EN parity pass. The expanded 46-suite gate, including the 1,659-case persistence matrix and clean boot, passes in 112.84 seconds. The published 31.93 MB ARM64 APK passes API 24+, synchronized version, stable-signature and reference-boundary checks with SHA-256 `55cb7a7e74246bb2d0c8c583908935bca4013bfb6a72114b2ddbee783bf00102`. Version 0.31.3/code-80 is the mission-comparison and compatibility hardening batch; save schema, mission values, combat, economy and transport bonuses are unchanged.

221. The intentionally provisional seconds-long routes were compared with the local reference implementation and the reference's documented public cadence before tuning. Crooked Galaxy now uses original base routes of 5/8/12/16/20 minutes for Dustball, Congelária, Micélia, Ferro-Velho, and Cassino. A transparent onboarding multiplier applies 40% of base travel at levels 1–3, 60% at levels 4–7, 80% at levels 8–12, and the complete route from level 13; pursuit remains fully additive. The Hangar ladder is now 10/20/30/50%, still discounts travel only, and therefore makes the final transport meaningful without modifying combat or incidents. The dossier explicitly names the active starter reduction, while Galaxy and Career show full base routes in readable minute format. A clean standard-contract simulation reaches Congelária at win 8/1,188 seconds, Micélia at win 23/5,388 seconds, Ferro-Velho at win 46/15,796 seconds, and Cassino at win 77/37,308 seconds. Accepted snapshots reconstruct from mission level alone; valid hunts already in progress preserve their persisted deadline across the update, while invalid timers safely rebuild with canonical duration. Real 450×800 captures keep all three tickets, the complete dossier, timing explanation, primary action, and dock visible. The complete 46-suite gate, including exhaustive persistence, translations, text resilience, mobile constraints, and clean boot, passes in 104.47 seconds. The published 31.93 MB ARM64 APK passes API 24+, synchronized version, stable-signature, and reference-boundary checks with SHA-256 `7adcb01ede08caa0784ae567c0961a1acce587149c1e5dd824074524b932e2a1`. Version 0.32.0/code-81 is the reference-informed mission-cadence release; save schema is unchanged.

222. The reference-paced routes exposed a contradictory incident boundary: reaching 45% opened a mandatory-looking decision and stopped local countdown updates, but returning after the original deadline could skip that event and enter combat. Hunt time is now one authoritative wall-clock interval across the live screen, an open incident, app backgrounding, save/reload, and ignored decisions. Incidents are explicitly optional opportunities in motion; ignoring one is neutral, tactical choices apply their stated effect, and detours extend the existing deadline by a visible 30, 45, or 60 seconds instead of restarting the route from the decision time. Expiration through any of those paths enters the same combat state and records timer provenance for the dossier. On Android, returning to a completed hunt produces a one-shot native toast and vibration through Godot's built-in `AndroidRuntime`; a notification while the application is fully terminated remains intentionally unclaimed because that requires dedicated Android receiver/plugin integration. Pure timing rules are extracted from the state aggregate and independently tested. A new level-1-to-100 economy audit keeps contract rewards, catch-up stock and refresh costs monotonic: a standard reward grows from 33 credits at level 1 to 13,532 at level 100 while stock grows from 157 to 7,681, and all four transports remain obtainable without AFK income. Buying each transport as soon as possible reaches the final 50% yacht at win 83/level 20 and a 200-win standard simulation ends near level 41 with 193,327 credits, so no speculative economy retune was made. A real 450x800 incident capture keeps all three decisions and abandonment reachable with the moving-route copy. The expanded 49-suite gate, including exhaustive persistence, translations, long-horizon economy, timing, Android feedback boundaries, mobile constraints and clean boot, passes in 108.31 seconds. The published 31.94 MB ARM64 APK passes API 24+, version `0.33.0`/code 82, signature, pack and reference-boundary checks with SHA-256 `b52af4a13db25637457bda0df59b7c9e69001a51b3949d7b001ca00afcca1450`. Version 0.33.0/code-82 is the authoritative asynchronous-hunt release; save schema is unchanged.

223. The Android performance/QoL pass separated occasional cold work from normal cached navigation instead of treating every screen rebuild as equivalent. The first local profile measured Arsenal at 59.10 ms cold, Market at 30.45 ms, Career at 35.50 ms, the three-offer board at 25.39 ms cold, and that same board at 8.24 ms warm. Combat estimates now use 768 rather than 1,024 deterministic trials: the permanent twelve-target cold probe falls from 29.08 ms to 23.85 ms (about 18%), while class identity, tactical profiles, complete-career allocation, balance windows, and the 55% recommendation gate remain intact. The clearer cold/warm UI benchmark records Arsenal at 56.18/10.30 ms, Market at 35.86/11.57 ms, and the network board at 23.39/9.14 ms on this machine; one-time construction remains visible, but ordinary revisits fit an interaction frame. Multi-minute hunt polling falls from 10 Hz to 4 Hz, a 60% wakeup reduction, while one absolute timestamp still owns completion; countdown translation/text changes occur only when the displayed second changes, and the live pursuit stage now advances instead of retaining its initial percentage. Identical environment context/planet requests are no-ops, and a stored market purchase preserves still-valid odds cache entries. A separate transactional-save benchmark measures 10.42/14.19 ms median/p95 with no inventory, 12.19/13.49 ms with 30 items, and 14.53/16.48 ms with 120 items; atomic primary-plus-latest-backup semantics remain unchanged pending physical-storage evidence. QoL closes the incident promise literally: a visible `IGNORAR · CONTINUAR ROTA` action and Android Back both dismiss the optional event without credits, result, or deadline change. Market, Hangar, and Inventory remember scroll through ordinary transaction rerenders, while refresh/filter/sort/page replacement intentionally returns to the first item. Real 450x800 Portuguese and English captures keep all three incident choices plus Ignore/Abandon visible on one screen. The complete 49-suite gate, including exhaustive persistence, translations, expanded text, lifecycle, mobile scrolling and clean boot, passes in 105.32 seconds. The published 31.95 MB ARM64 APK passes API 24+, version `0.33.1`/code 83, signature, pack and reference-boundary checks with SHA-256 `8108bf1d0a415f4a8cce41c7f1fb1507bb0b91be4617ae45b0715aedcaa1550f`. Version 0.33.1/code-83 is the Android performance and quality-of-life patch; save schema, economy, mission cadence and combat rules are unchanged.

224. Decomposing the remaining first-Arsenal delay separated a 13–14 ms portrait-background decode, roughly 21 ms of cold field analysis, and about 20 ms of Control construction instead of moving all work into startup. While the player is idle on the three-offer board, one reusable deterministic estimate is now warmed per timer frame: three contract approaches, Power and integrity projections, and every currently affordable workshop action. The timer is single and restartable, so repeated state-driven renders cancel stale work without duplicate signal connections; generation, phase, focus suspension, and onboarding guards prevent background work from escaping its valid screen. The exact same projection builder now owns both warmup and the visible best-investment recommendation, and regression coverage proves that warming neither mutates player state nor creates a different result. After estimates complete, only the shared workshop painting is requested through Godot's threaded resource loader; other contexts remain demand-loaded and the worker does not use subthreads on modest Android CPUs. A permanent first-visit profiler measures the representative mature profile at approximately 27.24 ms after fifteen idle frames, versus the preceding 53.95 ms fully cold visit, while the cold analysis remains independently visible at 22.07 ms for regression diagnosis. Real 450x800 Arsenal and mission-board captures retain the complete equipment/status hierarchy and touch-safe dock. The final complete 49-suite gate, including the 1,659-case persistence matrix, translations, lifecycle, focus, mobile layout and clean boot, passes in 117.28 seconds without changing combat, economy, save schema, mission cadence, or visible behavior. The published 31.96 MB ARM64 APK passes API 24+, version `0.33.2`/code 84, signature, pack and reference-boundary checks with SHA-256 `6f24dc7326e93f05b0ad697d7939cb651274f7061585349b10e6ae5cbddc10b2`. Version 0.33.2/code-84 is the bounded first-visit responsiveness patch.

225. The full visual audit found that navigation and reward hierarchy already read as a real game, but procedural sticker-like characters remained disconnected from the painterly frontier backgrounds and technical navy UI. A concise production bible now defines three compatible layers—illustrated fantasy, frontier setting and compact interface—plus silhouette, palette, composition, authorship and mobile-memory contracts. The first character vertical slice is an independently generated, species-neutral Orbit Gunslinger with hat, sealed cyan visor, patched navy coat, mustard scarf and downward ray pistol. No source or style-reference image was supplied for its original generation; the PNG has verified genuine alpha and a 1024-pixel import cap. Class choice and the class dossier load the raster only when that class is focused, reuse Godot's resource cache, and present a deliberate upper-body crop that keeps the identifying shapes legible at 450×800. Personal species, appearance and equipped-item identity remain owned by the modular hunter portrait; unillustrated classes fail closed to their original vector emblems. A cleaner generated experiment was rejected because its checkerboard was baked into opaque RGB, demonstrating that visual polish cannot bypass the runtime alpha contract. PT/EN onboarding, 125% text expansion, mobile touch/scroll constraints, resource resolution and the complete 49-suite gate pass in 105.96 seconds. The published 34,335,039-byte ARM64 APK passes API 24+, synchronized version, stable-signature, pack and reference-boundary checks with SHA-256 `1da6c99909a3d98d14a46401fac4078c46f1368f23e3e081d758200e75bcf992`. Version 0.34.0/code-85 begins the character-led visual language without changing saves, balance, mission cadence, class mechanics, species behavior or equipment.

226. The approved Orbit Gunslinger vertical slice now expands into a complete illustrated initial-class roster. Warrant Breaker uses a broad low-center silhouette, burgundy scrap armor, coral sealed visor, reinforced gauntlets and a downward magnetic breach hammer; Contract Hacker uses a narrow hooded silhouette, fully sealed charcoal suit, teal armor, cyan decoder and probe, and restrained acidic-lime cabling. Both were generated as original production assets without third-party reference images, and their four corner pixels verify alpha zero in 32-bit ARGB sources. A first Hacker candidate was rejected because exposed human skin contradicted eight cosmetic species; an attempted isolated sleeve edit was also rejected because the generator baked a checkerboard into opaque RGB. Only the newly generated fully sealed, genuinely transparent replacement enters runtime. Every initial class now resolves through the same cached, 1024-pixel-capped upper-body presentation while future missing art retains the vector fallback. Permanent capture coverage records Breaker, Gunslinger and Hacker under identical Portuguese dossier conditions; real 450×800 comparison preserves distinct broad/tall/narrow silhouettes, complete mechanics text, the confirmation action and the persistent dock. The complete 49-suite gate, including exhaustive persistence, translations, expanded text, mobile constraints and clean boot, passes in 114.84 seconds. The published 35,822,379-byte ARM64 package passes API 24+, version 0.35.0/code 86, stable-signature, pack and reference-boundary checks with SHA-256 `37f15275aa175646f452a9c2218c42b1c37842a46ff6d12bb82489ea6d657d5b`.

227. Physical review rejected the entire generated class trio despite its technical validation. A direct comparison with the local registration, tavern, and monster references identified the missing visual grammar: readable faces and attitudes, exaggerated uneven anatomy, large silhouette props, thick confident contours, saturated broad color groups, asymmetry, humor, and low-noise phone readability. The generated trio instead used anonymous sealed tactical helmets, near-realistic heroic proportions, military symmetry, dense scratched materials, dark palettes, and serious concept-art lighting. Treating generic labels such as “comic sci-fi” and “cel shading” as adequate direction—and forcing species neutrality by hiding every face—allowed a polished but categorically wrong style to pass. Version 0.35.1 removes all three raster files and their runtime loader, restores the intentional vector class emblems, and records the rejected assets only in recoverable Git history. A repository-level mandatory gate now requires three inspected references, one current UI context, a written style delta, exactly one off-runtime draft, explicit user approval of that exact image, a hard-rejection pass, and a 450×800 integration capture before generated art may enter assets, tests, commits, or APKs. Broad autonomous permission cannot bypass that gate. The complete 49-suite authority passes in 107.69 seconds. The corrected 33,507,399-byte ARM64 APK passes API 24+, version `0.35.1`/code 87, stable-signature, pack, and reference-boundary checks with published SHA-256 `37d0eeeb43443f7f4c5dc066899f861cc9dfd6fb809897f1c08e44d6c4a237d2`.

228. The complete interface-identity audit replaced the remaining mixture of flat prototype cards, transparent neon controls, and unrelated large semantic panels with one enforced hierarchy. The already approved original 660×124 navy/steel/brass/coral/cyan frame now marks only focal dossiers: mandatory onboarding previews, selected mandate, briefing target, hunter/class identity, universal Arsenal sheet, active Hangar transport, hunt incident, victory, normal/Rift reward, chapter conclusion, Career summary, and locked/current/completed Rift states. Combat retains its bespoke tactical visualization behind a two-pixel brass material edge. Every supporting panel receives a restrained steel boundary, outlined actions sit on navy rather than floating over painted environments, and the five-cell dock uses aged brass/steel structure with semantic color confined to icons and active edges. No new raster was generated. Renderer and integration suites now assert the exact focal `StyleBoxTexture` contract and the arena exception. Identical support, action, focal, and dock `StyleBox` resources are shared for the UI host lifetime; the selected Galaxy card duplicates before mutation. A representative first-Arsenal visit improves from approximately 27.1 ms to 24.4 ms and cold Market construction from approximately 36.1 ms to 29.5–31.0 ms locally, without weakening persistence or odds caches. The capture pipeline removes only the obsolete version-0.11 Arsenal-settings artifact and now leaves 99 current UI captures plus one portrait diagnostic, all exactly 450×800. The complete suite, including the 1,659-case persistence matrix, PT/EN, 125% text expansion, lifecycle, focus, mobile layout and clean boot, passes in 102.94 seconds. A local 33,573,154-byte (32.02 MB) ARM64 inspection APK with SHA-256 `e989f5af62a5ccc8a06e5e538b35e5c0bf851ad81faf56361b6070593f1bc186` passes package `com.crookedgalaxy.game`, version `0.35.1`/code 87, API 24+, stable test signature, required-art pack inspection and reference exclusion; it was not copied to the shared test APK or published.

229. The Android readability completion pass migrates login, onboarding, mission, incident, combat, reward, hunter, class, career, market, hangar, Arsenal, Fenda and transient-state typography to one semantic token system. Every direct action now meets the 72-logical-pixel touch floor, dense workshop and pagination controls use physical Android-safe targets, and PT/EN labels use wrapping or bounded ellipsis rather than shrinking. The contract header keeps level and power inside its hunter card, while combat no longer lets long event, pressure or kit lines widen the entire mobile shell; build and kit evidence remain complete through controlled wrapping. Geometry tests now bind the combat header, arena, report and speed action to the safe width, and visual review covers the complete 450×800 capture set. The full 49-suite gate plus clean boot passes in 110.01 seconds without changing saves, economy, mission cadence, combat rules or progression. Version `0.36.0`/code 88 is the Android interface-readability release and remains update-compatible with the established direct-test signature.
230. The asynchronous-hunt usability pass removes two physical-Android blockers. The mandatory species roster now keeps a visible 24-pixel scroll rail and fixed 72-pixel touch paging controls above its confirmation action, so all eight cosmetic origins remain reachable even when drag scrolling is unreliable. Once a route is committed, the primary dock remains available and marks Mandados as an active `CAÇADA`/`HUNT`; Arsenal, Hunter, Galaxy and Menu can be opened without changing the persisted hunt deadline, Android Back minimizes rather than abandons the contract, and the active-contract destination restores the live progress surface. Optional incidents can likewise be minimized and revisited while their wall-clock mission remains pending. The existing absolute `hunt_ends_at` persistence continues to advance through app suspension and a complete process restart. PT/EN labels and automated coverage were extended for touch paging, background navigation, incident minimization and deadline invariance. The fast 48-suite gate plus clean boot passes without save-schema, balance, economy or combat changes. Version `0.36.1`/code 89 is the compatible Android interaction hotfix.
231. Physical-device review corrected the species interaction itself instead of retaining paging buttons as a workaround. The class/species choice trees now propagate pointer motion through nested cards and buttons, while a dedicated touch scroller owns the full `press → vertical drag → release` gesture. It claims the interaction after an eight-pixel deadzone, moves continuously with the finger and consumes the release so swiping over a choice cannot accidentally select it. Fixed `SUBIR`/`DESCER` controls and their translations were removed; direct taps remain unchanged and the visible rail remains a secondary position indicator. Automated onboarding coverage injects actual `InputEventScreenTouch` and `InputEventScreenDrag` events through the viewport over a species card and requires the roster position to change. Version `0.36.2`/code 90 supersedes the paging workaround while retaining the background-hunt changes and save compatibility from 0.36.1.
232. The physical-Android text-margin pass increases the minimum horizontal safe gutter from 30 to 42 logical pixels (18.75 to 26.25 pixels at the 450×800 target) while preserving larger device-reported cutout insets. The safe shell now clips as a final containment guard, and dense Arsenal inventory/workshop names, item statistics, origins, investment summaries and traits wrap across bounded lines rather than extending or disappearing at the right edge. Fenda reward names receive the same treatment. All 103 current UI states were recaptured at 450×800 for visual review. Text resilience now audits every visible Label across content, header and navigation against a narrower simulated safe area, repeats after 125% font expansion, and still requires unwrapped text to fit its assigned control. The complete fast 48-suite gate plus clean boot passes. Version `0.36.3`/code 91 is a save-compatible Android layout correction with no gameplay or economy changes.

233. The year-one progression audit replaced the linear XP requirement with the centralized quadratic contract `80 + 45 × (level − 1) + round(0.80 × (level − 1)²)`, represented as the exact `4/5` coefficient. Mission rewards, fuel, refill prices, transport timing and offer generation remain unchanged. Exact fuel-limited simulations now place the free standard profile at level 165 after 2,388 hunts and the current maximum-intensity profile at level 323 after 7,569 hunts; that extreme reaches level 300 on day 318 and level 320 on day 358. The production ceiling therefore expands from 33 planets/132 targets through level 300 to 35 planets/140 targets through level 320, while explicitly retaining only six planets/24 targets as implemented. The executable audit now calls the production XP rule for its active scenario instead of duplicating the former curve. Discovery, long-horizon economy and exact XP checkpoints were recalibrated, and unchanged save schema 21 preserves existing level and partial XP without retroactive progression. The complete 54-suite gate, including the 1,659-case persistence matrix and clean boot, passes in 143.42 seconds. The published 32.13 MB ARM64 APK passes API 24+, version `0.49.0`/code 107, stable signature, required-art pack and reference-boundary checks with SHA-256 `51c03b99ef0fbef0400526d1a6f257b258b3d996babdca86a6ed7752158e682c`.

234. The Fenda Clandestina changes from a one-time twelve-floor ladder into a keyed daily reality system. The first key is derived permanently at level 8; completing its twelve enemies and reaching level 100 makes a second key discoverable through ordinary hunts with a five-hunt bad-luck ceiling. Each reality persists independent progress and uses canonical composite encounter/reward IDs, while a vertical selector appears only when more than one key is owned. Exactly one global entry is available per UTC day across all realities, consumed atomically on combat start and never restored by defeat, navigation or restart. It uses no fuel and cannot be bought, repeated, skipped or rerolled with Warp Chips. The pre-entry dossier now shows only the current enemy, odds and anomaly; all reward identity, slot, rarity, Credits and XP remain sealed until victory. The second reality's twelve placeholders are calibrated against all three class builds at projected levels 100–155, producing a 47–82% envelope rather than an opening quartet at 99% followed by a terminal 1% wall. Schema 22 migrates legacy `challenge_floor` without false recovery warnings, and PT/EN translations preserve the base authored identity for composite content. Real 450×800 review confirms one-reality simplicity and two-reality selection above the fixed daily action. The complete 54-suite gate, including the 1,659-case persistence matrix and clean boot, passes in 146.66 seconds. The validated 33,705,811-byte ARM64 APK passes API 24+, version `0.50.0`/code 108, stable signature, required-art pack and reference-boundary checks with SHA-256 `988f7626435d61d9b3e3f45079372bc01b7cec503702b4ddd44f2dbc4b1283fb`.

235. The repeatable year-one production pipeline now delivers its second post-slice world: `Arquivo Abissal N-9` unlocks at level 40 and remains in the permanent three-offer network with every previously discovered planet. The oceanic habitat adds four original target identities, twelve named attacks, two three-way hunt incidents, eight weapon/armor families and four universal implant families, all with exact PT/EN catalog parity. Distinct code-drawn abyssal motifs cover the Galaxy icon, mission space, combat arena, environment tint and equipment/loadout accents without adding texture residency or unapproved raster art. The bounded normal series catalog grows from 440 to 500 combinations; gadget and relic drops remain Fenda-only. A fresh pacing audit measures the free standard profile at level 153 and the maximum 160-fuel/cheapest-route profile at level 302, with level 300 on day 360, so the existing level-320 production ceiling remains a deliberate buffer. Rotation guards now audit a complete seven-world epoch, mobile tests own an isolated International 1 session, all 56 repository suites pass after targeted correction, and the fast gate including clean boot completes in 57.73 seconds. Version `0.53.0`/code 111 is save-schema-compatible with version 23.

236. A visual-coverage audit found that the eight targets added after Cassino Quasar still resolved through `ProceduralPortrait`'s generic hunter branch despite owning distinct content identities. Aerópolis now has an authored atmospheric family: parcel courier, pressure duchess, turbine engineer and sentient storm bank. Arquivo Abissal now has an authored oceanic family: electric eel courier, coral landlady, eight-armed notary and archival leviathan. Each uses an individual silhouette and recognizable prop language within the established high-contrast code-drawn cast, with no new runtime raster or texture residency. The 450×800 cast sheet confirms all 29 hunter/target portraits remain readable at archive size. Runtime exposes a stable visual-identity contract and tests require every canonical target to avoid fallback, so a future content pack cannot silently ship as hunter clones. The capture grid derives its rows from the live cast instead of a fixed 21-character layout. Version `0.53.1`/code 112 is a save-compatible presentation correction.

237. The modular pipeline is proven by the first post-migration world. `Verdântia Patenteada` unlocks at level 50 with a 32-minute base route, a bioluminescent patented-jungle identity, four warrants, twelve attacks, two three-way incidents, eight weapon/suit families and four boot families. Exact PT/EN parity raises the active catalog to eight planets, 32 targets, 16 incidents and 560 collectible series. An initial extrapolation stalled 30% of balanced Enforcer careers and was rejected; the calibrated pack has no sub-55% Verdântia attempt in the complete class/strategy campaign sample, while preserving earlier campaign behavior. The fuel audit now projects the free standard profile at level 144 and maximum 160-fuel/cheapest-route play at level 284 after one year. Codex created no visual asset: the planet and four targets carry explicit `pending_user_asset` contracts, deterministic catalog paths and tested generic fallbacks. The full gate, including the 1,659-case persistence matrix and clean boot, passes in 194.80 seconds without a save-schema change.

238. `Caldeira de Garantia` extends the modular mission network to level 60 with a 36-minute base route and a volcanic-industrial habitat where eruptions clock in, magma concessions are licensed and even geological disasters carry warranties. The pack adds four warrants, twelve attacks, two three-way incidents, eight weapon/armor families and four glove families with exact PT/EN parity, raising the active catalog to nine planets, 36 targets, 18 incidents and 620 collectible series. The first balance pass made the third warrant a class-sensitive wall and was rejected; the isolated correction to `Magnata Obsidiana` leaves every Caldeira class/strategy attempt at or above the 55% campaign guard, while preserving the boss as the strongest test. The updated fuel audit projects the free standard profile at level 137 and the maximum 160-fuel/cheapest-route profile at level 271 after one year; the current nine-world discovery route reaches level 60 after approximately 477 hunts and 124.7 base-route hours. Codex created no visual asset: the planet and all four targets declare `pending_user_asset`, deterministic catalog paths and tested generic fallbacks. The fast gate passes in 64.72 seconds and the complete gate, including the 1,659-case persistence matrix, PT/EN completeness, Android layout guards and clean boot, passes in 216.58 seconds without a save-schema change.

239. The repository-organization audit separates authority from history and generated output from source. `Notes/README.md` now ranks `AGENTS.md`, executable behavior, active contracts, the technical README and dated reports; every current Notes file is classified. The obsolete autonomy manual that contradicted the visual-authorship boundary and the orphan UID for a removed placeholder loader are deleted. Seven dated audits and blueprints are explicitly historical instead of competing with current contracts, and references to retained QA binaries/captures now describe reproducible evidence rather than files that should live in the project. Approximately 322 MB of caches, APKs, Windows/Android exports, logs, captures, rejected drafts and thirteen obsolete internal-reference payloads were moved to the recoverable sibling quarantine `Crooked Galaxy Cleanup Quarantine 2026-08-28`; the 2 GB local study library and all production assets remain untouched. A new repository-hygiene gate rejects misplaced APKs, legacy payloads, the obsolete instruction file, tracked build output and orphan or missing Godot UIDs. Testing from an empty `.godot` directory exposed that the old gate depended on a previously populated global-class cache; `check_project.ps1` now performs a one-time headless import for clean checkouts and the fifteen UID companions discovered by that scan are retained. The cold fast gate passes in 67.30 seconds and the complete gate, including the 1,659-case persistence matrix and clean boot, passes in 218.17 seconds. No gameplay, save, economy, localization or visual asset changed.

240. The instruction-contract optimization removes the remaining ambiguity between current behavior, planned systems and historical reports. All thirteen active design and production documents now declare their authority near the top and are classified from one canonical `Notes/README.md` index. The visual boundary is absolute and consistent across `AGENTS.md`, asset intake, provenance, placeholder, direction, character brief and UI inventory documents: Codex does not generate or modify project artwork; it may specify requirements, preserve code-native fallbacks, review an externally supplied candidate and integrate that exact file only when explicitly requested. Product terminology is normalized around `species` as the technical identifier, Raça as the Portuguese product label, Fichas de Dobra/Warp Chips as the premium currency, Mandado as an offer and Caçada as an accepted mission. Current facts are separated from proposals: nine worlds currently extend routes to 36 minutes, the weekly free profile is approximately 33–34 standard hunts rather than 45, the Market and fuel loop are active while paid Jump Cells and Arena remain planned, transports already cap at 50%, the live rarity set has three tiers, target-art requirements cover 36 identities, and the Arsenal exposes two loadouts. The root README is reduced from approximately 54 KB/401 lines to 7.9 KB/147 lines and now serves as a technical entry point instead of a competing implementation chronicle. A new executable documentation gate checks classification, status declarations, links, vocabulary, visual authorship and the absence of known stale claims on every project validation run. The fast gate passes in 63.11 seconds; the complete run produced 59 clean logs in approximately 240.2 seconds, including the 1,659-case persistence matrix and clean boot. No gameplay, save, balance, economy, localization or visual asset changed.

241. The deep project audit finds a stable, heavily guarded playable foundation and two different scale risks. Runtime contains approximately 17,285 GDScript lines against 7,916 test lines; persistence remains around 9–14 ms through a 120-item inventory, hot UI navigation generally remains inside 6–18 ms, and the slowest observed cold views are Arsenal/collection near 45 ms. `game_state.gd` and `main.gd` are still 2,831 and 2,703-line ownership concentrations respectively, but their phase, save, lifecycle and mobile boundaries have broad deterministic coverage, so an unmotivated rewrite would currently add more risk than value. The material production gap remains larger: the year-one contract requires 35 worlds/140 targets while the prior build supplied nine/36. Version `0.54.0`/code 113 adds `Condomínio Lunar 7` at level 70 with a 40-minute route, four residential-moon warrants, twelve attacks, two three-choice incidents, eight weapon/armor families and four helmet families in exact PT/EN parity. The catalog reaches ten worlds, 40 targets, 20 incidents and 680 collectible series; all twelve recent target portraits plus the three recent world packs remain explicit `pending_user_asset` deliveries with deterministic fallbacks, and Codex created no visual asset. Fuel-limited year projections move to level 132 for free standard play and level 258 for 160-fuel cheapest-route play; current final discovery occurs around 604 standard hunts and 171.6 base-route hours. Multi-build campaign simulation finds no sub-55% attempt in the new world, with its sampled final-boss minima at 71–92%. The same audit exposes a pre-existing systemic priority: from Aerópolis onward, route multipliers frequently collapse standard and dangerous approaches to 1%, leaving the safe route as the only rational late-game choice; Hacker careers are especially fragile in the middle Arquivo Abissal tiers. That issue must be recalibrated and guarded before adding level 80. The Fenda auditor was also misleading because it measured all first-reality enemies at unlock level 8; it now reuses the tested levels 8–90 checkpoints and reports the real 44–90% envelope, while the second reality remains 47–82%. Visual readiness is 6/241 supplied assets, leaving 235 explicitly external deliverables. The fast gate passes in 64.94 seconds and the complete 59-log gate, including the 1,659-case persistence matrix and clean boot, passes in 240.98 seconds without save-schema changes.

242. Version `0.54.1`/code 114 resolves the late-game approach collapse at the active mission-network boundary. The prior fast and corporate routes multiplied attack, defense and resistance simultaneously and accumulated resistance by planet, so the compounded combat cost reached 1% even when the quiet route remained near-certain. New contracts now blend those three dimensions from levels 20–30 into an ordered bounded envelope derived only from the immutable mission level or authored world unlock level; current equipment is never read. Early and mid-campaign calibration through Cassino remains unchanged. A shared representative-build factory and new executable cohort suite exercise all three classes, levels 30/40/50/60/70, twenty complete board cycles at each checkpoint and every current late-world unlock contract. Every sampled offer retains at least two routes at or above the 55% recommendation threshold, preserves `quiet ≥ hot ≥ corporate` pressure ordering and includes a visibly separated decision per class/checkpoint. At level 70 the most demanding sampled Hacker offer changes from an ineffective base 14% with only one marginal recovery route to an ordered `96% / 72% / 10%`: two rational choices plus a clearly speculative corporate option, rather than three equivalents. The audit tool now prints all route odds and reuses the same representative factory. Accepted pre-0.54.1 contracts are reconstructed with their legacy approach definition, so an update cannot rewrite combat, timing or payment already committed in a save; new contracts carry an explicit balance version. The fast 60-suite gate passes in 67.06 seconds and the complete gate, including all 1,659 persistence combinations and clean boot, passes in 224.95 seconds without a save-schema or visual-asset change.

243. Version `0.55.0`/code 115 extends the modular year-one pipeline to level 80 with `Necrópole Solar Umbral`, a funeral station around a dead sun. The permanent mission network now contains eleven worlds, 44 targets and 22 three-choice incidents; this pack adds four named warrants, twelve attacks, eight weapon/armor families and four universal rig families in exact PT/EN parity. Its 44-minute base route moves current final discovery to 742 standard hunts and 226.2 route hours. Fuel-limited year projections settle at level 127 for free standard play and level 247 for 160-fuel cheapest-route play, with level 100 reached on days 223 and 68 respectively; the level-320 production ceiling remains a deliberate buffer. The late-approach cohort now includes level 80 and retains at least two rational routes above 55% for every sampled class and board cycle. The normal collection grows to 740 series without altering its bounded 37-chip lifetime reward ladder; across 100 deterministic 5,000-contract careers, the median catalog reaches 732 series and only one career completes all 740, confirming that the new volume remains a long-tail collection rather than a short completion task. The new world and all four portraits are explicit `pending_user_asset` deliveries with existing deterministic fallbacks. Visual readiness is therefore 6/248 supplied assets, leaving 242 external deliverables; Codex created no visual asset. The fast gate passes in 66.72 seconds and the complete gate, including all 1,659 persistence combinations and clean boot, passes in 274.43 seconds without a save-schema change.

244. Version `0.56.0`/code 116 extends the modular year-one pipeline to level 90 with `Central de Tempestades 24h`, a meteorological support center suspended above a gas giant. The permanent network now contains twelve worlds, 48 targets and 24 three-choice incidents; the pack adds four customer-support storm warrants, twelve named attacks, eight weapon/armor families and four universal implant families in exact PT/EN parity. Its 48-minute route moves current final discovery to 892 standard hunts and 291.5 route hours. Fuel-limited projections settle at level 124 for free standard play and level 237 for 160-fuel cheapest-route play, with level 100 reached on days 229 and 68 respectively. The late-approach cohort now includes level 90 and preserves two rational routes at or above 55% for every sampled class, board cycle and current late-world unlock. The normal collection grows to 800 series without changing the bounded 37-chip lifetime reward ladder; after 5,000 contracts, 100 deterministic careers discover a median 789 series and none completes all 800, retaining collection as a genuine long-tail goal. The new habitat, arena, icon and four portraits are explicit `pending_user_asset` deliveries with existing fallbacks. Visual readiness becomes 6/255 supplied assets, leaving 249 external deliverables; Codex created no visual asset. The fast gate passes in 68.63 seconds and the complete gate, including all 1,659 persistence combinations and clean boot, passes in 278.00 seconds without a save-schema change.

245. Version `0.57.0`/code 117 extends the modular year-one pipeline to level 100 with `Museu do Amanhã Obsoleto`, a hollow-asteroid museum for futures that never happened. The permanent network now contains thirteen worlds, 52 targets and 26 three-choice incidents; the pack adds four retrofuturist museum warrants, twelve named attacks, eight weapon/armor families and four universal boot families in exact PT/EN parity. Its 52-minute route moves current final discovery to 1,054 standard hunts and 367.4 route hours. Fuel-limited projections settle at level 122 for free standard play and level 229 for 160-fuel cheapest-route play, with level 100 still reached on days 229 and 68 respectively. The late-approach cohort now includes level 100 and preserves two rational routes at or above 55% for every sampled class, board cycle and current late-world unlock. The normal collection grows to 860 series without changing the bounded 37-chip lifetime reward ladder; after 5,000 contracts, 100 deterministic careers discover a median 846 series and none completes all 860, retaining the intended long tail. The new habitat, arena, icon and four portraits are explicit `pending_user_asset` deliveries with existing fallbacks. Visual readiness becomes 6/262 supplied assets, leaving 256 external deliverables; Codex created no visual asset. The fast gate passes in 69.59 seconds and the complete gate, including all 1,659 persistence combinations and clean boot, passes in 307.91 seconds without a save-schema change.

246. Version `0.58.0`/code 118 extends the modular year-one pipeline to level 110 with `Biblioteca do Silêncio Taxado`, a crystal moon whose kilometer-long shelves bottle forbidden sounds and license every echo. The permanent network now contains fourteen worlds, 56 targets and 28 three-choice incidents; the pack adds four acoustic-library warrants, twelve named attacks, eight weapon/armor families and four universal glove families in exact PT/EN parity. Its 56-minute route moves current final discovery to 1,227 standard hunts and 453.9 route hours. Fuel-limited projections settle at level 121 for free standard play and level 222 for 160-fuel cheapest-route play, with level 100 still reached on days 229 and 68 respectively. The late-approach cohort now includes level 110 and preserves two rational routes at or above 55% for every sampled class, board cycle and current late-world unlock. The normal collection grows to 920 series without changing the bounded 37-chip lifetime reward ladder; after 5,000 contracts, 100 deterministic careers discover a median 903 series and none completes all 920. The new habitat, arena, icon and four portraits are explicit `pending_user_asset` deliveries with deterministic fallbacks. Visual readiness becomes 6/269 supplied assets, leaving 263 external deliverables; Codex created no visual asset. The fast gate passes in 70.90 seconds and the complete gate, including all 1,659 persistence combinations and clean boot, passes in 311.87 seconds without a save-schema change.

247. Version `0.59.0`/code 119 extends the modular year-one pipeline to level 120 with `Resort do Horizonte de Eventos`, a luxury archipelago orbiting a black hole where every delay is sold as premium time dilation. The permanent network now contains fifteen worlds, 60 targets and 30 three-choice incidents; the pack adds four relativistic-tourism warrants, twelve named attacks, eight weapon/armor families and four universal helmet families in exact PT/EN parity. Its 60-minute route moves current final discovery to 1,412 standard hunts and 553.8 route hours. Fuel-limited projections settle at level 121 for free standard play and level 218 for 160-fuel cheapest-route play, with level 100 still reached on days 229 and 68 respectively. The late-approach cohort now includes level 120 and preserves two rational routes at or above 55% for every sampled class, board cycle and current late-world unlock. Expanding to sixty target identities exposed that the prior eight-pass mature-board test window no longer covered every tier; its non-gameplay audit window now spans sixteen complete catalog passes and again proves balanced planet exposure, no three-card repetition and every target identity. The normal collection grows to 980 series without changing the bounded 37-chip lifetime reward ladder; after 5,000 contracts, 100 deterministic careers discover a median 958 series and none completes all 980. The new habitat, arena, icon and four portraits remain explicit `pending_user_asset` deliveries with deterministic fallbacks. Visual readiness becomes 6/276 supplied assets, leaving 270 external deliverables; Codex created no visual asset. The fast gate passes in 77.70 seconds and the complete gate, including all 1,659 persistence combinations and clean boot, passes in 323.75 seconds without a save-schema change.

248. Version `0.60.0`/code 120 extends the modular year-one pipeline to level 130 with `Tribunal de Clones Não Autorizados`, a courthouse planet of duplication laboratories, mirrored hearing rooms and disputed biological patents. The permanent network now contains sixteen worlds, 64 targets and 32 three-choice incidents; the pack adds four clone-court warrants, twelve named attacks, eight weapon/armor families and four universal rig families in exact PT/EN parity. Its 64-minute route moves current final discovery to 1,609 standard hunts and 665.8 route hours. Fuel-limited projections settle at level 121 for free standard play and level 214 for 160-fuel cheapest-route play, with level 100 still reached on days 229 and 68 respectively. The late-approach cohort includes level 130 and preserves at least two rational routes at or above 55% for every sampled class, board cycle and current late-world unlock. The normal collection grows to 1,040 series without changing its bounded 37-chip lifetime reward ladder; after 5,000 contracts, 100 deterministic careers discover a median 1,015 series and none completes all 1,040. The new habitat, arena, icon and four portraits remain explicit `pending_user_asset` deliveries with deterministic fallbacks. Visual readiness becomes 6/283 supplied assets, leaving 277 external deliverables; Codex created no visual asset. The fast gate passes in 75.15 seconds and the complete gate, including all 1,659 persistence combinations and clean boot, passes in 319.34 seconds without a save-schema change.

249. Version `0.60.1`/code 121 removes the dominant validation bottleneck without reducing persistence authority. The equipment matrix had silently grown to 7,776 semantic combinations—27 traits across nine universal slots, three rarities, sixteen planets, three calibration histories and six integrity histories—but still performed the identical JSON write, atomic rename, backup copy and full load for every combination. That made one suite consume 245.95 of the complete gate's 319.34 seconds and left current audit notes reporting an obsolete 1,659-case size. The revised suite validates item safety, sanitization and exact payload identity exhaustively for all 7,776 combinations, then rotates 27 representative durable round-trips across every trait and slot, all rarities, both planet boundaries and both upgrade boundaries. Incident choices and contract approaches retain their complete file round-trips, for 132 durable transactions in total; dedicated save-failure, backup, corruption, phase, AFK and migration suites remain unchanged. The optimized matrix passes in 4.45 seconds, the fast gate now includes it instead of omitting it and passes in 74.99 seconds, and the complete gate passes in 73.42 seconds—a 77% release-validation reduction with stronger routine coverage. No gameplay, save schema, content, balance, localization or visual asset changed.

250. Version `0.61.0`/code 122 extends the modular year-one pipeline to level 140 with `Mosteiro da Gravidade Reversa`, a hollow asteroid where ceiling cloisters, ascending bells and spiritual mass accounting make orientation part of the joke. The permanent network now contains seventeen worlds, 68 targets and 34 three-choice incidents; the pack adds four inverted-monastery warrants, twelve named attacks, eight weapon/armor families and four universal implant families in exact PT/EN parity. Its 68-minute route moves current final discovery to 1,817 standard hunts and 800.2 route hours, placing the five-hunt reference unlock on day 364. That boundary exposed an honest discoverability flaw: the global random rotation could unlock the world without displaying it before year end. A bounded spotlight now gives the newest unacknowledged world exactly one card, rotating through all three pressure roles, during the first six standard-mission XP equivalents at its unlock level; acknowledgement or expiration immediately restores the balanced network. Permanent tests prove all four identities appear within six spotlight boards and mature epochs remain evenly distributed. Fuel-limited projections settle at level 120 for free standard play and level 209 for 160-fuel cheapest-route play, with level 100 reached on days 231 and 68 respectively. The normal collection grows to 1,100 series; after 5,000 contracts, 100 deterministic careers discover a median 1,069 and none completes it. Visual readiness becomes 6/290 supplied assets, leaving 284 external deliverables; the new habitat, arena, icon and four portraits remain explicit `pending_user_asset` entries and Codex created no artwork. The fast gate, now including 8,262 equipment semantic combinations, passes in 78.95 seconds; the complete gate passes in 78.83 seconds without a save-schema change.

251. Version `0.62.0`/code 123 extends the modular year-one pipeline to level 150 with `Mercado de Memórias Usadas`, a megacity bazaar where recollections, childhoods and identities are traded as ordinary goods. The permanent network now contains eighteen worlds, 72 targets and 36 three-choice incidents; the pack adds four memory-market warrants, twelve named attacks, eight weapon/armor families and four universal boot families in exact PT/EN parity. Its 72-minute route moves current final discovery to 2,037 standard hunts and 942.5 route hours. Fuel-limited projections settle at level 120 for free standard play and level 205 for 160-fuel cheapest-route play, with level 100 still reached on days 231 and 68 respectively. The normal collection grows to 1,160 series; after 5,000 contracts, 100 deterministic careers discover a median 1,121 (p10 1,115, p90 1,128) and none completes it. Visual readiness becomes 6/297 supplied assets, leaving 291 external deliverables; the new habitat, arena, icon and four portraits remain explicit `pending_user_asset` entries and Codex created no artwork. The complete 51-suite gate, including 8,748 equipment semantic combinations, mobile/text guards and clean boot, passes in 79.54 seconds without a save-schema change; the next production priority is the level-160 planet pack.

252. Version `0.63.0`/code 124 extends the modular year-one pipeline to level 160 with `Estaleiro de Naufrágios Temporais`, an orbital yard that salvages ships before their accidents and invoices every surviving timeline. The permanent network now contains nineteen worlds, 76 targets and 38 three-choice incidents; the pack adds four temporal-shipyard warrants, twelve named attacks, eight weapon/armor families and four universal glove families in exact PT/EN parity. Its 76-minute route moves current final discovery to 2,268 standard hunts and 1,097.9 route hours. Fuel-limited projections settle at level 120 for free standard play and level 203 for 160-fuel cheapest-route play, with level 100 still reached on days 231 and 68 respectively. The normal collection grows to 1,220 series; after 5,000 contracts, 100 deterministic careers discover a median 1,170 (p10 1,161, p90 1,178) and none completes it. Visual readiness becomes 6/304 supplied assets, leaving 298 external deliverables; the new habitat, arena, icon and four portraits remain explicit `pending_user_asset` entries and Codex created no artwork. The complete 51-suite gate, including 9,234 equipment semantic combinations, mobile/text guards and clean boot, passes in 80.60 seconds without a save-schema change; the next production priority is the level-170 pack.

253. Version `0.63.1`/code 125 bounds the Galaxy map for the growing annual catalog. Early players previously instantiated every current destination as a full card, including sixteen inaccessible worlds: at nineteen worlds the level-one map built 202 descendant nodes and took 18.15 ms synchronously on the desktop benchmark, a cost that scales linearly and is amplified on physical Android. The map now renders every unlocked world plus exactly two upcoming signals and collapses the remaining future catalog into one bilingual lightweight summary. Level-one output falls to 78 nodes and 10.25 ms, reductions of 61% and 43% respectively, without hiding an unlocked route or weakening the new-world focus path. UI and mobile regressions prove the bounded count, distant-signal summary, touch behavior and visible visual-asset boundary in both languages. The complete 51-suite gate, including mobile/text guards, persistence semantics and clean boot, passes in 80.32 seconds. No artwork or save-schema change is involved.

254. Version `0.63.2`/code 126 removes a second Galaxy scaling cost. `is_planet_available()` previously built and deeply duplicated the full unlocked planet array for every visible card, turning a simple identity check into repeated allocation. It now scans immutable canonical definitions and returns directly without exposing or copying them. A 19-world, 1,000-cycle benchmark measures the old allocating lookup at 414.21 ms and the direct lookup at 21.35 ms, approximately 19.4 times faster, while mission and UI regressions prove identical unlock behavior, unknown-ID failure and full visibility at level 160. The UI benchmark now reports the complete mature Galaxy explicitly, preventing future catalog growth from remaining invisible to profiling. The complete 51-suite gate passes in 80.86 seconds. No artwork or save-schema change is involved.

255. Version `0.64.0`/code 127 extends the modular year-one pipeline to level 170 with `Bolsa de Luas Fracionadas`, an orbital financial exchange that sells fractional craters, gravity futures and majority ownership of moons. The permanent network now contains twenty worlds, 80 targets and 40 three-choice incidents; the pack adds four lunar-market warrants, twelve named attacks, eight weapon/armor families and four universal helmet families in exact PT/EN parity. Its 80-minute route moves current final discovery to 2,511 standard hunts and 1,268.1 route hours. Fuel-limited projections settle at level 120 for free standard play and level 201 for 160-fuel cheapest-route play, with level 100 still reached on days 231 and 68 respectively. The normal collection grows to 1,280 series; after 5,000 contracts, 100 deterministic careers discover a median 1,218 (p10 1,209, p90 1,228) and none completes it. Visual readiness becomes 6/311 supplied assets, leaving 305 external deliverables; the new habitat, arena, icon and four portraits remain explicit `pending_user_asset` entries and Codex created no artwork. The long-horizon market audit also pins its simulated economy day so calendar rollover cannot alter the sampled rarity and create false monotonicity failures. The complete 59-test gate, including 9,720 equipment semantic combinations, mobile/text guards, repository/reference audits and clean boot, passes in 81.63 seconds without a save-schema change.

256. Version `0.65.0`/code 128 extends the modular year-one pipeline to level 180 with `Fábrica de Sóis Recondicionados`, a system-sized foundry that refurbishes expired stars and sells their cores with three-sunrise warranties. The permanent network now contains twenty-one worlds, 84 targets and 42 three-choice incidents; the pack adds four solar-factory warrants, twelve named attacks, eight weapon/armor families and four universal rig families in exact PT/EN parity. Its 84-minute route moves current final discovery to 2,765 standard hunts and 1,455.7 route hours. Fuel-limited projections remain level 120 for free standard play and level 201 for 160-fuel cheapest-route play, with level 100 still reached on days 231 and 68 respectively. The normal collection grows to 1,340 series; after 5,000 contracts, 100 deterministic careers discover a median 1,266 (p10 1,257, p90 1,276) and none completes it. Visual readiness becomes 6/318 supplied assets, leaving 312 external deliverables; the new habitat, arena, icon and four portraits remain explicit `pending_user_asset` entries and Codex created no artwork. The living-audit header and current-state bullets are also corrected after being left at the early five-to-seven-world era. The complete 59-test gate, including 10,206 equipment semantic combinations, mobile/text guards, repository/reference audits and clean boot, passes in 83.98 seconds without a save-schema change.

257. Version `0.66.0`/code 129 extends the modular year-one pipeline to level 190 with `Clínica de Planetas Descontinuados`, an orbital hospital that sutures continents, replaces oceans and disputes whether extinction is a pre-existing condition. The permanent network now contains twenty-two worlds, 88 targets and 44 three-choice incidents; the pack adds four planetary-clinic warrants, twelve named attacks, eight weapon/armor families and four universal implant families in exact PT/EN parity. Its 88-minute route moves current final discovery to 3,031 standard hunts and 1,661.6 route hours. Fuel-limited projections settle at level 120 for free standard play and level 200 for 160-fuel cheapest-route play, with level 100 still reached on days 231 and 68 respectively. The normal collection grows to 1,400 series; after 5,000 contracts, 100 deterministic careers discover a median 1,307 (p10 1,295, p90 1,316) and none completes it. Visual readiness becomes 6/325 supplied assets, leaving 319 external deliverables; the new habitat, arena, icon and four portraits remain explicit `pending_user_asset` entries and Codex created no artwork. Catalog growth exposed a UI smoke test that treated level 160 as permanently complete; it now derives the mature level from the final canonical planet, so future packs cannot create the same false failure. The complete 59-test gate, including 10,692 equipment semantic combinations, mobile/text guards, repository/reference audits and clean boot, passes in 85.05 seconds without a save-schema change.

258. Version `0.67.0`/code 130 extends the modular year-one pipeline to the level-200 milestone with `Correio de Buracos de Minhoca`, an interdimensional postal hub that sorts parcels by century, reality and probability of delivery. The permanent network now contains twenty-three worlds, 92 targets and 46 three-choice incidents; the pack adds four paradox-post warrants, twelve named attacks, eight weapon/armor families and four universal boot families in exact PT/EN parity. Its 92-minute route moves current final discovery to 3,308 standard hunts and 1,885.0 route hours. Fuel-limited projections remain level 120 for free standard play and land exactly at level 200 for 160-fuel cheapest-route play, with level 100 still reached on days 231 and 68 respectively. The normal collection grows to 1,460 series; after 5,000 contracts, 100 deterministic careers discover a median 1,347 (p10 1,335, p90 1,356) and none completes it. Visual readiness becomes 6/332 supplied assets, leaving 326 external deliverables; the new habitat, arena, icon and four portraits remain explicit `pending_user_asset` entries and Codex created no artwork. The complete 59-test gate, including 11,178 equipment semantic combinations, mobile/text guards, repository/reference audits and clean boot, passes in 86.28 seconds without a save-schema change.

259. Version `0.67.1`/code 131 hardens the runtime for the level-200 catalog without changing content, balance, saves or visuals. Lazy immutable indices now resolve planets, target IDs, planet/tier pairs, per-planet target groups, per-planet incidents, incident choices and the maximum authored rank once, while every public dictionary/array lookup still returns a defensive deep copy. Board and market candidate selection no longer scan all 92 targets for one destination; completed hunts no longer scan the target catalog for rank or all 46 incidents to localize one choice; Career uses indexed groups and allocation-free counts instead of repeatedly filtering the complete catalog; offline patrol and hub counters avoid constructing unused planet copies. A dedicated regression suite exhaustively compares every indexed result with canonical authored order, locks all legacy unknown-ID fallbacks and actively mutates returned planets, targets, groups, incidents and choices to prove the canonical catalog cannot be corrupted. The mature UI benchmark now derives level 200 from the last authored world instead of retaining a level-160 constant. On this desktop, 92,000 target-ID lookups fall from 630.83 ms linear scanning to 197.15 ms through the copy-safe index (3.2× faster), while 23,000 Galaxy availability checks remain 21.0× faster through their allocation-free path. The integrated 60-test fast gate passes in 103.75 seconds and the complete gate, including repository/reference/document audits, mobile/text guards, persistence semantics and clean boot, passes in 98.30 seconds. No artwork was created or modified.

260. Version `0.68.0`/code 132 begins the post-level-200 production margin with `Aquário de Oceanos Confiscados`, a transparent orbital station where seized seas, evidentiary reefs and detained leviathans wait for interplanetary litigation. The permanent network now contains twenty-four worlds, 96 targets and 48 three-choice incidents; this level-210 pack adds four aquatic criminal identities, twelve attacks, eight weapon/armor families and four universal glove families in exact PT/EN parity. Its 96-minute route moves current final discovery to 3,597 standard hunts and 2,126.7 base-route hours. Fuel-limited year projections remain level 120 for free standard play and level 200 for 160-fuel cheapest-route play, confirming that level 210 is real production margin beyond the fastest approved annual profile rather than a paid requirement. Every level-210 class/policy cohort retains two viable routes at or above 55% and visibly ordered pressure. The normal collection grows to 1,520 series; after 5,000 contracts, 100 deterministic careers discover a median 1,381 (p10 1,366, p90 1,393) and none completes it. Visual readiness becomes 6/339 supplied assets, leaving 333 external deliveries; the habitat, arena, icon and four portraits remain explicit `pending_user_asset` records and Codex created no artwork. The complete 60-test gate, including 11,664 equipment semantic combinations, exact translation parity, mobile/text guards, repository/reference/document audits and clean boot, passes in 97.58 seconds without a save-schema change.

261. Version `0.69.0`/code 133 continues the production margin to level 220 with `Central de Sonhos Penhorados`, a moon-sized repossession complex where dreams serve as collateral, overdue nightmares enter storage and prefabricated fantasies accrue compound interest. The mission network now contains twenty-five worlds, 100 targets and 50 three-choice incidents; the pack adds four dream-economy criminal identities, twelve attacks, eight weapon/armor families and four universal helmet families in exact PT/EN parity. Its 100-minute route moves current final discovery to 3,897 standard hunts and 2,388.8 base-route hours. Fuel-limited year projections remain level 120 for free standard play and level 200 for 160-fuel cheapest-route play, leaving both level-210 and level-220 worlds as genuine launch margin beyond the fastest approved annual profile. Every sampled level-220 class/policy cohort retains two viable routes at or above 55% and ordered pressure. The normal collection grows to 1,580 series; after 5,000 contracts, 100 deterministic careers discover a median 1,411 (p10 1,398, p90 1,424) and none completes it. Visual readiness becomes 6/346 supplied assets, leaving 340 external deliveries; the habitat, arena, icon and four portraits remain explicit `pending_user_asset` records and Codex created no artwork. The complete 60-test gate, including 12,150 equipment semantic combinations, exact translation parity, mobile/text guards, repository/reference/document audits and clean boot, passes in 105.17 seconds without a save-schema change.

262. Version `0.70.0`/code 134 continues the production margin to level 230 with `Canil de Asteroides Domésticos`, an orbital kennel that restrains asteroids, trains comets and tracks runaway moons. The mission network now contains twenty-six worlds, 104 targets and 52 three-choice incidents; the pack adds four orbital-animal-handler identities, twelve attacks, eight weapon/armor families and four universal rig families in exact PT/EN parity. Its 104-minute route moves current final discovery to 4,209 standard hunts and 2,671.0 base-route hours. Fuel-limited year projections remain level 120 for free standard play and level 200 for 160-fuel cheapest-route play, leaving levels 210–230 as genuine launch margin beyond the fastest approved annual profile. Every sampled level-230 class/policy cohort retains two viable routes at or above 55% and ordered pressure. The normal collection grows to 1,640 series; after 5,000 contracts, 100 deterministic careers discover a median 1,432 (p10 1,418, p90 1,449) and none completes it. Visual readiness becomes 6/353 supplied assets, leaving 347 external deliveries; the habitat, arena, icon and four portraits remain explicit `pending_user_asset` records and Codex created no artwork. The complete 60-test gate, including 12,636 equipment semantic combinations, exact translation parity, mobile/text guards, repository/reference/document audits and clean boot, passes in 98.32 seconds without a save-schema change. The next production priority is the level-240 pack.

263. Version `0.71.0`/code 135 continues the production margin to level 240 with `Cartório do Último Horizonte`, a registry at the observable edge of the universe that certifies extinctions, surveys cosmic boundaries and grants provisional extensions to existence. The mission network now contains twenty-seven worlds, 108 targets and 54 three-choice incidents; the pack adds four terminal-bureaucracy identities, twelve attacks, eight weapon/armor families and four universal implant families in exact PT/EN parity. Its 108-minute route moves current final discovery to 4,532 standard hunts and 2,973.7 base-route hours. Fuel-limited year projections remain level 120 for free standard play and level 200 for 160-fuel cheapest-route play, leaving levels 210–240 as genuine launch margin beyond the fastest approved annual profile. Every sampled level-240 class/policy cohort retains two viable routes at or above 55% and ordered pressure. The normal collection grows to 1,700 series; after 5,000 contracts, 100 deterministic careers discover a median 1,448 (p10 1,435, p90 1,464) and none completes it. Visual readiness becomes 6/360 supplied assets, leaving 354 external deliveries; the habitat, arena, icon and four portraits remain explicit `pending_user_asset` records and Codex created no artwork. The complete 60-test gate, including 13,122 equipment semantic combinations, exact translation parity, mobile/text guards, repository/reference/document audits and clean boot, passes in 104.05 seconds without a save-schema change. The next production priority is the level-250 pack.

264. Version `0.72.0`/code 136 continues the production margin to level 250 with `Universidade de Vilania por Correspondência`, a modular orbital campus that teaches dramatic monologues, overengineered traps and henchman management by mail. The mission network now contains twenty-eight worlds, 112 targets and 56 three-choice incidents; the pack adds four academic-villain identities, twelve attacks, eight weapon/armor families and four universal boot families in exact PT/EN parity. Its 112-minute route moves current final discovery to 4,867 standard hunts and 3,300.1 base-route hours. Fuel-limited year projections remain level 120 for free standard play and level 200 for 160-fuel cheapest-route play, leaving levels 210–250 as genuine launch margin beyond the fastest approved annual profile. Every sampled level-250 class/policy cohort retains two viable routes at or above 55% and ordered pressure. The normal collection grows to 1,760 series; after 5,000 contracts, 100 deterministic careers discover a median 1,454 (p10 1,440, p90 1,477) and none completes it. Visual readiness becomes 6/367 supplied assets, leaving 361 external deliveries; the habitat, arena, icon and four portraits remain explicit `pending_user_asset` records and Codex created no artwork. The complete 60-test gate, including 13,608 equipment semantic combinations, exact translation parity, mobile/text guards, repository/reference/document audits and clean boot, passes in 101.08 seconds without a save-schema change. The next production priority is the level-260 pack.

265. Version `0.73.0`/code 137 continues the production margin to level 260 with `Agência de Deuses Reformados`, an orbital temple-employment center that retrains forgotten deities, audits miracles and turns prophecies into résumés. The mission network now contains twenty-nine worlds, 116 targets and 58 three-choice incidents; the pack adds four divine-employment identities, twelve attacks, eight weapon/armor families and four universal glove families in exact PT/EN parity. Its 116-minute route moves current final discovery to 5,213 standard hunts and 3,649.0 base-route hours. Fuel-limited year projections remain level 120 for free standard play and level 200 for 160-fuel cheapest-route play, leaving levels 210–260 as genuine launch margin beyond the fastest approved annual profile. Every sampled level-260 class/policy cohort retains two viable routes at or above 55% and ordered pressure. The normal collection grows to 1,820 series; after 5,000 contracts, 100 deterministic careers still discover a median 1,454 (p10 1,440, p90 1,477) and none completes it because the new level-260 families remain beyond those sampled careers. Visual readiness becomes 6/374 supplied assets, leaving 368 external deliveries; the habitat, arena, icon and four portraits remain explicit `pending_user_asset` records and Codex created no artwork. The complete 60-test gate, including 14,094 equipment semantic combinations, exact translation parity, mobile/text guards, repository/reference/document audits and clean boot, passes in 101.14 seconds without a save-schema change. The next production priority is the level-270 pack.

266. Version `0.74.0`/code 138 continues the production margin to level 270 with `Reserva de Espécies Impossíveis`, an orbital zoo preserving paradox eggs, already-extinct futures and predators that appear only when unobserved. The mission network now contains thirty worlds, 120 targets and 60 three-choice incidents; the pack adds four impossible-fauna identities, twelve attacks, eight weapon/armor families and four universal helmet families in exact PT/EN parity. Its 120-minute route moves current final discovery to 5,570 standard hunts and 4,016.1 base-route hours. Fuel-limited year projections remain level 120 for free standard play and level 200 for 160-fuel cheapest-route play, leaving levels 210–270 as genuine launch margin beyond the fastest approved annual profile. Every sampled level-270 class/policy cohort retains two viable routes at or above 55% and ordered pressure. The normal collection grows to 1,880 series; after 5,000 contracts, 100 deterministic careers still discover a median 1,454 (p10 1,440, p90 1,477) and none completes it because the new level-270 families remain beyond those sampled careers. Visual readiness becomes 6/381 supplied assets, leaving 375 external deliveries; the habitat, arena, icon and four portraits remain explicit `pending_user_asset` records and Codex created no artwork. The complete 60-test gate, including 14,580 equipment semantic combinations, exact translation parity, mobile/text guards, repository/reference/document audits and clean boot, passes in 102.25 seconds without a save-schema change. The next production priority is the level-280 pack.

267. Version `0.75.0`/code 139 continues the production margin to level 280 with `Oficina de Realidades Defeituosas`, an orbital repair shop that patches cracked universes, replaces counterfeit physical laws and recalls defective dimensions. The mission network now contains thirty-one worlds, 124 targets and 62 three-choice incidents; the pack adds four reality-repair identities, twelve attacks, eight weapon/armor families and four universal rig families in exact PT/EN parity. Its 124-minute route moves current final discovery to 5,939 standard hunts and 4,409.1 base-route hours. Fuel-limited year projections remain level 120 for free standard play and level 200 for 160-fuel cheapest-route play, leaving levels 210–280 as genuine launch margin beyond the fastest approved annual profile. Every sampled level-280 class/policy cohort retains two viable routes at or above 55% and ordered pressure. The normal collection grows to 1,940 series; after 5,000 contracts, 100 deterministic careers still discover a median 1,454 (p10 1,440, p90 1,477) and none completes it because the new level-280 families remain beyond those sampled careers. Visual readiness becomes 6/388 supplied assets, leaving 382 external deliveries; the habitat, arena, icon and four portraits remain explicit `pending_user_asset` records and Codex created no artwork. The complete 60-test gate, including 15,066 equipment semantic combinations, exact translation parity, mobile/text guards, repository/reference/document audits and clean boot, passes in 103.93 seconds without a save-schema change. The next production priority is the level-290 pack.

268. Version `0.76.0`/code 140 continues the production margin to level 290 with `Seguradora de Apocalipses Evitáveis`, an orbital insurer that values destroyed planets, excludes prophecies and collects extinction deductibles. The mission network now contains thirty-two worlds, 128 targets and 64 three-choice incidents; the pack adds four apocalypse-insurance identities, twelve attacks, eight weapon/armor families and four universal implant families in exact PT/EN parity. Its 128-minute route moves current final discovery to 6,320 standard hunts and 4,834.5 base-route hours. Fuel-limited year projections remain level 120 for free standard play and level 200 for 160-fuel cheapest-route play, leaving levels 210–290 as genuine launch margin beyond the fastest approved annual profile. Every sampled level-290 class/policy cohort retains two viable routes at or above 55% and ordered pressure. The normal collection grows to 2,000 series; after 5,000 contracts, 100 deterministic careers still discover a median 1,454 (p10 1,440, p90 1,477) and none completes it because the new level-290 families remain beyond those sampled careers. Visual readiness becomes 6/395 supplied assets, leaving 389 external deliveries; the habitat, arena, icon and four portraits remain explicit `pending_user_asset` records and Codex created no artwork. The complete 60-test gate, including 15,552 equipment semantic combinations, exact translation parity, mobile/text guards, repository/reference/document audits and clean boot, passes in 102.47 seconds without a save-schema change. The next production priority is the level-300 pack.

269. Version `0.77.0`/code 141 continues the production margin to level 300 with `Leilão de Impérios Falidos`, an orbital exchange that liquidates thrones, fleets and insolvent civilizations. The mission network now contains thirty-three worlds, 132 targets and 66 three-choice incidents; the pack adds four imperial-liquidation identities, twelve attacks, eight weapon/armor families and four universal boot families in exact PT/EN parity. Its 132-minute route moves current final discovery to 6,712 standard hunts and 5,281.9 base-route hours. Fuel-limited year projections remain level 120 for free standard play and level 200 for 160-fuel cheapest-route play, leaving levels 210–300 as genuine launch margin beyond the fastest approved annual profile. Every sampled level-300 class/policy cohort retains two viable routes at or above 55% and ordered pressure. The normal collection grows to 2,060 series; after 5,000 contracts, 100 deterministic careers still discover a median 1,454 (p10 1,440, p90 1,477) and none completes it because the new level-300 families remain beyond those sampled careers. Visual readiness becomes 6/402 supplied assets, leaving 396 external deliveries; the habitat, arena, icon and four portraits remain explicit `pending_user_asset` records and Codex created no artwork. The complete 60-test gate, including 16,038 equipment semantic combinations, exact translation parity, mobile/text guards, repository/reference/document audits and clean boot, passes in 106.83 seconds without a save-schema change. The next production priority is the level-310 pack.

270. Version `0.78.0`/code 142 continues the production margin to level 310 with `Fábrica de Coincidências Industriais`, an orbital line manufacturing luck, useful accidents and wholesale plot twists. The mission network now contains thirty-four worlds, 136 targets and 68 three-choice incidents; the pack adds four industrial-fate identities, twelve attacks, eight weapon/armor families and four universal glove families in exact PT/EN parity. Its 136-minute route moves current final discovery to 7,115 standard hunts and 5,755.9 base-route hours. Fuel-limited year projections remain level 120 for free standard play and level 200 for 160-fuel cheapest-route play, leaving levels 210–310 as genuine launch margin beyond the fastest approved annual profile. Every sampled level-310 class/policy cohort retains two viable routes at or above 55% and ordered pressure. The normal collection grows to 2,120 series; after 5,000 contracts, 100 deterministic careers still discover a median 1,454 (p10 1,440, p90 1,477) and none completes it because the new level-310 families remain beyond those sampled careers. Visual readiness becomes 6/409 supplied assets, leaving 403 external deliveries; the habitat, arena, icon and four portraits remain explicit `pending_user_asset` records and Codex created no artwork. The complete 60-test gate, including 16,524 equipment semantic combinations, exact translation parity, mobile/text guards, repository/reference/document audits and clean boot, passes in 105.94 seconds without a save-schema change. The next production priority is the final level-320 launch-catalog pack.

271. Version `0.79.0`/code 143 completes the launch planet axis at level 320 with `Central de Reinícios Cósmicos`, the universe's restart facility where Big Bang schedules, memory wipes, reality backups and cosmic reset permissions become a criminal bureaucracy. The permanent mission network now contains the promised thirty-five worlds, 140 targets and 70 three-choice incidents; the final pack adds four restart-system identities, twelve attacks, eight weapon/armor families and four universal helmet families in exact PT/EN parity. Its 140-minute route moves final discovery to 7,530 standard hunts and 6,254.3 base-route hours. Fuel-limited annual projections remain level 120 for free standard play and level 200 for 160-fuel cheapest-route play, confirming that levels 210–320 are production margin rather than paid progression. Every sampled level-320 class/policy cohort retains two viable routes at or above 55% and ordered pressure. The normal collection reaches 2,180 series; after 5,000 contracts, 100 deterministic careers discover a median 1,454 (p10 1,440, p90 1,477) and none completes it. Visual readiness becomes 6/416 supplied assets, leaving 410 external deliveries; the habitat, arena, icon and four portraits remain explicit `pending_user_asset` records and Codex created no artwork. The complete 60-test gate, including 17,010 equipment semantic combinations, exact translation parity, mobile/text guards, repository/reference/document audits and clean boot, passes in 105.13 seconds without a save-schema change. Planet expansion beyond level 320 is now frozen until a transversal launch-content audit identifies a proven systemic gap.

272. Version `0.80.0`/code 144 audits the completed launch catalog as a long-term game rather than another content-count exercise. The network, 2,180-series collection, daily/weekly cycles, two-reality Fenda and economy are now measured together in `LAUNCH_CONTENT_COVERAGE_AUDIT_2026-08-29.md` and protected by a permanent coverage gate. The first proven gap was Career: its eight milestones ended around the fifth world despite a 35-world catalog. Eleven stable goals now extend the ladder to nineteen, spanning 100/500/1,000/2,000/3,000 normal hunts and 10/15/20/25/30/35 discovered worlds. Every dossier shows exact numeric and bar progress; all rewards remain one-time, save-safe and bounded to 46,230 Credits plus 482 Scrap across the whole ladder, without premium currency, fuel, levels, attributes, Rift entries or exclusive equipment. Existing IDs, claims and schema remain unchanged. The audit also records the next two genuine gaps: the Fenda currently offers 24 first-clear days and a standard 200-win career retains 136,391 Credits after buying every transport. The complete 61-test gate, including 17,010 equipment semantic combinations, exact PT/EN parity, mobile/touch/125% text guards, repository/reference/document audits and clean boot, passes in 106.54 seconds. The next production priority is a simulated recurring Credit expense that preserves premium market refreshes as bounded choice rather than guaranteed power.

273. Version `0.81.0`/code 145 converts the measured excess Credit balance into a transparent, voluntary workshop service without weakening Scrap or competing with premium market refreshes. Power calibration and integrity reinforcement now require both the existing Scrap cost and a Credit fee derived from persisted item level plus prior work; insufficient funds reject the complete transaction before either balance or item state changes. Arsenal balances, actions, recommendations, tooltips and receipts expose both costs in exact PT/EN parity, while navigation advertises a funded field test only when one real calibration is affordable. A new deterministic audit runs 40 careers through 200 real contracts, loot decisions, recycling, transport purchases and rational workshop use: the median career reaches level 33, owns all four transports, performs 55 interventions and pays 23,707 Credits, ending at 112,808 instead of 136,391. The service therefore absorbs 17.4% of otherwise retained Credits without selling power for Warp Chips or delaying the transport ladder. The model, commercial limits and rejected alternatives are recorded in `WORKSHOP_CREDIT_SERVICE_AUDIT_2026-08-29.md`; no save-schema or visual-asset change was required. The complete 61-test gate, including 17,010 equipment semantic combinations, exact localization, 125% text, mobile, persistence, repository/reference/document audits and clean boot, passes in 109.08 seconds. The next production priority is a third keyed twelve-enemy Fenda reality inside this validated economy.

274. Version `0.82.0`/code 146 expands the Fenda from 24 to 36 first-clear days with the `Cidade dos Futuros Recusados`, a third keyed reality spanning projected levels 160–215. Its twelve enemies, 36 attacks and twelve sealed universal artifacts have distinct PT/EN identities, from the Temporal Visa Inspector to the Future That Fired the Present; their visual records add twelve explicit external deliveries and retain procedural fallbacks, so readiness becomes 6/428 and Codex created no artwork. Completing Frozen Verdict plus level 160 makes the permanent key discoverable through ordinary hunts with a seven-hunt bad-luck ceiling. One global UTC entry, no fuel, sealed rewards, defeat semantics, canonical composite IDs and premium prohibitions remain unchanged and require no save migration. Cross-class probes keep every encounter at 47–88% with a maximum 30-point spread. Advanced first clears now use one shared economic rule: 1.25× the standard warrant Credits and XP at their checkpoint, totaling 335,703 Credits/13,929 XP for Frozen Verdict and 718,428 Credits/20,229 XP for Rejected Futures; every payout funds between one and two first calibration services and can be claimed only once. Three vertical reality actions initially pushed the fixed entry below the 450×800 viewport at 125% text; a single 72-unit selector now exposes only owned realities and restores the complete mobile composition. The complete 61-test gate, including 17,010 equipment combinations, exact localization, three-reality mobile/text coverage, exhaustive persistence, repository/reference/document audits and clean boot, passes in 108.60 seconds. The next product gap is repeatable post-clear retention after this finite 36-day ladder.

275. Version `0.83.0`/code 147 adds the repeatable `Network Circuit` after auditing Daily, Weekly, Black Warrant, Rift and the complete mission catalog as one retention stack. Every Monday snapshots up to three unlocked worlds; one circuit destination is guaranteed among the three normal warrants while a newly unlocked world's first presentation keeps precedence in another slot. Two captures per destination produce a six-hunt mature route and one explicit 250-Credit/18-Scrap payment. No route changes fuel, timing, pressure, XP, normal payout, loot or incidents, and it cannot be rerolled, accelerated or repeated with Warp Chips. At full catalog, twelve consecutive weeks expose all 35 worlds while the existing target rotation continues cycling their 140 identities. The dossier sits inside Weekly Operations and compact board markers reuse the existing UI without new artwork. Schema 24 adds only the deterministic snapshot, bounded sparse per-world counters and one claim flag; migration and sanitization cannot invent progress, accept forged destinations or issue a false repair warning for untouched `0/2` routes. PT/EN, board, full reward transaction, mobile scroll, payment, reset and no-double-claim coverage protect the system. The complete 61-test gate, including persistence semantics, 125% text, mobile constraints, documentation and clean boot, passes in 106.96 seconds. The 34,070,435-byte ARM64 APK passes API 24+, synchronized Desktop/Android version, stable signature and reference-boundary checks with SHA-256 `3061c7eccafcac698f2e0356e421553bbee2ab16028e3fc69f9f1d31b81255b2`. The next product priority is to audit the full 36-day Rift economy calendar before considering any fourth reality.

276. Version `0.84.0`/code 148 closes the 36-day Rift economy audit before any fourth reality. A deterministic integrated model runs all 36 first clears beside real mission boards, fuel, loot, recycling, Daily, Weekly, Network Circuit, Black Warrants, transport purchases and rational workshop services. Free profiles perform 168–228 normal hunts, finish four to five complete Circuits and receive 22.9–23.9% of gross Credits from the Rift; the 160-fuel profile performs 370 hunts and lowers that share to 15.1%, while all profiles receive the exact same 1,059,023 Rift Credits. Premium refills therefore buy ordinary activity, never extra Rift entry or payout. The audit exposed two contract violations: advanced artifacts omitted their authored economy level and Arsenal only presented weapon/armor services despite nine universal slots. Rewards now persist `item_level`, schema 25 repairs old equipped/stored/pending copies, and a compact nine-slot selector presents one complete mobile workbench dossier while recommendations and field projections cover every slot. All 24 advanced payouts fund exactly 1.69 matching first services. Finally, normal mission fuel is capped at the free 100-unit reserve while authored 124–140-minute waits remain intact, guaranteeing at least one free hunt on every unlocked planet without weakening transports. No visual asset was created or changed. The complete 62-suite gate, including exhaustive persistence, PT/EN, 125% text, mobile constraints, documentation and clean boot, passes in 110.27 seconds. The 34,074,531-byte ARM64 APK passes API 24+, synchronized version, stable signature and reference-boundary checks with SHA-256 `441e74c8046fb9aa907ac51f42cbdc9de885afd267e7322227c7393d7668abcb`.

277. Version `0.85.0`/code 149 replaces the old one-and-done Rift entry with the approved defeat-recovery contract while preserving its daily reward ceiling. Every UTC day starts with one free attempt and consumes no hunt fuel; victory immediately closes the Rift until reset, while defeat keeps the exact enemy, anomaly and sealed reward available for at most three explicitly confirmed retries costing 1/5/20 Warp Chips. Transactions verify the expected price and stage atomically, persist victory/retry state through schema 26 and cannot charge after midnight, change opponents or duplicate a reward. Advanced keys now roll once per eligible UTC day instead of once per hunt, retain five/seven-day pity, and trigger a skippable portal-stabilization ceremony before the new enemy is exposed. That ceremony is a documented code-native fallback; final keys, rings, core and distortion remain external asset deliveries and no artwork was created. A new chronology model combines live XP/fuel curves, gates, pity and retry probability: at a 55% per-attempt baseline, free-standard play completes its reachable realities around days 23/254, free-efficient reaches the third around day 371, and the fastest 160-fuel profile completes all current content around day 224 with roughly 123 retry Chips. This exposes a measured 141-day fastest-profile gap and supports a future post-level-200 fourth reality without authoring it prematurely. Active monetization, Rift, year-one and UI-asset contracts were reconciled with the executable rules. The complete 63-suite gate, including schema migration, exact PT/EN parity, mobile/text constraints, documentation and clean boot, passes in 119.32 seconds. The 34,086,990-byte ARM64 APK passes API 24+, synchronized version, stable signature and reference-boundary checks with SHA-256 `df967dbb192e6bff8ab250b34aa8907af1550273c11145cc4b317f4be970a351`.

278. Version `0.85.1`/code 150 corrects the first year-one Rift chronology before its optimistic conclusion could drive unnecessary content production. The initial model applied a flat 55% chance immediately after each key and therefore ignored that the 36 encounters were explicitly calibrated at levels 8–90, 100–155 and 160–215. The corrected model records every real level-crossing day and schedules each floor only once its representative envelope exists. Free-standard play now finishes reality one around day 182 and reaches five floors of reality two; free-efficient finishes realities one/two around days 90/323 and reaches one floor of reality three; the fastest 160-fuel profile finishes the first two around days 55/194 but reaches only nine floors of reality three because level 200 arrives on day 364. The remaining level-205/210/215 enemies carry the ladder beyond year one, so the previously reported 141-day gap and immediate fourth-reality recommendation are withdrawn. Every Rift dossier now exposes the current enemy's authored recommended level beside its live build chance without revealing future enemies or drops. The model, active contracts and tests protect this distinction; no save, economy, combat stat or visual asset changed. The complete 63-suite gate passes in 112.25 seconds. The 34,086,990-byte ARM64 APK passes API 24+, synchronized version, stable signature and reference-boundary checks with SHA-256 `2a18987fca063f01eeb54b89840dca9b07e271c4020cd678f7ff56b3f1be7fc5`.

279. Version `0.85.2`/code 151 follows the corrected annual audit into the long-term navigation seam it exposed. Career and Frontier Menu previously treated `12/12` in the selected reality as if all three keyed realities were complete; selecting an old finished archive could hide an owned unfinished ladder, remove Career's route and provide no next-key guidance. A shared progression resolver now prefers the selected incomplete reality, otherwise the first owned incomplete reality, and reserves “Rift complete” for all 36 clears with all three keys. Career names the active reality, includes the current enemy's recommended level, retains access after a reality clear, selects an unfinished owned reality before navigation and reports either the next key's level gate or exact eligible-day/pity progress. Frontier Menu mirrors the same truth in its compact service card. Focused Career, Fenda, PT/EN, mobile, 125%-text and complete UI tests protect the multi-reality transitions. The launch-content audit now records the checkpoint-aware calendar and this correction instead of recommending another daily checklist or premature fourth reality. Test-log retention now prunes only GUID-named runs created by the complete gate, preserving focused evidence and avoiding false failures when a diagnostic log remains open. No save schema, combat, economy or visual asset changed. The complete 63-suite gate passes in 114.32 seconds. The 34,091,086-byte ARM64 APK passes API 24+, synchronized version, stable signature and reference-boundary checks with SHA-256 `8743b16d7439ca2e6f7ad3146291254e41e6e6a9dc17dc92c2b0106ed2ada090`.

280. Version `0.86.0`/code 152 performs the post-catalog product audit and resolves its first measured transversal bottleneck. The game now contains 35 worlds, 140 targets, 70 incidents, 2,180 equipment series and 36 keyed Rift enemies; fuel-limited annual profiles end at levels 120–200, so neither another planet nor a fourth reality is a launch need. The honest blockers are remote authority/billing, 422 external visual deliveries and physical Android evidence. Local persistence remains bounded at 11.5 ms median/13.7 ms p95 with 120 items, while 40 × 200-contract careers show the Workshop absorbing 18.2% of otherwise retained Credits. UI profiling exposed a scale defect in the mature Galaxy: rendering all 35 routes synchronously constructed 368 nodes and cost 31.8 ms although most cards were off-screen. The map now presents five-route pages with 72-unit touch navigation, exact PT/EN range context and automatic page selection for the current or newly discovered world. Every route remains reachable, acknowledgement preserves its page and distant signals remain on the final known page. The mature render falls to 103 nodes/11.7 ms, reductions of 72% and 63% respectively. UI, pagination, focus, touch, 125%-text, localization, content, persistence and clean boot coverage pass in the complete 63-suite gate in 114.67 seconds. No save schema, balance, economy or visual asset changed. The 34,091,086-byte ARM64 APK passes API 24+, synchronized version, stable signature and reference-boundary checks with SHA-256 `428bd57261c4feb57884416d516f9a1981924236e3f0c278713173e743e96fb7`.

281. Version `0.87.0`/code 153 converts the generic future-guild note into the server-ready `Bounty Agency` product contract without pretending the local test account is online. An Agency belongs exclusively to `International 1`, contains at most 25 characters with exactly one Director, and exposes Director/Coordinator/Agent/Recruit permissions under a revisioned server snapshot; device authority, foreign shards, duplicate characters and forged leadership are rejected. The weekly Agency Warrant snapshots at least four recently active eligible Agents and moves through investigation, target revelation and collective capture. Normal hunts contribute at most three Intel per Agent/UTC day regardless of paid fuel. Capture grants one free, non-premium attempt per Agent/day against a level-bounded personal instance; contribution is normalized to 3 points for a legitimate defeat or 10–15 for victory rather than raw damage. Goals scale from the eligible active roster, and permanent simulations prove 4-, 10- and 25-Agent rosters can finish within seven days at 75% participation. Expected revisions and unique event IDs make every transaction conflict-aware and idempotent. Reward eligibility requires the weekly snapshot plus real Intel or capture participation; Warp Chips cannot buy membership, contribution, attempts, Prestige or ranking. Membership and warrant state are deliberately absent from the local player save, `agency_backend` remains false, and no social UI or fake players were added. The active account, monetization and Agency contracts now reserve the complete remote boundary; Consortiums remain a post-population system. The complete 64-suite gate passes in 120.48 seconds without save, combat, economy or visual-asset changes. The 34,099,437-byte ARM64 APK passes API 24+, synchronized version, stable signature and reference-boundary checks with SHA-256 `efe3c810493bd0edc0e5b06be8e4fa1440972ed191ab0f73f38dbd6f76579cc8`.

282. Version `0.88.0`/code 154 establishes the first executable remote-backend seam without inventing a deployed service. Protocol version 1 validates authenticated sessions against `International 1`, explicit account/character ownership, bounded identifiers and a maximum 30-day lifetime; its persistable session summary strips all credentials while bearer extraction is isolated for memory-only transport. Server-UTC samples use request midpoint, round-trip and uncertainty and reject reversed or stale measurements. Authoritative character snapshots bind shard, account, character, revision and server time as one non-mergeable envelope. Profile and command payloads recursively reject passwords, authorization values, tokens, API keys and client secrets. Six reserved profile/Agency mutations carry stable command and idempotency IDs plus expected revision; accepted or duplicate receipts complete the same operation, conflicts fetch server truth, and transient retries must preserve the original command identity. Account, clock, profile and Agency capabilities remain independently false, the local save remains device-authoritative and `local_only`, and no online UI, network call, credential persistence, billing behavior, gameplay balance, save schema or visual asset changed. The active backend, account and Agency contracts document the activation order. The complete 65-suite gate passes in 116.26 seconds. The 34,107,808-byte ARM64 APK passes API 24+, synchronized version, stable signature and reference-boundary checks with SHA-256 `c7b5c312493bbcd910adff056ac77ee40754137e78129923fa8022b97a4b98cd`.

283. Version `0.89.0`/code 155 selects the online foundation after comparing Nakama, Supabase, Firebase and PlayFab against Godot integration, server authority, social systems, billing integrity and early operating cost. Open-source Nakama with PostgreSQL wins because its maintained Godot 4 client, authentication, conditional storage, groups, leaderboards, sandboxed server runtime and Google Play receipt validation map directly onto character authority, Bounty Agencies, asynchronous Arena and Warp Chip purchases; the alternatives require either a community Godot bridge or substantially more custom game infrastructure. The reviewed stack pins Nakama `3.40.0`, runtime types `1.47.0` and Godot client `3.4.0`. Android identity uses Play Games Services v2 server auth codes, with linked e-mail/password recovery; internal device authentication must use a random app-private UUID rather than `OS.get_unique_id()`. Executable deployment rules keep the current client explicitly `offline`, accept cleartext only on loopback, require TLS/443 remotely, reject server secrets in client configuration and make account, clock, profile, Agency and billing activation depend on independent end-to-end evidence. A dedicated backend workspace records environment separation and excludes all backend material, secrets, dependencies and data from runtime exports. Docker/Compose is absent on the development host, so neither an untestable server scaffold nor Nakama client add-on was introduced; all remote capability flags remain false and gameplay/save authority remains unchanged. The complete 66-suite gate passes in 115.43 seconds. The 34,112,087-byte ARM64 APK passes API 24+, synchronized version, stable signature and reference-boundary checks with SHA-256 `9e5192a334406d3d5bbfaf8639113f1c4b910b497b9596ea2ccefcd429e14931`.
284. Version `0.90.0`/code 156 turns the selected online architecture into a reproducible local foundation without changing the exported game's authority. Docker Desktop 4.88.1 now runs pinned Nakama `3.40.0` and PostgreSQL `16.8`; the official Docker Hub image is the documented fallback after the Heroic Labs Scarf gateway returned HTTP 429. A multi-stage image compiles the pinned TypeScript runtime types `1.47.0` and registers protocol-v1 `cg_clock` for `international_1`. PostgreSQL remains private to the Docker network, Nakama API/console bind only to loopback, and random local database, session, runtime and console secrets remain ignored by Git, absent from Docker build context, excluded from Desktop/Android exports and unprinted by tests. The repeatable integration smoke test verifies HTTP health, rejects anonymous RPC calls, creates a stable development device session, and bounds server UTC inside the observed request window. Both containers are healthy and clean startup logs load exactly one runtime module and RPC without insecure-default warnings. The APK deliberately vendors no client SDK or endpoint and keeps account, clock, profile, Agency and billing capabilities false until client-side gates pass. The complete 66-suite game gate plus backend static and live integration gates passes; the game suite completes in 125.63 seconds. The unchanged-content 34,112,087-byte ARM64 APK passes API 24+, synchronized version, stable signature and reference-boundary checks with SHA-256 `7021205928d7c4446bbaf1d4077fcb43b14094ad488bd0ca15506d07d765e71c`.
285. Version `0.91.0`/code 157 completes the local Godot transport gate without presenting the internal APK as an online game. The official Nakama Godot `3.4.0` release is vendored with its Apache-2.0 license, signed release commit, archive size/SHA-256 and guarded core-file hashes, then registered through its documented autoload. A Crooked Galaxy adapter is inert by default, accepts cleartext only on loopback, exposes a key-free configuration summary, keeps the SDK session in memory and returns sanitized error codes instead of remote payloads. Development identity uses a stable test-only app-private ID rather than `OS.get_unique_id()`. The PowerShell harness injects the ignored local client key only into the child Godot process and restores its prior environment afterward. A real Godot 4.7.1 run authenticates through the official SDK, obtains a server account identity, invokes `cg_clock`, validates protocol v1, `international_1`, server authority and bounded round-trip, proves no tokens enter its result, then discards the session. Normal boot receives no endpoint or key, and account, clock, profile, Agency and billing flags remain false because ownership and authoritative profile state do not yet exist. The complete 67-suite gate plus direct backend and Godot-to-Nakama integration tests passes; the game gate completes in 162.03 seconds. The 34,368,114-byte ARM64 APK passes API 24+, synchronized version, stable signature and reference-boundary checks with SHA-256 `7ee175b85c811db00d243e0def07997407f80e1976f69bdde2664137903bf769`.
286. Version `0.92.0`/code 158 completes the local character-authority and reconnect-policy batches while deliberately leaving normal gameplay offline. The authenticated account owns exactly one mandatory launch character on `international_1`; class, species, name and four appearance choices are validated, while the server—not the client—sets level 1, zero XP, 25 credits and zero Warp Chips/scrap. Server-only conditional storage binds profile, opaque storage version, public revision and idempotency receipt atomically. Cosmetic commits permit exactly name and appearance, reject injected progression, return explicit stale-revision conflicts and recover same-command races as duplicates. Direct HTTP tests now race two creations and two commits concurrently, proving one mutation and one replay; the official Godot client independently creates/recovers, canonicalizes its sanitized session, commits, retries, conflicts and refetches. Stored profile integrity, exact envelope shape and a 4 KiB payload ceiling are enforced. An inactive seven-day cache writes through a staging file and reopens only as `cached_server` read-only state with economy, social actions and billing disabled. Reconnect replaces uncontested cache wholesale, retries exact pending identities, reviews divergent state and rejects foreign ownership or server regression. Established local progress versus a pristine server baseline offers only a one-time keep-remote or reviewed-import request—never field merge—and no trusted import operation is falsely implemented. All remote capability flags remain false pending TLS staging and normal-boot evidence. The complete 68-suite gate passes in 161.57 seconds. The published 34,376,650-byte ARM64 APK passes content, API 24+, synchronized version, stable signature and reference-boundary checks with SHA-256 `18c4af3a7296fa6bd283d4261e22da491e9f2d7e89ff7b941ec991910d2193b9`.
287. The online foundation gains a reproducible but deliberately unactivated TLS staging operations package without changing the game export or version. The official Caddy `2.11.4-alpine` image is pinned by tag and reviewed multi-platform digest in front of Nakama; PostgreSQL and the game API have no public host port, the console is loopback-only, and Caddy alone exposes 80/443 with automatic certificates, a 128 KiB request ceiling, upstream health checks, HSTS and structured logs. An ignored environment generator creates independent 256-bit secrets; validation rejects placeholders, reserved DNS suffixes, malformed operator e-mail, secret reuse, unpinned topology and unsafe port rendering. The external smoke test covers trusted TLS/HSTS, authentication, clock, character ownership, creation, replay, profile idempotency and stale-revision conflict. Backups use atomic custom-format dumps plus SHA-256; a real local drill verified the dump, restored it with error-stop semantics into a random isolated PostgreSQL volume, found 20 public tables and removed only those exact disposable resources. Compose rendering, Caddy validation, direct backend authority and official Godot-client regression all pass; the complete 68-suite game gate also passes in 162.79 seconds. No external host, DNS name, account flag, normal-boot endpoint, production credential or billing capability was created; public staging and the local-save transition remain explicit gates.
288. The future online cutover is closed against its last avoidable authority flaw. Current saves were created under explicitly device-authoritative internal builds and therefore cannot safely seed competitive levels, currencies, equipment, claims or monetized balances. The previous reviewed-import option is retired: established local progress meeting a pristine revision-zero server character now permits only an explicit `archive_local_start_remote` transition. Import, merge and legacy ambiguous choices fail closed; the offer is one-time and absent over any progressed remote. The old file remains a non-authoritative local archive, while any later internal-tester recognition requires an independent audited server entitlement. Executable sync rules and documentation guards enforce the boundary; normal offline gameplay, save schema, balance and capability flags remain unchanged. The complete 68-suite gate passes in 161.38 seconds.
289. The unactivated online cutover now has a crash-conscious archive primitive rather than documentation alone. It copies every existing primary, interrupted-staging and backup member into a unique app-private bundle, verifies each exact copy by SHA-256 and finalizes a manifest only after all members succeed. That manifest declares `local_archive_only` and explicitly forbids seeding server progress. Existing archive identities cannot be overwritten, a missing primary fails closed, and source files remain untouched until a future normal-boot transaction has separately confirmed both the pristine remote snapshot and durable archive. Focused tests exercise the full three-file family, hashes, manifest order, overwrite refusal and source preservation. The primitive is not called by the current offline boot, changes no save schema or game behavior and creates no online claim. The expanded 69-suite gate passes in 160.16 seconds.
290. The staging hosting decision is narrowed from a generic Linux VPS to an all-Hetzner arrangement: one disposable CX33 x86 instance for Caddy, Nakama and PostgreSQL, with an independent Storage Box for encrypted Borg archives. The live operator console currently offers the selected CX33 only in Helsinki at USD 10.88, so Helsinki replaces the provisional Nuremberg assumption; the Storage Box is assigned to Falkenstein for cross-region recovery. The cloud firewall exposes only restricted SSH and public 80/443; database, Nakama API and console remain behind the existing Docker boundary. Hetzner server snapshots/backups are supplemental disk recovery and never substitute for application-consistent PostgreSQL dumps, SHA-256 evidence and the isolated restore drill. The Borg transfer, recovery-key custody and download/restore proof remain pending until the resources exist. This is explicitly a staging provider choice, not approval of a single-node production topology, and changes no game build or online capability.
291. The operator created the selected Helsinki CX33 at `2.29.2.190` and acquired `crookedgalaxy.com`; `staging-api.crookedgalaxy.com` becomes the canonical staging API hostname. The initial non-interactive probe was correctly denied, but the subsequent rescue audit proved that Hetzner had installed the exact intended ED25519 public key. The actual local blocker was a passphrase-protected private key with no running Windows `ssh-agent`, not a missing server key. After the agent loaded that identity, normal Ubuntu key access passed without changing the installed key. DNS now resolves the canonical hostname to the server. TLS, Storage Box and deployment remain pending and all game capability flags stay false.

292. The first remote host bootstrap is complete and reproducible. Ubuntu 24.04 was fully updated and rebooted into kernel `6.8.0-138-generic`; official Docker Engine/Compose `29.7.2`/`5.5.0`, PowerShell `7.6.5`, UTC, unattended upgrades and bounded local container logs were verified. A key-only `cgdeploy` administrator owns the deployment path with Docker and explicitly authorized passwordless sudo privileges. Root remained temporarily available only until an independent `cgdeploy` login proved SSH, sudo, Docker and configuration validity; the separate finalizer then set `PermitRootLogin no`, `PasswordAuthentication no` and `AllowUsers cgdeploy`. A post-lock session and post-reboot health test passed, while a direct root probe was rejected. The Hetzner Cloud Firewall, public TLS application deployment and independent encrypted Storage Box restore proof are the remaining infrastructure gates; no APK or online capability flag changed.

293. The first public staging deployment is operational at `https://staging-api.crookedgalaxy.com` without activating any APK capability. The Hetzner Cloud Firewall `crooked-galaxy-staging` is fully applied to the CX33 with only TCP 22/80/443 and UDP 443 inbound; an outside probe reaches those intended ports while 5432, 7350 and 7351 remain inaccessible. PostgreSQL 16.8 and Nakama 3.40.0 are healthy with zero restarts, the Nakama console binds only to loopback, and Caddy serves a publicly trusted Let's Encrypt certificate for the exact hostname with HSTS and no Server header. The complete staging test passes authentication, server UTC, mandatory character ownership, revisions, idempotent replay and stale conflicts. Its first backup exposed that Docker had created the ignored bind directory as root, allowing `pg_dump` but preventing the operator checksum. Environment preparation now creates and write-probes that directory before Compose, while backup finalization writes its checksum atomically and removes partial artifacts on failure. After deleting only the failed unchecksummed dump, the corrected 48,675-byte dump passed its SHA-256 check and a complete isolated restore of all 20 public tables. The remaining recovery gate is encrypted off-host transfer to the Falkenstein Storage Box followed by a cross-region download/restore proof; Agency, billing and client online flags remain false.

294. Version `0.93.0`/code 159 closes the desktop normal-boot staging gate without activating the game client. The separate Falkenstein BX11 now receives authenticated `repokey-blake2` Borg archives from the Helsinki CX33; a full repository data check and downloaded SHA-256 restore recovered all 20 public tables, while a hardened persistent systemd timer retains 14 daily, 8 weekly and 12 monthly points. A new command-gated autoload runs only when both the explicit staging probe argument and process-only endpoint/client key are supplied. Through the ordinary main scene and official Nakama add-on, a fresh account proves trusted public TLS, disposable device authentication, pristine remote baseline, exact three-member local-save archival without import or source deletion, server clock, character ownership, accepted and duplicate profile receipts, stale conflict, authoritative refetch, atomic seven-day read-only cache and uncontested reconnect. The operator script retrieves the client key through SSH into process memory, never prints or persists it, and restores prior environment values. The exported project still declares `offline`, embeds no staging hostname/key and leaves account, clock, profile, Agency and billing false. The complete 69-test fast gate plus clean boot passes in 160.39 seconds. The published 34,389,471-byte ARM64 APK passes API 24+, synchronized version, stable signature and reference-boundary checks with SHA-256 `2719df3e4e137c608bbc78475527f76d7bf6ffa7761f93cec5c6a9dc975ac152`. Physical Android certificate/lifecycle and a real-save cutover confirmation remain gates.

295. Version `0.94.0`/code 160 prepares the physical Android public-TLS proof without embedding staging state or requiring destructive device setup. A one-use envelope is accepted only on Android from the app-private `user://` mailbox, with an exact schema/mode, canonical TLS/443 endpoint, bounded disposable device ID and expiry no more than ten minutes ahead. The autoload deletes the mailbox before parsing or networking; malformed, expired and overly durable envelopes fail closed. GameState detects the mailbox first and substitutes a default in-memory profile with persistence disabled, so neither load, migration nor save touches the tester's real profile. The PowerShell operator retrieves the client key through SSH into memory, streams a five-minute JSON payload to the debug package through `adb run-as` standard input, installs with `-r`, launches the normal activity, waits for sanitized PASS/FAIL evidence, verifies the mailbox is absent, records ignored log/package/device evidence and removes the exact mailbox again in `finally`. It rejects emulators and ambiguous device sets by default. Static harness validation, the focused deployment-rules test, backend export-boundary audit and a repeated desktop normal-boot proof against public staging pass. The complete 69-test gate plus clean boot passes in 168.29 seconds. The published 34,393,567-byte ARM64 APK passes content, API 24+, synchronized version, stable signature and reference-boundary checks with SHA-256 `271ab0d34c00900463f98e731c0c0e7fa90d1c136e36d1040beef0d5a7691b6b`. Physical execution remains deliberately pending until one USB-authorized device is connected. Normal APK startup remains offline and endpoint-free, and every online capability flag remains false.

296. The first Android execution used the established Android 15 Pixel 9 AVD because the only available physical handset is enterprise-managed and correctly refuses USB debugging. The one-use mailbox initially exposed two Windows-to-Android operator defects: a fresh package may not yet have its private `files/` directory, and `adb shell` reconstructs unquoted semicolon commands. The harness now creates only that exact private directory and quotes both remote shell programs as single arguments. Version `0.94.0` then reached the normal Godot scene but failed device authentication; manifest inspection proved the APK declared no `android.permission.INTERNET`, so Android blocked transport before Nakama. Version `0.94.1`/code 161 explicitly enables that normal permission, and the export gate now rejects any future APK that omits it. Installed in place on the 1080×2424 API-35 AVD through ARM64 native translation, the corrected build completed trusted public TLS, disposable authentication, exact local-save archival, server clock and ownership, accepted/replayed/conflicting profile commits, authoritative refetch, atomic read-only cache and clean reconnect. The app-private mailbox was absent afterward, package data was never cleared and sanitized evidence is retained only in ignored artifacts. The complete 69-test gate plus clean boot passes in 168.41 seconds. The published 34,393,567-byte APK passes content, API 24+, manifest permission, synchronized version, stable signature and reference boundaries with SHA-256 `2b5ec328e87358be9277697f881dfad5929f46350536078cee2e100b2b880028`. This closes Android-runtime connectivity and certificate evidence on an emulator; physical touch latency, thermal/storage behavior and vendor lifecycle remain honest later gates. Online capabilities remain false because the ordinary APK still boots offline and account/profile activation has not been integrated into player flow.

## Current priorities

1. Keep vector class emblems and code-drawn habitat motifs active. Any externally supplied raster candidate must pass the evidence, user-approval and 450x800 intake gate before its exact file may be integrated; Codex must not generate or modify the candidate.
2. Physically validate the paged Galaxy and Network Circuit on Android: traverse all seven route pages, open a new destination directly on its page, confirm the marker is legible on all three mission roles, and check both dossiers at 100%/125% text.
3. Physically compare first-visit versus repeated Android navigation for Arsenal, Market, Career, Galaxy, and the three-offer board; record whether remaining hesitation correlates with cold estimates, first-time Control construction, or transactional storage.
4. Exercise the full 5/8/12/16/20-minute cadence: dismiss an event with its button and Android Back, leave another open past its deadline, choose every detour, background the application, and verify one-shot toast/vibration on return.
5. Measure save latency with a mature physical inventory before considering deferred backup work. Never weaken the latest-transaction backup guarantee from desktop-only measurements.
6. Measure return behavior before adding energy, paid skipping, transport rentals, longer routes, or scheduled system notifications. Preserve the rule that transports discount travel only; add Android receiver/plugin infrastructure only if killed-app reminders prove necessary in physical testing.
7. Validate migrated physical saves with old chapter finales, completed planets, accepted contracts, active hunt/incident timers, and owned transports. Compatibility state may remain, but new missions must never re-enter sequential chapter progression.
8. Rebalance level-banded combat, rewards, and long-term prices only from cohort simulations and physical play. Keep the level-100 economy guardrails and never derive an enemy's statistics from current equipment power.
9. Keep a fourth Fenda reality frozen until a second-year projection and representative builds above level 215 exist. The corrected checkpoint-aware calendar leaves three current enemies beyond the fastest approved year-one profile; prioritize transversal variety rather than another vertical ladder.
10. Continue physical checks of all eight cosmetic species, the three class identities, all three Fenda realities, universal equipment responses, Android input latency, and native feedback intensity.
11. Validate the already operational public TLS client path on physical Android, including certificate trust, background/resume cache behavior and explicit local-save archival cutover. Keep the normal APK endpoint-free and keep Agency and billing independently false until their own server transactions pass.
12. Deploy the proven economy/build runtime to staging and execute public-TLS Windows lifecycle evidence before considering any client capability. Keep physical Android lifecycle evidence and production-save cutover as independent later gates; every normal APK flag remains false meanwhile.

297. Version `0.94.2`/code 162 establishes direct-content scrolling as a global interface contract. Every runtime list now uses the shared touch-aware container: a finger drag that starts over a card or button propagates to its parent after the eight-pixel deadzone, while taps, text fields and sliders retain their normal interaction. Both scroll rails are fully transparent, ignore pointer input and carry no custom minimum width; the onboarding species roster no longer forces the former 24-pixel visible rail. The UI asset inventory now explicitly rejects a visible scrollbar as a required deliverable. The complete 69-test fast gate plus clean boot passes in 179.78 seconds, including a synthetic finger drag over the species roster and a regression that requires its rail to be invisible and non-blocking. The update installed in place on the API-35 Pixel 9 AVD; an actual Android swipe moved the roster from its first card to the final species with no lateral rail rendered. Export review also caught a locally captured emulator screenshot being imported from the ignored `artifacts` workspace; both platform presets now exclude that complete evidence tree, the export checker protects the exclusion, and screenshots and staging logs remain outside future binaries. The published 34,393,567-byte APK passes API 24+, ARM64, manifest, signature, version, content and reference boundaries with SHA-256 `85b9ea80e0022188dc5133fe7a465ac00e534b378556fdc5b96b85e21a4beb89`. No production visual asset, save, balance or online-authority change was made.

298. Version `0.94.3`/code 163 closes the first post-audit Android UI and idle-performance batch. Ordinary and newly unlocked warrant reward actions now explicitly restore the Wanted Board instead of retaining a stale Menu destination; a complete 2m33s AVD hunt, automatic combat, reward claim and return proved the route and exposed the renewed three-contract network. Three-offer selectors reserve and clip enough vertical space for the optional Weekly Circuit marker; both Portuguese and English 1080×2424 captures keep target, route, role, odds, payment and duration inside every card. The Hunter sheet uses a compact localized Relic label, while Series families become bounded progress cards; undiscovered families show only slot, sealed-record state and progress instead of repeating two raw absence sentences. Static scenes enable Godot low-processor mode only after two presented frames: direct project-level activation was rejected after a Pixel 9 AVD cold launch remained black until touch, while the deferred final build cold-started without input and measured 0% process CPU on repeated static Board and Series samples versus the earlier 34–38% directional AVD readings. Local Android evidence now owns a tracked `.gdignore`, so Godot no longer imports ignored captures, and the backend export audit correctly accepts `backend/*` anywhere in each exclusion list instead of throwing a null-property false failure. The complete 69-test fast gate plus clean boot passes in 166.81 seconds. The published 34,393,567-byte ARM64 APK passes API 24+, manifest, synchronized version, signature, content and reference boundaries with SHA-256 `560c0ee3dd5a762c3c824a4a171c622cdf8b165b3d8bc379c78e5bc4fa88f51d`. No production artwork, balance, save schema or online capability changed.

299. Version `0.95.0`/code 164 establishes the fail-closed authority boundary required before moving the local hunt economy to Nakama. The project audit confirms that the 35-world/140-target year-one catalog is complete, remaining UI performance questions require physical Android evidence, and server-backed Agencies cannot honestly precede normal server-owned hunts because current remote characters begin at level 1. Three newly reserved mutations therefore carry intent only: `hunt_accept` selects canonical board/offer/target/approach IDs, `hunt_resolve` names one accepted hunt, and `reward_claim` names the sealed server reward plus `store`, `equip` or `recycle`. Client-authored fuel, costs, timestamps, odds, damage, victory and reward values are rejected recursively and by exact payload shape. A canonical economy snapshot binds account, character, shard, revision, UTC, level, XP, currencies, fuel, inventory revision, active hunt and pending reward as one non-mergeable server unit; foreign ownership, excess fuel, malformed timing, unknown wallet fields and overlapping hunt/reward phases fail closed. The deployment gate now orders profile → economy → Agency/billing, while `economy_backend` and every existing remote capability remain false and the normal APK stays endpoint-free. The artist-facing package is also reconciled to the approved single-rule brief, and the separate English asset file remains an inventory of names and quantities only. Read-only benchmarks keep the known Arsenal/Series cold path near 51 ms but warm at 12–13 ms, so no speculative UI rewrite was attempted without physical evidence. The complete 70-test fast gate plus clean boot passes in 161.07 seconds, including repository, documentation, reference, backend, mobile, localization, persistence and new remote-economy guards. The 34,401,934-byte ARM64 APK passes API 24+, manifest, synchronized version, stable signature, content and reference boundaries with SHA-256 `5f73500924c899a704b86da871e3b93e99545ea0132a96fdb86998c43897ed77`. No production artwork, local gameplay balance, save schema, server mutation or online capability changed.

300. Version `0.96.0`/code 165 implements the first complete server-owned hunt transaction on the loopback-only Nakama stack without activating it in the game. Existing ten-field remote characters migrate in memory to one canonical eighteen-field profile, so level, XP, currencies, daily fuel, inventory revision, active hunt and sealed reward share the same optimistic revision instead of drifting across duplicate stores. The server publishes three first-world offers, validates the exact board/offer/target/approach tuple, spends five fuel once, creates an authoritative UTC deadline, refuses early resolution, seals the server-calculated tutorial capture reward and applies Credits, XP and exactly one store/equip/recycle decision. Every accepted mutation is atomic with its receipt; exact retries return `duplicate`, mismatched keys reject and stale revisions conflict without a second spend or claim. The isolated local environment reduces the authored 144-second Dustball Prime duration to two seconds for repeatable tests, while staging keeps the authored value. The TypeScript runtime compiles cleanly and the backend workspace plus PowerShell smoke harness pass static validation. The complete 70-test Godot gate and clean boot pass in 163.02 seconds. Docker Desktop was unavailable during this batch, so execution of the expanded live Nakama smoke test remains an explicit local gate before staging; shared generation of all 35 worlds and the full class/build combat resolver also remain pending. The 34,401,934-byte ARM64 APK passes API 24+, manifest, synchronized version, stable signature, content and reference boundaries with SHA-256 `b3d810cf7f802e9e1745bc6a444d55d5c98fa9376254ef3a09cb170c4fb79c9d`. `economy_backend` and every other online capability stay false, ordinary APK boot remains endpoint-free, and no local save, gameplay balance or production artwork changed.

301. Version `0.97.0`/code 166 removes the remaining duplicated first-world server catalog and guaranteed tutorial victory. One Godot generator now derives a 28,000-byte TypeScript manifest directly from the canonical registry: all 35 planets, 140 target identities, three pressure roles, `quiet_net`/`hot_hatch`/`premium_warrant`, all three class effects, numeric enemy profiles and pacing constants are bound by content hash `6c83967b74498707bdca23128004162fb7286e7aec0b3f9062d117b54814cec8`. A freshness test compares the generated source byte for byte and rejects localized presentation fields. Remote characters migrate compatibly from ten or eighteen fields to one 23-field server profile containing attributes, base power, nine equipment slots and inventory. Hunt acceptance snapshots the selected approach's complete target pressure, reward, duration, loot tier, enemy modifiers and content revision, so an in-flight contract remains deterministic after catalog updates. Resolution now runs up to 100 alternating rounds with equipment power, armor, integrity, attributes, planetary set bonuses and the distinct Breaker mitigation/counter, Gunslinger accuracy/evasion/follow-up and Hacker opening/bypass mechanics; its server-issued receipt preserves the combat result for exact retries. Reward decisions now store the concrete item, equip its universal slot or recycle its server value, and level-ups grant the canonical +2 base power and two attribute points. The compiled runtime self-tests all three class identities and a real starter recovery fight at initialization. The complete 71-test Godot gate and clean boot pass in 175.81 seconds, while TypeScript compilation, runtime initialization, manifest freshness, backend workspace and PowerShell syntax gates also pass. Docker Desktop still fails to start its Linux engine on this workstation, so the expanded HTTP/storage smoke remains explicitly pending before staging. Every online flag stays false; no local save, gameplay balance, production visual or exported endpoint changed.

302. Final packaging verification measures the generated manifest at exactly 28,096 bytes. The 34,406,225-byte ARM64 Android APK exports successfully as `com.crookedgalaxy.game` version `0.97.0`/code 166, passes the reference-free pack audit and stable test-signature checks, and has SHA-256 `b8fc894bad9297e343b1afd269fce57c6902ece3bb230e609656d4b5f5e69281`.

303. The local Docker gate is unblocked without a factory reset or loss of volumes. Docker Desktop 4.88.1 was failing on inaccessible stale Windows AF_UNIX runtime sockets under `AppData/Local/Docker/run` and `AppData/Local/docker-secrets-engine`; both runtime directories were preserved as dated backups, the hanging CLI launcher was removed and the installed Desktop executable was started directly after `wsl --shutdown`. Docker Engine 29.7.2 and WSL2 then started normally, the official `hello-world` container passed, and pinned Nakama 3.40.0 plus PostgreSQL 16.8 rebuilt and became healthy. The expanded live smoke proves authentication, session ownership, UTC, concurrent idempotency, stale-revision conflicts, forgery rejection and a complete server-authoritative three-offer hunt, deterministic combat, sealed reward and inventory claim. One PowerShell-only assertion was hardened to count an empty JSON object's properties as an explicit array. Staging and gameplay activation remain pending; all online flags stay false.

304. Version `0.98.0`/code 167 closes the remaining local build-authority boundary before staging. A separate exact build snapshot binds the character revision to base power, five attributes, unspent points, inventory revision, nine equipment slots and the full owned-item list; malformed values, duplicate identities, foreign ownership and non-starter equipment absent from inventory fail closed. `attribute_allocate`, `inventory_equip` and `inventory_recycle` accept only positive allocation intent or one owned item ID: the server spends earned points, preserves ownership while equipping, protects equipped items and calculates salvage itself. Every mutation shares the existing optimistic revision and atomic idempotency receipt. Reward equip now records the dropped item in ownership instead of replacing equipment invisibly, while hunt acceptance freezes class, level, attributes and equipment so later workshop changes cannot rewrite an in-flight combat result. The rebuilt live Nakama/PostgreSQL smoke proves two complete hunts, level 2, exactly two earned/spent points, exact and duplicate-safe equip/recycle behavior, equipped-item protection, races, conflicts and forgery rejection. The complete 71-test Godot gate plus clean boot passes in 174.68 seconds; TypeScript, backend, documentation and Docker gates also pass. The 34,410,321-byte ARM64 APK passes API 24+, synchronized version, stable signature, content and reference boundaries with SHA-256 `1d22be4b9cf2d4b61a6429d8d30604b117561715d0b854bffb0eec6038b317ff`. Every online capability remains false; no production visual, local save or local gameplay balance changed. The next gate is staging deployment and public-TLS Windows lifecycle evidence, with physical Android remaining independent.

305. Commit `70e2a5494611d92814a405afef698638964b9b38` deploys version 0.98 to the Helsinki staging host through a clean fast-forward, with the prior Nakama image `sha256:bb459f2b95ca385cdb759d0a23789cb34a021515ab779d7e1186575a984b421a` retained as the code rollback identity. Before deployment, encrypted Storage Box archive `staging-2026-08-30T11-59-35Z` passed checksum verification. Its first recovery attempt exposed an initialization-server race in the isolated PostgreSQL drill: one successful readiness probe could occur immediately before the official image restarted into its final server. The drill now requires six consecutive SQL `SELECT 1` probes and the same recovered archive subsequently restored all 20 public tables in a disposable volume. The server checkout remained clean, staging validation passed, and Nakama rebuilt to image `sha256:57130af79e725008c44f394af74e0281dd664e0c03024b8b9c17da847fd4455a`; Caddy, Nakama and PostgreSQL are healthy. Public TLS/HSTS smoke proves authentication, UTC, character/build ownership, rejection of unearned attributes, revisions, exact replay and stale conflict behavior. A normal Godot 4.7.1 Windows boot then proved public TLS, archival local-save cutover, authoritative profile, acknowledged read-only cache and clean reconnect with the client key held only in process memory. Nakama logs register protocol v1 once against content `6c83967b7449`, Caddy redacts authorization headers and neither service reports a deployment error. The headless probe still reports three ObjectDB instances during forced test teardown; this is recorded as harness cleanup debt rather than ignored or treated as production evidence. Physical Android lifecycle and every gameplay capability remain disabled.

306. Version `0.99.0`/code 168 extends the inert Godot adapter through the complete deployed authority slice without enabling it in ordinary gameplay. Exact server-owned economy and build reads now join a content-hash-bound, ownership-checked three-offer board; the canonical board deliberately exposes selectable identities, duration and fuel while discarding hidden combat authority. Hunt accept/resolve, reward disposition, attribute allocation, equip and recycle wrappers all construct intent-only commands, verify their optimistic revision/idempotency receipts and independently canonicalize the returned economy or build snapshot. A disposable account traversed the ordinary Windows main scene over public TLS: the server rejected an early resolution, preserved the hunt for its real 147-second duration, accepted and replayed resolution deterministically, issued and stored one sealed drop, equipped it, rejected recycling while equipped, archived the unrelated local save family, opened only a read-only offline cache and selected the remote revision on reconnect. No endpoint, credential or session was persisted or printed. The complete 71-test Godot gate plus clean boot passes in 175.95 seconds, and the focused local Godot/Nakama integration also passes. The Android staging harness now allows 240 seconds and recognizes the expanded proof, but physical Android remains pending. The published 34,418,513-byte ARM64 APK passes API 24+, synchronized version, stable signature, content and reference boundaries with SHA-256 `7d45a3c3a05ca52a0191ce955ac08a7c0d0338f934a077c6397a457dfe4c3f6b`. All online capability flags remain false; no local save, gameplay balance or production visual asset changed.

307. Version `0.99.1`/code 169 separates technical server proof from authorization to activate the normal client. The existing ordered capability evidence still describes what staging has demonstrated, but a second fail-closed gate now also requires physical Android TLS and lifecycle, a real-save archival cutover, the normal login flow and explicit activation approval before even account/profile can become available. Economy additionally requires physical authoritative-hunt and build-mutation evidence; Agency and billing preserve independent product-flow evidence and approvals. Missing-gate diagnostics distinguish unfinished client proof from already completed server authority. The first post-migration headless run also exposed that the restricted execution context could not write Godot state on the new SSD and the Windows binary crashed instead of reporting that boundary cleanly. The copied `.godot` cache was preserved outside the repository, rebuilt under the explicitly authorized D: context and then used for all validation. The complete 71-test fast gate plus clean boot passes in 154.52 seconds directly from `D:\Projects\Crooked Galaxy`. International 1 and every exported capability remain offline and false; no endpoint, credential, save, economy, UI or production asset changed.

308. Version `0.100.0`/code 170 turns the proven staging lifecycle into one reusable Godot boundary without connecting it to ordinary gameplay. `RemoteSessionCoordinator` begins inert, accepts only explicit local or staging configurations, authenticates through the reviewed adapter, verifies server clock plus account/character ownership and independently canonicalizes every adopted snapshot. Missing characters remain a distinct authenticated state before explicit test creation. Established local progress can cross the boundary only through the exact archival choice: the complete save family is copied and hash-protected while active sources remain untouched; ambiguous merge/import choices fail closed. Disconnect writes and immediately reopens the bounded cache as read-only with economy, social and billing mutations false, while reconnect compares ownership and revisions through the existing conflict rules. The public-TLS staging probe now uses this coordinator instead of duplicating connection, cutover, cache and reconnect orchestration. The coordinator rejects production, nested credentials and foreign ownership, is not an autoload and is never instantiated by `GameState`; International 1 and every exported capability remain offline and false. Dedicated fake-adapter coverage proves inert startup, production rejection, owned creation, credential-free summaries, archival confirmation, read-only disconnect and foreign-account rejection. The expanded 72-test fast gate plus clean boot passes in 157.18 seconds directly from the migrated D: checkout.
